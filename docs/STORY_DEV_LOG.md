# STORY_DEV_LOG.md — 스토리 관련 과거 결정·작업 히스토리 아카이브

> **이 파일의 역할:** `HANDOFF.md`가 너무 길어져 그 안에 섞여 있던 **스토리/서사 관련 과거 결정과 작업 기록**을 따로 빼둔 파일. 대부분 지금은 `docs/STORY_BIBLE.md` v2로 대체된 v1(길손=현감·수문·이미 죽은 정인 윤슬) 시절 기록이라 당장 쓸 일은 없을 수 있음 — 나중에 "그때 왜 이렇게 정했더라" 싶을 때 참고용으로 남겨둠. 시스템/코드 설명이 필요하면 `HANDOFF.md`·`CLAUDE.md`를 볼 것(여기 옮겨진 항목도 당시엔 코드 작업이 섞여 있지만, 핵심은 서사 쪽 결정이라 이쪽으로 옮김).
>
> 최신이 위로. 원문은 손대지 않고 `HANDOFF.md`에서 그대로 옮겨왔음.

---

## PART 2 「현재 상태」 발췌 (세션 히스토리)

**추가(같은 텔레그램 세션 — 사용자 "이미지는 나중, 스토리부터 완성" 지시):** 아트 작업 전부 보류(PixelLab 갱신 후로) + 사이드 진혼을 1기 → **3기로 확장**. 4막(`haewon_side_lost_wraith`, 신칼 두고 간 무당마저 못 푼 넋) / 6막 전반(`haewon_side_nameless_servant`, 곳간 그늘의 이름 없는 종) / 7막(`haewon_side_buried_together`, 증조모 곁에 함께 묻힌 넋) — 세 곳 모두 STORY_BIBLE 톤(신분·익명성·복수의 희생)에 맞춰 명명. `EndingResolver.BURN_FLAGS` 는 이제 4단서+3전투=7종. **다음(최우선):** PC/CI에서 헤드리스 그린 확인(특히 `test_gameplay_chain`·`test_haewon_chain`·`test_endings`·`test_story_data` — 그룹 리네임이 옛 스코어어택 체인에도 영향), 이상 없으면 푸시 승인 요청.

**지금 여기 (2026-08-18 후반 — v2 4~7막 + 엔딩 4종 완성, 장면 정합 수리):** 사용자 피드백 "스토리는 괜찮은데 **상황에 맞는 장면**이 나왔으면 한다(어색한 부분이 있다)" + "4~7막과 엔딩도 진행". **①장면 정합:** 가장 어색했던 원인은 **담 안 장면 뒤로 원경 산맥·솔숲이 보이던 것**. `parallax_backdrop` 에 `mountains`/`clouds` 토글을 신설하고 stage/cutscene JSON 에서 `mountains·clouds·mist·horizon_y·night_luminance` 를 넘기게 배선 → 마당·우물·곳간·사당·뒤란에서 산이 사라짐. 「마당의 매질」 컷신도 돌바닥+담+기와집 본채로 다시 짜 '닫힌 마당'으로 만들고, 안마당/사당/뒤란 소품을 촘촘히 깔아 빈 화면을 없앰. **②4~7막:** 4막(gut3) 「기와집에 재앙이 든다」 — 굿판이 줄줄이 실패하고 **큰무당 입으로 '저것들은 대감 같은 분이 부른 사람 말은 안 듣는다'** 를 말해 왜 길손뿐인지를 설정으로 못박음. 5막 「부탁」 = `CutRequestNight` 컷신, **첫 엔딩 분기**(승낙/거절/말없이 등 켜기). 6막(gut4·5) 우물의 여종 → 곳간의 아이들 → 사당, 주제문 **"너도 우리 같은 것이, 어찌 저들을 돕느냐"**, 사당 앞 걷는 회상 `CutShrineNight`(삼대 전 입을 막던 밤). 7막(gut6) 뒤란 땅속 = **증조모**, 마지막 진혼 후 **엔딩 분기 선택**(자기 한까지 놓을 것인가). **③엔딩 4종:** `scripts/story/ending_resolver.gd` 신설 — 저울 하나(얼마나 자기를 태웠나 = 선택 단서/진혼 수) + 마지막 선택 + 숨김 조건. **③소멸 > ④윤슬(아낌+증조모 이름+회상) > ①망각(많이 태움) > ②해원** 순으로 판정하고, 엔딩 화면이 제목·본문 4줄·등불 수·빛깔을 달리 연출한다. **검증:** 헤드리스 **46스위트 전부 그린**. 신규 `test_endings`(판정 9조합 전수 + **네 엔딩 모두 도달 가능** + 연출 데이터 채움 + 실 플래그 경로), `test_haewon_chain` 은 v2에 맞춰 **7굽이 전수 배선 검사**로 교체, `test_cutscene` 은 컷 래퍼 4종으로 갱신. 안마당·곳간·뒤란·마당 컷신 렌더 확인. **커밋 `24067b7` 푸시.** **⚠ 남은 것:** 매질 장면의 **무릎 꿇은 포즈 아트가 없어 서 있는 스프라이트로 대체**(hurt 시트 프레임 슬라이싱이 깨져 되돌림 — 아트 작업 필요), 씬 파일명이 v1 잔재(Ferry/Market/Watergate/EmptyTown), 사이드 진혼(선택 전투)이 아직 '단서 줍기'로만 구현돼 저울이 얕음.

**이전 (2026-08-18 — 스토리 대개편 v2 확정 + 1~3막 수직 슬라이스):** 사용자가 새 줄거리 7단계를 제시 → **STORY_BIBLE v2 로 전면 개편**(커밋 `323e070`). **폐기:** 길손=현감·수문·홍수, 이미 죽은 정인 윤슬, 강 6굽이 하룻밤. **새 뼈대:** 주인공은 **세습무 집안의 박수무당(팔천)** — 넋을 달래주고 삯 대신 쌀을 받으며 문간 밖에 서는 자. 여주는 **살아 있는 양반가 규수**(아명 '윤슬' 승계 — 회상·엔딩 회수 자산 유지). 무대는 강 6굽이 → **서씨 기와집 한 채**(담장 밖 → 안마당·우물 → 곳간·별당 → 사당 → 뒤란 땅속), 사흘 밤. **악귀의 정체 3겹:** 우물에 던져진 여종 / 굶어 죽은 아이들 / **삼대 전 산 채로 묻힌 무당 = 주인공의 증조모**(이 집이 잘살게 된 값이 제 핏줄). **무당들이 못 푸는 이유를 설정으로 못박음:** 원혼은 양반이 부른 무당의 말을 듣지 않는다 — 같은 천한 자의 말만 듣는다. **사용자 확정 3건:** ①엔딩 **3+1**(망각/해원/소멸/숨김 '윤슬', 저울 하나 = 얼마나 자기를 태웠나) ②**대가 = 기억 소거 채택** — `MemoryLedger` 를 그대로 쓰되 의미 전환: **그녀를 지키려 싸울수록 그녀를 잊는다** ③구현은 1~3막 수직 슬라이스 먼저. **구현(커밋 `5a88fa2`):** 씬 파일명(Haewon0~2)은 유지하고 **내용만 교체**(참조 깨짐 0) — 1막 「넋을 달래는 아이」(문간의 쌀·마당의 밥상으로 신분을 대사 없이 체감, 어머니 복선), 2막 「물빛」(규수와 첫 대면 + 아명 '윤슬'의 뜻 심기 → **그 목소리를 바로 잃음**), 3막 「문턱」(수군거림 → 담장 밖 봉분 셋 → 대문 앞 은가락지 → **컷신 「마당의 매질」**, '네 어미도 무당이었지' 직후 어머니 얼굴 소거). `MemoryLedger.FRAGMENTS` v2 교체, `CutYardBeating` 컷신 신설. **`tests/test_story_data` 신설** — 대사·스테이지·컷신 JSON 전수 파싱 + 끊긴 next·죽은 파일 참조·굽이 조각 누락 검사(스토리가 전부 JSON 이라 오타가 런타임에야 터지던 문제). **검증:** 헤드리스 **45스위트 전부 그린**, 1막 스테이지·3막 컷신 렌더 확인(`shots/verify/story_*.png`). **⚠ 남은 것:** 4~7막 + 엔딩 4종 미구현, 스토리 모드 진입로(`GAMEPLAY_ONLY=true` 라 메뉴에서 못 감 — 진입 방식 결정 필요), 씬 파일명이 v1 잔재(Ferry/Market/Watergate)라 나중에 일괄 개명 검토.

**추가(같은 날, Ⓒ 선택 — 환경 서사 확장):** 사용자 "C로 진행". STORY_BIBLE '환경 서사' 도구로 6굽이에 흔적을 더 흩음(잠긴 6굽이/기억소거 구조는 안 건드림). 새 흔적: 1굽이 **부서진 나룻배**(내레이션+'윗고을 수문 닫음' 흘림, `ferry_boat.json`)·3굽이 **못 떠난 넋/차사 매듭**(`village_chasa.json`)·5굽이 빈 안방 **꽃신·가락지** 픽업 추가(바이블의 비녀+꽃신+가락지 4소품 중 셋 채움). 후반(고진행도)은 글자 dissolve로 안 읽히므로 **pickup 평문**, 초·중반은 **내레이션 auto_dialogue**로 배치(읽힘 보장). `haewon_ending.gd`를 단서 6종 일반화 → 발견 수만큼 등불 3~9개 + 마지막 줄 4단계 변주(`CLUE_FLAGS`). ⚠ 여전히 Godot 없어 미검증 — PC에서 단서 발견·엔딩 변주 눈 확인 필요. 남은 (e): 비녀/꽃신/가락지/물등 **전용 PixelLab prop**(현재 charm 글린트 placeholder).

**추가(같은 날, Ⓒ 계속 — 서사 확장 2: 진혼-후 공백 + 동틀녘 시간선):** 사용자 텔레그램 "서사 확장 진행". 데이터만(폰·🟢). ① **2굽이 진혼-후 공백 메움** — 저잣거리(gut 2)는 전투 굽이인데 유일하게 `clear_dialogue`가 없어 '자리(the_seat)' 기억 소거가 조용히 일어났음 → `market_clear.json` 신설(통문 수결이 제 손에 익다는 자각이 막 떠오르려는 찰나 그 '자리'의 기억이 도로 지워짐 = '기억이 지워짐' 시그니처를 2굽이에서도 체감) + 스테이지에 배선. ② **동틀녘 굿(STORY_BIBLE Ⓓ) 시간선을 텍스트로** — 그간 sky 색으로만 있던 '하룻밤·동트기 전에 끝내야 함' 압박을 처음 대사로: `night_deepens.json`(2굽이 진입, 하룻밤 데드라인 명시+망각 엮음) · `dawn_near.json`(5굽이 진입, 동 트기 직전 압박→엔딩 '첫 햇살' 페이오프와 연결). 황혼(프롤로그)→깊은 밤(2굽이)→동틀녘(5·6굽이) 아크 완성. **검증:** 신규 3종 포함 haewon 대사·스테이지 JSON 전부 파싱·스키마(id/start/nodes/next)·참조 무결 OK, test_haewon_chain 불변식(전진출구·결계·gut소거) 영향 없음. ⚠ Godot 없어 헤드리스·시각 미검증 — PC에서 팝업 타이밍·읽힘 눈 확인.

**추가(같은 날, Ⓒ 계속 — 회상 '컷 전환' 시스템 신설 + PixelLab 인물 선제작):** 사용자 텔레그램 "대사가 나올 때 새 맵으로 전환해 그 장면을 보여주고 전투로 복귀하는 기획이 있어야" → "B로(걷는 회상도 컷 통일) + PixelLab 미리 만들고 PC선 적용만". **만든 것(데이터·코드, 폰):** ① **컷 전환 시스템** — `scripts/world/cutscene.gd`(데이터 기반 '기억 맵' 빌더: backdrop/ground/props/figures/camera/tone/dialogue, figure PNG 없으면 수묵 실루엣 폴백) + `SceneManager.play_cutscene/return_from_cutscene`(돌아갈 전투 씬+entry 기억, Cut* 씬은 autosave 제외 → 컷 도중 종료해도 전투맵으로 복귀) + `stage.gd` 굽이 JSON에 `clear_cutscene` 트리거(굽이 클리어→기억 소거→전용 회상 씬으로 컷 전환→복귀). **전투맵은 클리어 시 게이트 flag set → 복귀해도 적·결계 재생성 X, 컷 재발동 X(무한루프 없음).** ② **회상 2장**: `assets/cutscenes/recall_yunseul.json`(1굽이 후 — 강가 동틀녘, 젊은 길손+윤슬, 따뜻한 톤, 대사=recall_yunseul) · `watergate_flashback.json`(4굽이 보스 후 — 수문 그날, inkwash 수묵, 대사=watergate_reveal) + 래퍼 `scenes/cutscenes/CutRecallYunseul.tscn`·`CutWatergate.tscn`. haewon_1/4 JSON: `clear_dialogue`+`clear_inkwash`→`clear_cutscene`로 교체 + `from_recall` 복귀 entry(x=1280) 추가. ③ **PixelLab 인물 선제작**: 윤슬(옥색 저고리+아이보리 치마, ID `2954c31a`)·젊은 길손/현감(흑립+남색 관복+흉배, ID `d61c4810`) v3 측면 생성 → **east 프레임을 이미 repo에 커밋**(`assets/sprites/cutscene/{yunseul,gilson_young}/idle.png`). 발 정렬은 `cutscene.gd`가 런타임 `Image.get_used_rect()`로 처리(캔버스 여백 보정). 재생성용 `tools/pixel/integrate_cutscene.py`(east 재다운로드+autocrop)도 추가했으나 **PNG가 이미 repo에 있어 PC선 실행 불필요.** PixelLab 4 gen 사용(잔여 ~1802). **⚠ Godot 없어 미검증.** **PC 적용(=Godot 1회 열기뿐):** `git pull` → Godot 에디터 열어 새 PNG/.tscn/.gd import(.import·.uid 생성) → `플레이.bat`로 1굽이·4굽이 클리어 시 **회상 맵으로 컷 전환됐다가 전투로 복귀**하는지 눈 확인(인물 접지·카메라 프레이밍·대사·복귀 위치). 어긋나면: 인물 위치/scale는 cutscene JSON `figures`, 카메라는 `camera.x/y`, 복귀 위치는 stage JSON `from_recall` entry x.

**추가(같은 날 — 회상 3번째 '윤슬 잃던 밤' + 위치 트리거 신설):** 사용자 "다음 회상도 진행". 미싱 비트(가장 아픈 기억)인 **홍수 그 밤 윤슬을 잃음**을 5굽이(빈집)에 추가. 5굽이는 전투 없어 게이트(=clear 트리거)가 없으므로 **위치 기반 컷 트리거 `auto_cutscene` 신설**: `scripts/world/auto_cutscene.gd`(밟으면 play_cutscene→복귀, once_flag) + `stage.gd._build_auto_cutscenes` + JSON `auto_cutscenes` 키. 5굽이 빈집 대사(x=1180, 비녀 발견) **뒤** x=1340에 트리거 → `CutFloodNight.tscn`(`flood_night.json`+`flood_night` 대사). 연출은 절제: **윤슬은 화면에 안 보이고 물 밖 목소리만**(serene idle 재사용 부적합 회피), 젊은 길손이 수문 앞에 선 수묵 한 폭(inkwash) — '열면 성안이 잠긴다…닫으면' 하고 빗장을 내리는 그 순간(4굽이 '그 문을 내린 자가 나다'의 인간적 대가, 반전 2 정서핵). **새 PixelLab 불필요**(기존 gilson_young 재사용). 5굽이에 from_recall(x=1340) entry 추가, once_flag로 복귀 후 재발동 없음. **검증:** JSON·참조 무결, 들여쓰기 일관. PC 적용은 위와 동일(Godot 1회 열기). 현재 회상 컷 **3장**: 1굽이 윤슬강가 / 4굽이 수문그날 / 5굽이 윤슬잃던밤.

**이전(23차):** **시그니처 '기억이 지워짐' 시스템 + 6굽이 스테이지/대사 데이터 구축 (2026-06-29, 23차).** 22차에서 주인공 '해원→길손'(해원=게임 제목=한 푸는 일) + 무대(물등·동틀녘·빈집) 확정 후, 바이블의 '다음 제작 순서' 2·3단계를 실제로 만듦. ① **시그니처 시스템(우리 게임 한 방) `MemoryLedger` 오토로드**(`scripts/memory/memory_ledger.gd`) — 원혼 한 굽이를 진혼할 때마다 그에 얽힌 기억 조각이 지워짐(속죄=망각=자기소멸). 굽이별 7조각(프롤로그~6굽이), `erase_for_gut(gut)`·`progress()`·`is_erased(id)`·`screen_names()`(마지막엔 '윤슬' 하나→빔)·`last_name_released` 신호, SaveManager `"memory"` 키 연동. ② **순수 글자 변환 `MemoryGlyph`**(`scripts/memory/memory_glyph.gd`, class_name) — 텍스트+소거비율→흐려진/지워진 글자(BBCode dissolve / 공백 strip), **결정적**(같은 입력=같은 결과, 비율↑=소거 단조증가)이라 헤드리스 검증 가능. ③ **6굽이+프롤로그 스테이지 JSON 7장**(`assets/stages/haewon_0_prologue~6_yunseul.json`) — 바이블 비트시트대로 물/밤/동틀녘 톤, 적=기존 넋 아트 재활용(Reaper/Goblin/Fox, 4굽이 보스 ReaperLord, 6굽이 FoxQueen), 강 따라 체인·전진게이트·환경서사 픽업(통문/비녀). ④ **1막+전막 대사 14편**(`assets/dialogue/haewon/*.json`) — 프롤로그 독백·뱃사공·**윤슬 회상(이름의 뜻)**·수문 반전('그 문을 내린 자가 나다')·빈집·**엔딩 회수 대사**까지 사극체로. ⑤ 헤드리스 테스트 `test_memory_ledger`(ledger 5 + glyph 3 케이스) 추가(CI 자동 발견). **검증: 이 환경(WSL) Godot 없음 → 신규 JSON 21장 전부 파이썬으로 유효성 OK, GDScript는 자동로드 파싱 안정성 위해 typed-const/리터럴 반환 보수화. 헤드리스 테스트 실행·실제 글자 dissolve 연출은 PC 대기.** ★남은 결정 없음(무대 확정). **다음 배선 단계(미완)**: 스토리 모드 복원(`stage.gd` `GAMEPLAY_ONLY=false`) + 굽이 클리어→`MemoryLedger.erase_for_gut(gut)` 연결 + 새 스테이지 .tscn 래퍼 7개 + 대사/HUD 글자에 MemoryGlyph 적용 + '걷는 회상' 수묵 흑백 오버랩 + 물등 PixelLab 아트. (지금은 데이터·시스템만 추가해 현재 부팅(GAMEPLAY_ONLY=true 전투체인)은 안 건드림.)

**이전(21차):** **사랑하는 이 이름 '윤슬' + 문 닫은 사유 '수문(水門)·홍수' 확정 (2026-06-29).** 20차 코어 위에, 사용자 텔레그램 지시로 두 가지 교체: ① 정인 이름 **여울 → 윤슬**(평소 안 쓰는 순우리말 = '물에 부서지는 빛', 물+빛을 한 단어에 담아 엔딩 '첫 햇살이 강에 닿음'과 직결), ② 문을 닫은 사유 **기근/돌림병 → 수문·홍수**(이 게임은 '물의 게임'이라 문 닫은 것도 물이어야 강·물등·동틀녘 모티프가 한 줄로 꿰임). 비트시트의 죽음·단서를 전부 **물(수몰)** 로 통일(뱃사공=급류 익사, 저잣거리·강마을=수몰, 4굽이 보스=닫힌 수문). 윤슬 뜻을 자연스럽게 심는 **'걷는 회상' 대사**("물에 빛 드는 걸 보셔요, 거기 어디 제가 있을 터이니")를 추가 — 엔딩에서 회수되어 터짐. (`docs/STORY_BIBLE.md` 반영 완료.) **확정 코어:** 역할/이름 **'해원'**, 당위 **ⓑ 수문의 선택 + ⓔ 정인 윤슬**(둑 밖 마을을 물에 내주며 정인까지 잃은 현감). **시그니처 둘:** ① 전투=진혼(후반 의미 전복 — 15차 '혼 흩어짐' VFX 톤 일치) ② 진혼마다 대사·HUD·이름 글자 소거(끝엔 '윤슬' 한 이름만 남고 그마저 소거=자기 천도). **★남은 결정 1개:** 무대 그릇 — 수문·강 코어로 '물등(Ⓒ)+동틀녘 굿(Ⓓ)+빈집(Ⓐ)' 융합이 사실상 정답에 가까움(최종 확인만). 그릇 확정되면 스테이지 JSON 스켈레톤 + '기억 지워짐' 시스템 프로토타입으로.

---

## PART 3 「완료된 작업 로그」 발췌

- **2026-06-30 (WSL세션 Ⓒ — 환경 서사 확장: 단서 흔적 4개 추가 + 엔딩 변주 일반화)** — 사용자 "C로 진행". STORY_BIBLE의 '환경 서사'(배경에 흔적을 두고 플레이어가 '발견') 도구로 6굽이를 더 채움 — **잠긴 6굽이/기억소거/엔딩 구조는 그대로**, 흔적만 흩음. ① 새 대사 2편: `ferry_boat.json`(1굽이 부서진 나룻배 + '윗고을은 진작 수문을 닫았다더라' 흘림)·`village_chasa.json`(3굽이 못 떠난 넋 — 저승차사를 길손이 거부해 못 떠남, 바이블 3굽이 캐넌). 둘 다 내레이션 auto_dialogue(저·중진행도라 글자 dissolve 약함=읽힘), once_flag를 단서 플래그(`haewon_clue_boat`/`_chasa`)로 겸용. ② 5굽이 빈 안방에 **꽃신·가락지 픽업 2개**(바이블 '비녀+꽃신+가락지+꺼진 등' 중 셋 충족) — 후반은 progress 높아 대사 글자가 dissolve로 안 읽히므로 **pickup 평문 라벨**(직전 세션의 pickup 가시화·피드백 수정이 여기서 빛을 봄). 세 keepsake 모두 charm 글린트(coin 엽전은 빈집 grief 톤 깨므로 배제). ③ `haewon_ending.gd`: 단서를 6종 `CLUE_FLAGS`로 일반화 → 발견 수만큼 **등불 3~9개** + 마지막 줄 **4단계 변주**(0/적음/중간/거의다, 5+면 '물 위로 첫 빛…' 윤슬 이미지). **검증(WSL, Godot 없음):** 전 stage·dialogue JSON 파싱·참조·아이콘 무결성 0결함, 새 대사 노드 next-id 정합, `test_haewon_chain`(체인 타깃·결계·소거)과 무충돌(픽업/auto는 CombatGate 아님) 정적 확인. **⚠ PC 검증 필요:** 헤드리스 전체 그린 + 1·3굽이 흔적 내레이션·5굽이 꽃신/가락지 픽업 발견·엔딩 등불 수/마지막 줄 변주 눈 확인.
- **2026-06-29 (25차 — 게임 제목 '해원' + '걷는 회상' 수묵 흑백 오버랩)** — 사용자 "비주얼은 PC 단계가 남은 듯, 다음 작업 계속". 그래서 **PC 단계 없이 웹빌드에 바로 보이는 코드 연출**만 골라 진행. ① `project.godot` config/name '귀창록'→'해원'(+description) — 탭/부트 제목 교정. ② `scripts/world/ink_wash.gd`(오토로드 `InkWash`, CanvasLayer layer=5) — 전체화면 ColorRect+내장 스크린텍스처 셰이더로 월드를 흑백+한지 톤 탈채도. 대화창/HUD(layer 10+) 아래라 대사는 또렷이 남고(가독 유지), 입력 안 막아 조작 유지. `enter()/exit()` 트윈. ③ `stage.gd`: `clear_inkwash` JSON 플래그 → 진혼 후 대사를 수묵 연출로 감싸고 대사 종료 시 복귀. 적용: 1굽이(윤슬 회상=걷는 회상)·4굽이(수문 반전=보스 정적)·6굽이(윤슬 천도). **검증: CI 헤드리스 그린(InkWash 오토로드·test_haewon_chain 정상) + 웹빌드/Pages 재배포 성공 → https://ksjmgrkks.github.io/joseon-rpg/ 에 반영.** 셰이더 실제 렌더 미감만 폰 확인 영역. 잔여 cosmetic(탭 제목) 해소됨.
- **2026-06-29 (25차 이어서 — 해원 엔딩 화면 「빈 강」)** — 6굽이(윤슬 천도) 후 도달하는 엔딩. `scripts/ui/haewon_ending.gd` + `scenes/ui/HaewonEnding.tscn`: 동틀녘 강 그라데이션(코드 _draw) + 지평선 첫 햇살 금빛 띠('그 빛이 곧 윤슬') + 물등 스프라이트 3~5개 느리게 떠감(bob). 고요 3s → 마지막 한 줄 페이드 → '처음으로' 버튼. **사이드 단서(통문·비녀) 발견 수에 따라 등불 개수·마지막 줄 변주.** haewon_6 exit 를 Clear→HaewonEnding 로 변경, scene_manager NON_GAMEPLAY_SCENES + HaewonEnding, test_haewon_chain 기대 타깃 갱신, '처음으로'는 MemoryLedger.reset 포함. 셰이더/외부에셋 추가 import 없음 → 웹 빌드에 바로 반영. **검증: JSON 유효 + CI(헤드리스/웹) 확인.**
- **2026-06-29 (23차 — 시그니처 '기억 지워짐' 시스템 + 6굽이 스테이지/대사 데이터)** — 사용자 텔레그램 "조선 RPG 쭉 이어서 진행 + 필요하면 PixelLab 적극 활용". 바이블 '다음 제작 순서' 2·3단계 실제 구현. ① `scripts/memory/memory_ledger.gd`(오토로드 `MemoryLedger`) — 굽이별 기억조각 7개(FRAGMENTS), `erase`/`erase_for_gut`/`progress`/`is_erased`/`name_intact`/`screen_names`/`next_fragment`, 신호 `memory_erased`·`ledger_changed`·`last_name_released`, SaveManager `"memory"` 키 save/load. ② `scripts/memory/memory_glyph.gd`(class_name `MemoryGlyph`, RefCounted) — 순수 결정적 글자 dissolve(BBCode)·strip(공백)·erased_indices, seed별 패턴·비율 단조증가. ③ `project.godot` autoload 에 MemoryLedger 등록(Flags 다음). ④ 스테이지 7장 `assets/stages/haewon_{0_prologue,1_ferry,2_market,3_village,4_watergate,5_emptytown,6_yunseul}.json` — 비트시트·물/밤/동틀녘 backdrop·기존 적 재활용·전진게이트·환경서사 픽업·강따라 체인 exits(새 .tscn 래퍼는 다음 단계). ⑤ 대사 14편 `assets/dialogue/haewon/*.json`(프롤로그 open/after, ferry_omen, ferryman_spirit, recall_yunseul[윤슬 회상], market_flooded, village_drowned/confession, watergate_approach/reveal, emptytown_enter, empty_house, yunseul_approach/release) — 바이블 사극체 그대로. ⑥ `tests/test_memory_ledger.{gd,tscn}` 8케이스(ledger 5 + glyph 3). **검증: WSL Godot 없음 → JSON 21장 파이썬 유효성 통과, GDScript는 정적 점검만(autoload 파싱 안정 위해 const 타입주석 제거·typed 리턴 보수화). 헤드리스/인게임은 PC 대기.** 미배선(다음): GAMEPLAY_ONLY=false 복원 + 굽이클리어→erase_for_gut 연결 + .tscn 래퍼 + 글자 dissolve UI 적용 + 걷는 회상.
- **2026-06-29 (22차 — 주인공 '해원→길손' + 무대 확정)** — 작업 트리에 있던 미커밋 다듬기를 커밋(d712928). 주인공 호칭 '해원'→'길손'(이름 없는 강가 나그네), '해원(解冤)'은 게임 제목=한 푸는 일 그 자체로 재정의. 무대 그릇 '물등+동틀녘+빈집' 확정(★남은 결정 1개 해소). 윤슬 회상 대사 '잊거들랑'→'그리울 적엔'(망각 복선 은닉). 설계 문서만.
- **2026-06-29 (21차 — '윤슬' 이름 + '수문(홍수)' 사유로 교체)** — 사용자 텔레그램 지시: "윤슬 + 수문" + "윤슬 뜻을 자연스럽게 아는 대화를 끼워 달라(평소 안 쓰는 말이라)". `docs/STORY_BIBLE.md` 전면 갱신: 정인 이름 **여울→윤슬**(순우리말 '물에 부서지는 빛'), 문 닫은 사유 **기근/돌림병→수문(水門)·홍수** — '물의 게임' 모티프(강·물등·동틀녘)와 한 줄로 꿰기 위함. 비트시트 죽음·단서를 전부 물(수몰)로 통일(1굽이 뱃사공=급류 익사, 2굽이 '물에 잠긴 저잣거리', 3굽이 '물에 잠긴 강마을', 4굽이 보스 '닫힌 수문', 5굽이 빈집에 물 빠진 자국). 새 섹션 **「윤슬 — 이름의 뜻(회상 대화)」** 추가: 1~2굽이 사이 '걷는 회상'에 심고("물에 빛 드는 걸 보셔요, 거기 어디 제가 있을 터이니"), 엔딩 '첫 햇살이 강에 닿음'에서 회수해 터뜨림(이름 뜻=엔딩 이미지=마지막 한 줄이 한 점에 모임). 확정 경위·정체성·주인공·대사 톤 샘플·환경서사 예시까지 윤슬/수문으로 통일. **코드 변경 없음(설계 문서만).** 남은 결정: 무대 그릇(수문·강 코어로 물등+동틀녘+빈집이 사실상 정답).
- **2026-06-28 (20차 — '여울=사랑하는 이'로 변경 + 텔레그램 비트 명문화)** — 사용자 텔레그램 지시: "딸 말고 사랑하는 사람으로 가시죠" + 추가 비트 3개 반영. `docs/STORY_BIBLE.md`에서 **여울: 어린 딸 → 정인(情人)**으로 전면 교체 — 당위 ⓔ가 "義/생존"에 더해 **사랑 vs 직분** 갈등까지 겹침("닫힌 문 밖에 정인 여울이 있었으나 직분을 택해 닫음, 여울은 그의 이름을 부르다 죽음"), 5굽이 빈집 단서 아이 소품(댕기·짚신)→**정인 소품(비녀·꽃신·가락지)**, 6굽이 의미전복·로그라인·환경서사 예시·소제목 전부 '사랑하는 이'로 통일. 시그니처②'기억 지워짐' 섹션에 텔레그램 비트 명문화 추가: **원혼=그 당위로 죽은 이들 / 해원=죄책 지워짐=속죄+망각+자기소멸 / 반전=단죄 아닌 용서·놓아줌**(이미 로그라인엔 있던 정서를 코어 섹션에 못박음). HANDOFF 현재상태·Next Action·완료로그 갱신. **코드 변경 없음(설계 문서만).** 남은 결정 그대로: 무대 그릇.
- **2026-06-28 (19차 — 「해원」 서사 코어 확정 + STORY_BIBLE 작성)** — 사용자가 산나비식 비장미를 원함. 06-27 텔레그램 세션(5bcd90b0)에서 정서 코어 6안 → ⑤(망각)+⑥(진혼) 융합으로 좁힘, '산'·기존 몬스터·"내가 괴물" 집착 모두 폐기. 06-28 사용자가 마지막 2개 결정: **역할 이름 '해원'(解冤者)** + **당위 ⓑ 기근의 선택 + ⓔ 사랑하는 이**. 이전 대화가 repo·git엔 없어 Claude Code 트랜스크립트(`~/.claude/projects/-home-kyuseong/5bcd90b0….jsonl`)에서 전 맥락 복원 후 이어감. 산출: `docs/STORY_BIBLE.md` 신규(로그라인·시그니처 둘[진혼/망각]·주인공 해원 설정[고을 문 닫아 모두도 딸 여울도 잃은 현감]·6굽이 비트시트·대사 톤 샘플·기존 자산 관계·제작 순서). HANDOFF 현재상태/Next Action 20번/참조 갱신. **남은 결정: 무대 그릇(물등+동틀녘+빈집 추천).** 코드 변경 없음(설계 문서만).
