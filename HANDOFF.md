# HANDOFF.md — 조선시대 도트 횡스크롤 RPG · 작업 인수인계

> **이 파일의 역할:** 컴퓨터든 폰이든 어느 기기에서 다시 시작하든, "무슨 게임을 만드는지 + 지금 어디까지 했는지 + 다음에 뭘 할지"를 한 번에 파악하게 해주는 인수인계 문서.
>
> **CLAUDE.md와의 차이:** `CLAUDE.md`=안 바뀌는 규칙/구조/스타일. `HANDOFF.md`=이 게임의 기획 + 매 작업마다 바뀌는 진행 상황.
>
> **Claude Code 운영 규칙:** ① 세션 시작 시 이 파일을 먼저 읽고 상황 파악 ② 작업을 마칠 때 "현재 상태/다음 할 일/완료 로그"를 갱신 ③ 갱신 후 git 커밋 & 푸시.

---

# 🎮 PART 1. 게임 기획 (무엇을 만드는가)

## 한 줄 소개
가상의 조선시대를 배경으로 한 **도트(픽셀아트) 횡스크롤 액션 RPG**. 모바일(휴대폰)용. 은은한 수묵·한지 톤의 미감.

## 컨셉 & 분위기
- **배경:** 가상의 조선풍 세계 (특정 시대는 미확정 — 첫 캐릭터/맵 제작 후 분위기 보고 확정)
- **톤:** 미확정 (진지 사극 / 코믹 / 괴담 중 첫 비주얼 보고 결정)
- **장르:** 횡스크롤 액션 RPG (좌우로 이동하며 진행, 전투 + 성장 + 스토리)
- **플랫폼:** 휴대폰. 개발 중 미리보기는 web export → 폰 브라우저. 터치 조작 전제.

## 비주얼 방향
- **세밀도:** 적당히 디테일이 살아있는 도트 (너무 미니멀하지도, 너무 고해상도도 아님)
- **캐릭터 높이:** 48px (권장 시작값, 첫 캐릭터 보고 조정 가능)
- **타일 크기:** 32x32
- **팔레트:** 제한 팔레트 24~32색, **은은한 수묵·한지 톤** (먹, 한지 베이지, 절제된 채도)
- **모바일 UI:** 터치 버튼은 손가락에 맞게 크게, 손에 가리는 화면 모서리 피하기

## 조선시대 고유 요소 (정체성 — 흔들리면 안 되는 부분)
- **복식:** 양반(도포·갓), 상민(저고리·패랭이), 관리(관복) 구분이 드러나게
- **건축:** 한옥 기와·초가·담장·대문 — 중국/일본풍 혼입 금지
- **문양/UI:** 단청·태극·한지 질감 등 한국적 모티프
- **적/몬스터:** 한국 설화 소재 우선 — 도깨비, 구미호, 저승사자, 호환(호랑이) 등
- **대사:** 사극체로 통일 (현대 말투 금지)
- **텍스트:** 한글 픽셀 폰트 별도 준비 필요 (깨끗한 렌더 확인)

## 기술 스택
- **엔진:** Godot 4.x — **GDScript 전용** (C# 금지: web export 불가)
- **렌더러:** Compatibility (모바일·웹 호환 최적)
- **배포:** 깃헙 레포 + web export(HTML5). 미리보기는 GitHub Pages 등 정적 호스팅.
- **개발 방식:** 폰(텔레그램+Claude Code) 중심 + 필요 시 PC. 시각 작업은 web export로 눈 확인.

## 핵심 시스템 (만들 것)
- 플레이어 이동/점프 (횡스크롤)
- 전투 (적 AI, 피격/넉백, 스킬 또는 콤보)
- 인벤토리 / 아이템 / 장비
- 세이브/로드
- 대화 / 퀘스트 시스템 (분기형으로 시작, LLM 연동은 추후 검토)
- UI/HUD (모바일 터치, 한지·먹 테마)

## 월드 구조 — 스테이지(던전) 컨셉
- **구조:** 마리오·던전앤파이터처럼 **스테이지(던전) 단위로 이어지는 구조**로 만든다. 스테이지 하나 = 강한 컨셉 하나를 가진 던전.
- **컨셉 일관성:** 각 던전의 맵·배경·지형은 그 던전의 컨셉으로 통일한다 — 서로 다른 컨셉 요소가 한 던전 안에 섞이지 않게.
- **1스테이지 = 물:** 1스테이지 컨셉은 물로 확정. 맵·배경·지형은 조선시대 물가(강가·나루터·수변 마을 등) 컨셉으로 일관되게.
- **앞으로:** 스테이지마다 다양한 조선시대 크리처·던전 컨셉을 계속 만들어 나간다(2스테이지 이후 컨셉은 미정).

## 제작 단계 (로드맵 요약)
- **Phase 0** 스타일 바이블 — 주인공 스프라이트 1종 확정 (전체 스타일의 기준점)
- **Phase 1** 수직 슬라이스 — 주인공이 한 맵에서 걷고·싸우고·대화하는 10분 구간
- **Phase 2** 핵심 시스템 — 전투/인벤토리/세이브/대화 구축
- **Phase 3** 콘텐츠 양산 — 맵·적·스토리·사운드 확장
- **Phase 4** 폴리시 & 출시 — 밸런싱·버그·스토어

> 상세 로드맵은 별도 문서(`조선시대_도트_RPG_제작_로드맵.md`) 참조.

## 핵심 원칙 (반복해서 지킬 것)
1. **레퍼런스 우선** — 텍스트 프롬프트만으론 조선풍이 중국/일본풍으로 샘. 캐릭터 베이스 먼저 확정 후 그걸 기준으로 확장.
2. **스타일 먼저 잠그고 양산은 나중에.**
3. **AI 출력 = 초안.** 도트는 손보정 단계 필수 (일관성·서브픽셀 깨짐).
4. **수직 슬라이스부터.** 10분이 재밌은지 확인 후 양산.
5. **작게 쪼개기 + git 커밋으로 통제력 유지** (폰에선 코드 정독이 어려우므로).

---

# 📍 PART 2. 현재 상황 (어디까지 했는가)

## 현재 상태 (한 줄 요약)
> **추가(같은 날, 피드백 3번: 웹 세로↔가로 회전 시 안내 문구 안 사라지는 버그):** 사용자 지시 "네 푸시하고 3번 진행해주세요" — 마지막 남은 피드백. `scripts/ui/screen_fit.gd`(오토로드 `ScreenFit`)는 원래 `root.size_changed` 신호 **한 번**만 믿고 세로/가로 판정(`_apply()`)을 다시 계산했는데, 모바일 웹(특히 iOS Safari)은 회전 도중 과도기 크기로 신호가 한 번 오고 안정된 뒤엔 다시 안 오는 경우가 있어 "세로로 갔다가 가로로 되돌아와도 안내가 그대로 남는" 정확히 신고된 증상이 나옴 — 이 환경(WSL, 실기기 없음)에서는 iOS Safari 타이밍을 직접 재현할 수 없어 **3중 방어**로 대응: ①`_on_resize()`가 즉시 1회 반영 후 `RECHECK_DELAY=0.2`s 디바운스 타이머로 한 번 더 재검사(과도기 크기였을 경우 대비, 연달아 와도 타이머는 재시작만 되고 안 쌓임) ②`POLL_INTERVAL=0.5`s 저빈도 폴링 타이머가 신호 유실과 무관하게 계속 실제 상태로 수렴시킴(안전망) ③안내 veil 자체를 탭하면(`mouse_filter STOP`+`gui_input`) 즉시 재검사하는 수동 안전망 추가 — 자동 감지가 놓쳐도 사용자가 직접 풀 수 있음. 테스트를 위해 `_apply()`에 `size_override`/`handheld_override` 선택 인자를 추가(기본값이면 기존과 동일하게 실제 창·기기 판정, 값을 주면 판정 로직만 격리 검증). 신규 `tests/test_screen_fit.gd`(5케이스: 가로/세로 판정, 손안기기 게이팅, 타이머 구성, 리사이즈 연타에도 타이머 안 쌓임, **탭하면 박제된 안내가 풀리는지** 회귀 테스트) — **헤드리스 61스위트 전부 그린**(60+신규 1, 회귀 없음). **정직하게 밝힘: 이 수정은 실제 모바일 브라우저 회전에서 검증 못 함**(이 환경엔 실기기·실브라우저가 없음) — 폴링 안전망 덕에 최악의 경우도 0.5초 안에는 반드시 풀리고, 탭하면 즉시 풀리므로 이론상 "영영 안 사라짐"은 막히지만, **사용자가 실제 폰에서 세로→가로→세로→가로를 다시 해보고 확인 필요**. 로컬 커밋만, **푸시는 사용자 승인 대기**. 이걸로 이번 세션 피드백 3건(대화창 초상화/터치 버튼 드래그/회전 안내) 전부 착수 완료.
>
> **추가(같은 날, 피드백 2번: 모바일 터치 버튼 위치 드래그 편집):** 대화창 개편(1번) 완료 후 사용자가 "이제 2번 작업" 지시 — 설정 화면에 "터치 버튼 위치" → "드래그로 조정" 진입점 신설, `scenes/ui/TouchLayoutEditor.tscn`+`scripts/ui/touch_layout_editor.gd`. 편집기는 실제 `MobileControls`를 잠깐 띄워 현재 배치·버튼 텍스처를 그대로 복제해 드래그 가능한 `TouchLayoutHandle`(신규 `scripts/ui/touch_layout_handle.gd`, `TextureRect` 기반, `_gui_input`으로 터치/마우스 드래그 처리)로 만든다 — 시각적으로 실제 게임과 동일. 위치는 신규 오토로드 `TouchLayoutConfig`(`scripts/input/touch_layout_config.gd`, `InputConfig`와 같은 `ConfigFile` 패턴)가 `user://touch_layout.cfg`에 **정규화 비율(0~1)** 로 저장 — 해상도·회전이 달라져도 같은 비율로 재배치되도록 `MobileControls._layout()`이 쓰는 안전영역 좌표계(`_layout_origin`/`_layout_size`, 이번에 인스턴스 변수로 노출)와 동일 기준을 공유한다. `MobileControls._place()`가 `TouchLayoutConfig.has_custom(action)`이면 커스텀 위치를, 없으면 기존 기본식을 그대로 쓰도록 수정(폴백 유지, 회귀 없음). 세이브 슬롯과 무관한 기기 설정이라 `_fresh_run()`/새로 시작과 상관없이 유지됨. [저장]을 눌러야 커밋되고 [뒤로]는 드래그만 하고 버림, [초기화]는 커스텀을 지우고 기본값으로 재구성. 신규 `tests/test_touch_layout.gd`(4케이스: 저장/복원 round-trip, 커스텀이 기본식보다 우선 적용, 초기화 시 기본값 복원, 손대지 않은 다른 버튼은 영향 없음) — **헤드리스 60스위트 전부 그린**(`test_mobile_ui` 7/7 포함, 회귀 없음). X서버 죽어있어 실기기 화면 확인은 못 함(좌표 계산·테스트로만 검증) — 다음 세션에 폰으로 드래그감·저장 동작 확인 필요. 로컬 커밋만, **푸시는 사용자 승인 대기**. 3번(웹 세로↔가로 안내문구 안 사라짐)은 아직 미착수.
>
> **추가(같은 날, 대화창 초상화 2차 피드백 반영):** 사용자가 스샷 보고 "초록색 아직 있고, 일러스트가 대화창에 딱 붙고 화면 1/3은 돼야 할 듯"이라고 재요청. **①초록 잔여물 원인 재규명:** 1차 매팅(경계 침식만)으로는 못 잡는 두 종류가 남아 있었음 — (a) 시트 왼쪽 전신 일러스트의 지팡이 결정 초록빛이 그리드 0열 칸 왼쪽 경계 너머로 번져 들어와 **몸통과 픽셀이 이어진 채** 남은 얼룩(연결요소 분리로도 안 잡힘 — 어둡지도 않고 고립되지도 않음), (b) casting/profile_staff 칸 사이로 살짝 넘어온 파편. `probe_green.gd`/`probe_trim.gd`로 실측: 노이즈는 `g>=0.20`이고 `g`가 `r·b`보다 뚜렷이 높은(`g-min(r,b)>0.10`, `b<g*0.85`) 채도 높은 픽셀, 로브의 진짜 청록 자수는 `g<0.20`인 저채도 어두운 톤이라 색 시그니처로 안전하게 구분됨 — `_strip_bleed_green()` 신설(색상 직접 판정, casting/profile_staff 두 칸은 초록이 디자인 자체라 제외) + `_keep_largest_component()`(알파 연결요소 중 가장 큰 것=몸통만 남김, 고립된 색 섬 제거) 두 단계를 매팅 파이프라인에 추가, 침식 반복도 2→3회로. 재실행 후 neutral/glance/downcast/smirk/thinking/shocked/back 전부 육안 확인 완료(깨끗함), `signature_full.png`(클로즈업)는 미세한 잔여가 있지만 실제 게임에 쓰이지 않는 참고용 에셋이라 우선순위 낮춤. **②크기·밀착:** `scripts/ui/dialogue_balloon.gd`에 `PORTRAIT_HEIGHT_RATIO=1/3`, `PORTRAIT_ASPECT=0.81` 상수 추가, `_place_bar()`가 하단 바 위치를 구한 뒤 `_place_portrait(vp, bar_top)`를 호출해 초상화 rect를 `높이=vp.y/3, 하단=bar_top`(틈 0)로 매 대화 시작마다 재계산 — 예전엔 `.tscn`에 하드코딩된 좌표(상단에 뜬 채 바와 268px 간격)였던 게 원인. `DialogueBalloon.tscn`의 `Portrait` 앵커를 `Bubble`과 같은 기본값(0,0,0,0)으로 단순화. **검증:** 헤드리스 59스위트 전 시나리오 재실행 그린(`test_dialogue_portrait` 4/4 포함), 매팅 결과는 파일 직접 열람으로 확인. X서버 이번엔 죽어 있어 실제 화면 스크린샷은 못 찍음(레이아웃 계산은 좌표 수식으로 검증, 실기기 눈 확인은 다음 세션 권장). 로컬 커밋만, **푸시는 사용자 승인 대기**.
>
> **추가(같은 날, 대화창 개편):** 사용자 피드백 3건 중 1번(대화창 개편) 완료. `scripts/ui/dialogue_balloon.gd` + `scenes/ui/DialogueBalloon.tscn` 전면 개편 — 화자 머리 위로 떠다니던 말풍선을 **화면 하단 고정 바**(스테이지 선택 화면과 같은 PixelLab `create_ui_asset` 창틀 스타일 재사용, `assets/ui/dialogue_panel.png` — 이번엔 UI 키트 시트를 한 번에 받아 그중 필요한 패널만 크롭해 재사용, 신규 40gen)로 옮기고, **주인공("길손")이 화자일 때 화면 상단에 고퀄리티 초상화**를 띄우도록 바꿨다. 초상화는 **사용자가 외부(비-PixelLab) AI로 직접 만들어 온 글로시 애니메이션 CG 원본**(3×3 표정 그리드 + 클로즈업 1장)을 그대로 써달라는 명시적 요청 — PixelLab이 아니라 배경 매팅만 우리가 처리(`tools/pixel/extract_protagonist_portraits.gd`, `extract_protagonist_closeup.gd`). **매팅 실패담(교훈):** 처음엔 밝기<24 를 배경으로 보는 테두리 플러드필을 썼다가 캐릭터 옷·모자(실측 lum 10~34)까지 배경으로 오인해 큰 구멍이 났다 — `probe_luminance`로 실측해보니 진짜 배경은 정확히 (0,0,0)이었고, 임계값을 2.0으로 낮추고 경계만 얕게(2회) 침식하는 방식으로 교체해서 해결(다음에 비슷한 매팅 작업할 때 반드시 실측 먼저). 표정 9종(`neutral/glance/downcast/smirk/thinking/shocked/back/casting/profile_staff.png`) + 보너스 풀샷(`signature_full.png`)을 `assets/ui/portraits/protagonist/`에 저장, `docs/STYLE_BIBLE.md` §9에 "이 초상화만 픽셀아트 규칙 예외" 의도적 조항 명시(과거 주인공 아트 되돌림 이력이 있어 다음 세션이 오인해 되돌리지 않도록). 대화 노드 JSON에 `"expression":"smirk"` 식으로 표정 지정 가능(없으면 말=neutral, 혼잣말=thinking 기본값), `Dialogue.meta("expression")` 로 읽음. 신규 `tests/test_dialogue_portrait.gd`(4케이스: 주인공 기본 표정/expression 메타 반영/NPC 초상화 숨김/나레이션 숨김)로 고정 — **헤드리스 59스위트(211케이스) 전부 그린**. 이번 세션은 드물게 **X서버가 살아 있어서**(`DISPLAY=:0` + `tools/DlgShot.gd`) 실제로 스크린샷 캡처·육안 검증까지 했음(하단 바 아트·상단 초상화·말/혼잣말 모드 전환 전부 확인) — 텔레그램으로 스크린샷 전송함. 로컬 커밋만, **푸시는 사용자 승인 대기**. **다음 할 일(2/3번, 이번 세션 미착수):** ② 모바일 키 설정 화면에 드래그로 버튼 위치 직접 조정하는 기능, ③ 웹에서 세로→가로→세로→가로 회전 시 "가로로 돌려주세요" 안내 문구가 그 후로도 안 사라지는 버그.
>
> ## ▶ 다음 세션 시작점 (2026-08-21 갱신 · 여기부터 읽으면 됨)
>
> **추가(2026-08-21, 텔레그램 문의 대응 — 1스테이지 적용 확인 + 물웅덩이 제거):** 사용자가
> "1스테이지 처음부터 보스까지 타일/소품 개편이 적용된 게 맞냐, 웹 빌드엔 안 보인다" +
> "물웅덩이가 어색하니 빼달라" 문의.
> **① 적용 여부 조사 결과 — 실제로는 적용되어 있었다:** `997a4be`(나머지 4굽이 재제작)가
> `git log origin/main`과 로컬 HEAD가 완전히 일치함을 확인, `foothills/forest_deep/
> mountain_pass/ruined_temple/sacred_altar` 5개 JSON 전부 `ground.tileset`이 계획대로
> earth/cliff/ruins로, 신규 소품(reed/driftwood/seokdeung 등)도 실제 배치돼 있음을
> 데이터로 직접 확인. GitHub Actions "Godot Web Export"도 최신 커밋(`e0e3ea6`)까지 전부
> success, `gh api .../deployments`로 Pages 배포 이력 확인 결과 그 커밋 그대로 라이브
> 배포됨(`curl -I`로 index.html/index.pck last-modified가 배포 시각과 일치). 즉 **서버는
> 최신 상태를 정상적으로 서빙 중** — 사용자가 못 본 원인은 브라우저/기기 쪽 캐시일 가능성이
> 높음(폰 브라우저 강력 새로고침 또는 사생활 모드로 재확인 권장). 다만 **이 지형은 지난
> 세션에도 "⚠ 시각 미확인"으로 남아있던 것**이라, 실제 렌더링이 의도대로 보이는지는 여전히
> 아무도 눈으로 확인 못 한 상태 — 캐시 문제가 아니라 진짜 렌더 버그일 가능성도 배제 불가.
> **② 별개 발견 — CI 헤드리스 테스트가 최근 2번 연속 실패 중**(`e0e3ea6`/`ccda9f0`,
> `Godot Headless Tests` 워크플로): 원인은 `[FAIL] 붙잡기_감속_후_복구`(그랩 관련 테스트,
> 스테이지1 작업과 무관) + `default_theme.tres`가 `Galmuri11.ttf` 폰트 리소스를 못 찾는
> Parse Error. 이번 세션 스코프 밖이라 손 안 댐 — **다음 세션에서 조사 필요**(HANDOFF에
> "헤드리스 전부 그린"이라 적힌 과거 기록들은 로컬 실행 기준이라 CI 실패와 안 맞을 수 있음,
> 이제부턴 로컬 결과와 CI 결과를 둘 다 신뢰하지 말고 실제 `gh run list`로 교차 확인할 것).
> **③ 물웅덩이 제거:** "너무 어색하다"는 명확한 피드백이라 스코프 확인 없이 바로 제거 —
> `stage.gd`의 `_build_water_pools()`(지면 위 river 타일 덧그리기 + `WaterZone` 배치) 호출
> 2곳(스토리 경로/게임플레이 경로) 및 함수·`RIVER_TILESET` 상수 삭제, `scripts/world/
> water_zone.gd`(클래스 전체, 다른 곳에서 안 씀 확인)와 `tests/test_water_zone.gd(.uid)/
> .tscn` 삭제, 5개 스테이지1 JSON에서 `water_pools` 배열 전부 제거(JSON 재직렬화는
> `indent=2`로 원본 포맷 유지해서 diff를 그 필드만으로 최소화). `SkillFx.WATER`/`FOAM`
> 모트 색상 상수는 `tide_wave.gd`/`water_pillar.gd`(투사체 이펙트) 등 다른 곳에서 계속
> 쓰므로 안 건드림 — 진짜 죽은 코드만 제거. **⚠ 미검증:** 이 환경엔 Godot 실행 파일이 없어
> 헤드리스 테스트도 못 돌려봤음(제거한 함수·클래스에 대한 다른 참조가 없는지는 grep으로만
> 확인) — 다음 PC 세션에서 헤드리스 전체 스위트 그린 확인 필요. 로컬 커밋만, **푸시는
> 사용자 승인 대기**.
> **다음 세션 확인 필요(둘 다 미해결):** (a) 위 CI 헤드리스 실패 2건 원인 규명·수정.
> (b) 사용자가 예전에 요청한 "1스테이지 처음부터 보스까지 완전히 새로 재제작"(기존 소스
> 영향 없이) — 이번 문의는 "적용됐는지 확인"이었지 이 요청을 재개하라는 지시는 아니었음,
> 필요하면 스코프(터레인만인지/몬스터·보스까지인지)를 사용자에게 먼저 확인할 것.
>
> **추가(2026-08-21, 주인공 일러스트 교체 + 대화 초상화 화면 절반):** 사용자가 새 주인공
> 일러스트 2장(전신+표정 9칸 시트 / 얼굴 클로즈업)을 **채팅으로** 주면서 "적용하고, 대화할 때
> 일러스트가 대화창 위로 화면의 거의 절반을 차지하게" 요청.
> **① 채팅 이미지를 파일로 꺼내는 법(중요 — 다음에도 쓸 것):** 채팅에 붙인 이미지는 디스크에
> 없어서 처음엔 "직접 저장해 달라"고 했는데, 사용자가 "저장을 직접 해주세요"라고 해서 다시 찾음 →
> **세션 트랜스크립트(`~/.claude/projects/<슬러그>/<세션id>.jsonl`)에 base64 로 들어 있다.**
> PowerShell 로 해당 줄을 `ConvertFrom-Json` 해서 `message.content[].source.data` 를
> `[Convert]::FromBase64String` 로 디코드 → `.art_ref_incoming/*.webp` 저장. 이 방법이면
> 앞으로도 사용자가 채팅으로 준 이미지를 그대로 파이프라인에 넣을 수 있다.
> **② 새 원본은 배경이 이미 알파 0(투명)** — 이전 검은 배경 JPEG 시절의 초록 프린지 문제가
> 원천적으로 사라졌다. 그래서 `extract_protagonist_portraits.gd`·`extract_protagonist_closeup.gd`
> 를 전면 재작성: 플러드필 매팅·경계 침식을 **전부 삭제**하고, 그리드 좌표 하드코딩도 버리고
> 자동 검출로 바꿈(오른쪽만 봤을 때 행이 3개가 되는 x 를 찾아 좌우 분리 → 행 밴드 3개 →
> 열 경계). **함정:** 칸끼리 딱 붙어 있어 '완전히 빈 열'이 없고, 3행(주문 시전)은 보랏빛
> 이펙트가 칸 경계를 넘어 번져 그 행만으로는 열을 못 나눈다 → 빈 열이 깨끗한 행에서 구한
> 열 경계를 세 행에 공통 적용하고, 옆 칸에서 넘어온 손·빛 조각은 **연결요소 청소(가장 큰
> 덩어리만 유지)** 로 제거(`casting` 칸만 제외 — 그 이펙트는 자기 것). 파일명 9종은 그대로라
> 대화 시스템 쪽 코드 변경 없음. `signature_full.png` 는 아직 아무 데서도 안 쓴다.
> **③ 크기:** `dialogue_balloon.gd` 의 `PORTRAIT_HEIGHT_RATIO` 1/3 → **0.48**.
> `PORTRAIT_ASPECT` 도 0.81 → **0.99** 로 같이 올려야 했다 — TextureRect 가
> KEEP_ASPECT_CENTERED 라 **상자 폭이 좁으면 폭에 걸려 높이가 목표치까지 안 커진다**(새 초상화는
> 268:279 로 예전보다 넓다). 새 원본 중 가장 넓은 비율에 맞춰야 모든 표정이 화면 절반을 채운다.
> **검증:** DlgShot 로 실제 대화 화면 캡처(`shots/verify/portrait_new_art.png`) — 새 아트가
> 대화 바 위에 딱 붙어 화면 절반을 차지하고 테두리 잔여물 없음을 눈으로 확인. 헤드리스
> **64스위트 전부 그린**. 원본 2장은 `.art_ref_incoming/`(gitignore) 라 커밋 안 됨 — 다른
> 기기에서 재추출하려면 위 ① 방법으로 다시 꺼내야 한다.
>
> **추가(2026-08-21, 1스테이지 4굽이 지형 재제작):** 전날 foothills 만 rock→earth+물웅덩이로
> 고쳐놓고 나머지(forest_deep/mountain_pass/ruined_temple/sacred_altar)는 그대로 둔 채라
> "1스테이지=물"컨셉인데 정작 4곳엔 물이 없어 뒤죽박죽으로 보인다는 재지적을 받음.
> 스코프 확인("지형/타일셋/소품만, 몬스터·보스는 그대로") 후 나머지 4굽이 전부 재작업 —
> forest_deep 은 earth 유지+물웅덩이 확장, mountain_pass 는 신규 PixelLab "cliff"
> 타일셋(자연 균열 바위, 기존 rock 대체)+물웅덩이, ruined_temple·sacred_altar 는 신규
> "ruins" 타일셋(침수가 타일 자체에 녹아든 물 얼룩 판석, 기존 stone 대체)+물웅덩이(뒤로
> 갈수록 더 넓게 — 보스 직전 sacred_altar 가 가장 침수됨). 신규 소품 3종(갈대·유목·이끼
> 석등)으로 4굽이가 전부 똑같이 복붙하던 boulder/dead_tree 세트를 다양화. 몬스터 배치·
> FloodWraith 보스·NPC·퀘스트는 전부 그대로(요청 스코프 밖). 헤드리스 전체(64스위트)
> 그린(`997a4be`). **⚠ 시각 미확인 — 이번 스테이지1 재제작은 특히 확인이 중요함**(타일셋
> 2종 신규 생성이라 실제 조합이 어떻게 보이는지 전혀 못 봄, X서버 죽어있음). 로컬 커밋만,
> **푸시는 사용자 승인 대기**.
>
> **추가(같은 날, 그슨대 조사식 노출 + 스킬 아이콘 4종 재생성):** ① "부적(스킬)을 맞혀야
> 정체가 드러나는 것 대신, 조사 버튼으로" — `Geuseondae`/`GeuseondaeElder` 둘 다 위장 중
> 조사 사거리(interact_range) 안에 들어오면 머리 위 "!" 프롬프트가 뜨고, 「조사」키로
> 즉시 `_reveal()`(기존 Interactable 과 같은 `event.is_action_pressed("interact")` 패턴).
> 부적으로도 여전히 드러나긴 하지만 이제 기본 발견 수단은 조사(`6e9f10f`). ② 스킬
> 아이콘 4종(귀창/회천/호신부/일섬) PixelLab 재생성 — 확인해보니 **회천격 아이콘이
> 애초에 잘못된 그림**(등잔/초처럼 보임, 회전베기와 무관)이 들어가 있었음. 4개 전부
> 톤 통일해서 다시 만듦(`create_image_pixflux` ×5, 첫 일섬 결과가 "초승달"로만 보여
> "슬래시 이펙트"로 재요청해 교체) — 파일 경로·크기 그대로라 코드 변경 없음(`3717619`).
> 관련 헤드리스 전체(64스위트) 그린. **⚠ 시각 미확인:** X서버 죽어있어 실제 화면
> 못 봄 — "!" 프롬프트 위치·조사 판정감·새 스킬 아이콘 실물은 폰 확인 필요. 로컬
> 커밋만, **푸시는 사용자 승인 대기**.
>
> **다음 세션 미착수(사용자 요청, 스코프 확인 필요):** "1스테이지 처음부터 보스까지
> 모든 맵과 지형을 기존 소스에 영향받지 말고 아예 처음부터 만들어달라"(현재가
> "기존 소스와 뒤죽박죽이어서 어지럽다"는 이유) — 매우 큰 작업이라 다음 세션 시작 전에
> 스코프(터레인/타일셋/소품만인지, 몬스터 배치·보스까지인지, 「1스테이지=물」 컨셉은
> 유지인지)를 사용자에게 먼저 확인할 것.
>
> **추가(같은 날, 터치 버튼 드래그/크기/아트 3건):** ① "드래그가 통통 튄다" —
> `TouchLayoutHandle` 이 `event.relative` 를 그대로 누적해서, 웹/터치에서 한 프레임에
> 이벤트가 여러 번 몰리면 델타가 중복 반영돼 튀었음. 손가락 절대 좌표 기준으로
> 바꿔 해결(`7bcaca8`). ② "버튼마다 크기 조절" — `TouchLayoutConfig` 에 액션별
> 크기 배율(0.6~1.6) 저장 추가, 편집기 각 핸들 밑에 +/- 버튼 신설(pivot_offset
> 중심 고정이라 커져도 중심 안 밀림), `MobileControls._place()` 가 저장된 배율을
> 실제 버튼 scale 에 적용. ③ "버튼·스킬 아트를 고퀄리티로" — 이동/공격/점프/회피/조사
> 6개 액션 버튼은 지금까지 **코드로 그린 밋밋한 원**이었음(실제 아트 없음).
> PixelLab `create_ui_asset` 로 7개(동작 6 + 스킬용 빈 프레임 1)를 한 시트로 생성해
> 조선풍(한지·먹·단청) 톤 통일 — `assets/ui/mobile/btn_*.png`(40 generations,
> 546→**586/2000**). 액션 이름과 같은 파일이 있으면 판정원 크기에 맞춰 리샘플해
> 쓰고, 없으면 기존 코드-원으로 폴백(안전망 유지). 새 아트는 그림 자체가 뜻을
> 담고 있어(화살표·칼 등) 기존 글자 라벨은 생략. 눌림 표시는 별도 텍스처 대신
> modulate 로 살짝 어둡게. **스킬 4개 아이콘 자체(귀창/회천/호신부/일섬)는 이미
> 품질이 괜찮아 보여 재생성 안 함** — 빈 프레임만 새로 얹었다. 사용자가 스킬
> 아이콘도 다시 만들길 원하면 다음 세션에서(`5480a76` 이전 세션에 이미 있던
> 아트라 그대로 둠). `tests/test_mobile_ui.gd`(new_art_wired)·
> `tests/test_touch_layout.gd`(2케이스 추가) 로 고정 — 헤드리스 전체(64스위트)
> 그린(`8e9448c`). **⚠ 시각 미확인:** X서버 죽어있어 실제 화면 못 봄 — 드래그
> 손맛·버튼 크기 조절 UI·새 아트 실물은 폰 확인 필요. 로컬 커밋만, **푸시는
> 사용자 승인 대기**.
>
> **추가(같은 날, 사용자 피드백 3건 — 접촉 넉백/지형지물/1스테이지 강가):** ① 몬스터 접촉 피해가
> 무적시간(0.55초) 중에도 넉백·효과음을 중복 적용해 "뚝뚝 끊기는" 느낌 — 패트롤러 근접 타격
> + 투사체/장판류 5종(SpiritOrb/WaterPillar/TideWave/ShadowClaw/ClubWhirl/StallDebris)에서
> `HealthComponent.is_invulnerable()` 체크 후 무적 중이면 통째로 무시하도록 통일(`6ce6f01`).
> ② "지형지물이 바닥을 뚫고 간다" — 실측해보니 boulder/boulder_moss 소품이 12개 스테이지
> 34곳에서 전부 offset [-64,-64](캔버스 절반, 실제 그림 바닥과 무관)를 써서 지면보다
> 44~82px 파묻혀 있었음 → `offset.y = -(used_rect.y+used_rect.h)` 공식으로 전부 교정,
> `tests/test_prop_footing.gd` 로 고정(`5480a76`). **"주인공/몬스터가 가려져야 하는데 위에
> 있다"는 원인도 찾음** — props 가 항상 enemies/npc/player 보다 먼저 add_child 돼서 캐릭터가
> 구조적으로 항상 맨 위에 그려짐(z_index 전부 기본값 0, 순서로만 결정) — 다만 **어떤 소품을
> 실제로 앞에 나오게 해야 할지는 눈으로 봐야 판단 가능**해서 이번엔 손 안 댐(다음 세션: 사용자가
> 스크린샷/스테이지 지목해주면 그 소품만 z_index 로 앞에 오게 태깅). ③ 1스테이지(foothills)
> 지면 `rock`(벽돌처럼 보임) → `earth`(흙+풀)로 교체 + PixelLab 신규 "river" 타일셋(물+젖은
> 모래, earth 와 base_tile_id 연결) 생성 → `stage.gd`에 `water_pools` JSON 필드 신설, 지면
> 위 특정 구간에 덧그리고 그 구간에 `WaterZone`(신규)을 깔아 걸으면 기존 SkillFx 물보라가
> 튀게 함(피해 없는 순수 연출) — foothills.json 에 물웅덩이 3곳 배치(`d0f9d2e`). 관련
> 헤드리스 **전체 스위트(64개) 그린**(회귀 없음). **⚠ 시각 미확인:** X서버 죽어있어 실제
> 화면은 못 봄 — boulder 파묻힘 교정과 강가 흙/풀/물 배합, 물보라 타이밍은 폰에서 확인 필요.
> 커밋 3개 로컬만, **푸시는 사용자 승인 대기**([[feedback_git_push]]).
>
> **추가(같은 날, 3스테이지 이후):** 메인 메뉴 「새로 시작」이 이제 **스테이지 선택 화면**(`scenes/ui/StageSelect.tscn` + `scripts/ui/stage_select.gd`)을 먼저 연다 — 1/2/3스테이지 중 하나를 골라 그 스테이지 첫 굽이부터 새 런(스코어 리셋)으로 시작. 카드/배경은 **PixelLab `create_ui_asset` 신규 생성**(`assets/ui/stage_select_window.png` 688×384 창틀, `stage_select_button.png` 560×192 버튼판 — 각 40gen, 총 80gen 사용, 466→**546/2000**). 버튼 3개는 같은 판 텍스처를 재사용하고 스테이지별 은은한 색조(modulate)로만 구분(물=청, 숲=녹, 저잣거리=주황) — 원본 PNG 팔레트는 그대로. `main.gd`(구 `_on_new`)는 `_fresh_run()`+`ScoreManager.start_run()`을 그대로 재사용하되 트리거 지점만 선택 화면의 `stage_chosen` 신호로 옮김(`stage.gd`의 CHAIN[0] 판정은 손대지 않음 — 신규 런이 `start_run()`을 먼저 호출하고 이후 `Stage._ready()`의 `resume()`은 `_running=true`만 재확인하므로 안전). 신규 `tests/test_stage_select.gd`(4케이스)로 카드 3장·경로 매핑·클릭→emit·아트 존재를 고정. **헤드리스 58스위트(207케이스) 전부 그린.** 이 화면도 실기기(폰) 눈으로는 아직 확인 못 함 — 카드 레이아웃·터치 히트박스 크기가 다음 세션 확인 대상.
>
> **추가(같은 날, 보스 패턴 재탕 피드백 → 3스테이지 전용 패턴 2종 신설):** 사용자가 텔레그램으로 "2·3스테이지 보스 패턴이 단순하거나 재탕 같다" 피드백. 조사해보니 **2스테이지(그슨대 노괴)는 이미 전날 커밋(`aba886d`, HANDOFF 기록 누락)으로 전용 패턴 3종(그림자 늪/갈퀴 훑기/암전 강습)이 들어가 있었음** — 실제로 빈약한 쪽은 **3스테이지(도깨비 대장)** 였다(방망이 강타 1개뿐, 나머지는 부모 돌진/부적세례/소환 그대로). `scripts/enemies/dokkaebi_chief.gd`에 전용 패턴 2종 추가: ① **방망이 휘돌리기**(P_WHIRL, 신규 `scripts/combat/club_whirl.gd`) — 몸을 돌려 양쪽을 동시에 후려친다. 이 보스의 핵심 규칙("정면=막힘/등뒤=빈틈")이 통하지 않는 유일한 예외 패턴이라, 등 뒤만 노리고 버티는 플레이를 흔든다. 점프로만 회피. ② **좌판 던지기**(P_STALL_TOSS, 신규 `scripts/combat/stall_debris.gd`) — 저잣거리 좌판 부스러기를 던지는 원거리 견제. 부적 세례(SpiritOrb, 물 보스와 시각 공유)와 겹치지 않게 나무 상자가 회전하며 포물선을 그리는 별도 투사체로 새로 만듦. 둘 다 그슨대 전용 패턴과 같은 방식으로 **코드 생성**(Polygon2D/Line2D, PixelLab 신규 생성 0건). `tests/test_dokkaebi_chief.gd`에 3케이스 추가(신규 패턴 풀 도달성, 각 패턴 위험물 생성 확인) — 관련 헤드리스(`test_dokkaebi_chief`/`test_geuseondae_elder`/`test_boss_patterns`/`test_midboss`/`test_stage_roster`/`test_gameplay_chain`/`test_enemy_footing`/`test_enemy_attack`) 전부 그린(회귀 없음). **⚠ 시각 미확인:** 실기기(폰)에서 새 패턴 두 개의 손맛·타이밍은 아직 못 봄 — 다음 세션 확인 대상. 커밋(`5cb9880`)은 로컬만, **푸시는 사용자 승인 대기**([[feedback_git_push]]).
>
> **추가(같은 날, 버그 수정 2건):** 사용자가 폰 스샷으로 ① 스테이지 선택 창 테두리 핑크색 번짐 ② 3스테이지 보스(도깨비 대장) 평소엔 붕 뜨고 공격 시 바닥에 파고듦, 2건 신고. **①** `assets/ui/stage_select_window.png`에 완전불투명 마젠타(245,6,247) 픽셀 537개가 점선처럼 박혀 있었음(PixelLab 생성 잔여물로 추정, 안티에일리어싱 번짐이 아니라 순수 색상 매칭으로 안전하게 제거 가능했음) — 해당 색만 정확히 투명 처리해 재저장, 버튼 텍스처는 이상 없음 확인. **②** 근본 원인: `tools/pixel/assemble_dokkaebi_chief.gd`가 idle/walk/death(원본 캔버스 160×160)와 telegraph/attack(원본 캔버스 220×220, 무기 반경 때문에 더 크게 생성됨)을 **서로 다른 캔버스의 `get_used_rect()`를 그대로 merge**해 "공통 크롭 창"으로 쓰고 있었음 — 캐릭터는 각 원본에서 자기 캔버스 중심에 동일하게 배치돼 있었는데(실측으로 확인: 캔버스 중심 기준 상단 -78/하단 +74~76으로 두 그룹이 거의 일치), 서로 다른 좌표계를 그대로 합치는 바람에 idle 발끝(캔버스 내 y)과 attack 발끝이 어긋났다(idle 기준으로 맞춘 `foot_offset`이 attack에서는 30px 가까이 안 맞음). **고치기:** 조립 스크립트를 "각 프레임을 자기 캔버스 중심 = 출력 캔버스 중심으로 정렬"하는 방식으로 재작성(union 크롭 폐기) → `.pl_tmp/dokkaebi_chief/`에 남아있던 원본 프레임으로 5개 시트(220×220 통일)+manifest 재조립, `foot_offset` -11.6→**-25.6**(공식 `half/scale-(foot-frame_h/2)` 재적용, `test_enemy_footing` 권장치와 gap 0 일치). **재발 방지:** `test_enemy_footing.gd`에 "idle 이외 모든 애니메이션 프레임0 발끝이 idle과 8px 이내로 일치하는지" 교차검증을 추가(기존엔 idle 프레임만 봐서 이 버그를 놓쳤음) — 전체 25종 적 전부 통과. **PixelLab 신규 생성 0건**(기존 원본 프레임 재사용, 마젠타 제거도 픽셀 직접 조작 — 이번 세션 사용량 변화 없음). 헤드리스 58스위트(207케이스) 전부 그린. X서버 죽어있어 육안 스샷 확인은 못 함(코드/데이터 재계산 + 테스트로만 검증) — 실기기 확인은 다음 세션. 로컬 커밋만, **푸시는 사용자 승인 대기**.
>
> **상태:** `main` 위에 **3스테이지(저잣거리 도깨비) 신규 완성** — 5굽이(market_ruins→broken_stalls→well_court→lantern_alley→goblin_court) + 중간보스(곤봉 도깨비) + 최종보스(도깨비 대장, PixelLab 신규 아트). CHAIN 이 10→**15**스테이지로 늘어남. **헤드리스 57스위트(203케이스) 전부 그린.** 이 세션은 WSL/텔레그램(DISPLAY 죽어있어 게임 화면 캡처는 못 함) — 헤드리스 테스트 + PixelLab 프레임 직접 육안 검토로 대체.
> **3스테이지 기믹(그슨대와 다른 종류):** 그슨대는 "위장→노출"의 1회성 게이트였다면, 도깨비 대장은 **매 타격마다** 판정한다 — 보스가 보는 방향(정면)을 치면 방망이에 막혀 칩 데미지(×0.2)만, **등 뒤(빈틈, 늘 보이는 ◆ 표식)** 를 치면 온전한 피해+보너스(×1.4)가 들어간다(`scripts/enemies/dokkaebi_chief.gd`). 텔레그래프/공격 중엔 보스가 재조준하지 않으므로 회피 후 등 뒤로 도는 실력 플레이가 성립. HealthComponent 는 의도적으로 hurtbox 자동연결을 끊고 스크립트가 직접 `take_damage` 호출(이중 피해 방지, 테스트로 고정).
> 이야기 모드는 1막부터 7막+엔딩까지 데이터가 살아 있음(메뉴에서는 「이야기」 제거됨). 일반 모드(「새로 시작」)는 전투 체인 **15**스테이지(1=음침한 산골짜기·수몰 5굽이+수문의 원혼 → 2=그슨대 숲 5굽이+그슨대 노괴 → 3=저잣거리 5굽이+도깨비 대장).
>
> **적 크기 서열(화면상 실제 높이 px — 테스트로 고정됨, `tests/test_midboss.gd`):**
> 주인공 77 / 잡몹 63~90(물아이 넋 63 · 두억시니 70 · 창귀 74 · 수몰 원혼 78 · 물귀신 90 · 도깨비 ~68) / **중간보스 148~150(못의 이무기 · 비틀린 장승 · 곤봉 도깨비)** / 보스 245~289(그슨대 노괴 정체 247 · 도깨비 대장 ~245 · 수문의 원혼 289).
> **3스테이지 아트 예산:** 잡몹/중간보스는 기존 `enemies/goblin` 시트를 스케일만 바꿔 재사용(신규 생성 0). 보스만 PixelLab 신규 생성(idle/walk/telegraph/attack/death, east 1방향만) — 19 generations 사용(447→466/2000).
> **미검증(다음 세션 확인 필요):** ① 3스테이지 실기기 손맛 — 특히 등 뒤 판정이 "일부러 노려서 도는" 손맛인지, 화면이 좁은 폰에서 ◆ 표식이 잘 보이는지 ② `club_slam`(정면 강타) 예고 0.7초가 반응 가능한지 ③ 도깨비 대장 HP 760(=FloodWraith 동급)이 앞/뒤 배율(0.2/1.4) 감안했을 때 지루하지 않은지 — 빡세면/루즈하면 `DokkaebiChief.tscn`의 `front_block_mult`/`back_bonus_mult`/`max_hp` 조정.
> ⚠ **크기는 `.tscn` 의 노드 `scale` 이 아니라 `Sprite2D.sprite_scale` 로만 준다** — `CharacterVisual._ready()` 가 `scale` 을 `sprite_scale` 로 덮어쓰기 때문에 노드 scale 은 런타임에 통째로 무시된다(이게 "중간보스가 잡몹만 하다"의 진짜 원인이었다). `test_enemy_footing` 이 이제 노드 scale≠1 이면 실패시킨다.
>
> **⚠ pull 직후 함정:** 다른 세션이 새 `class_name` 스크립트를 올린 뒤 pull 하면, 로컬 `.godot` 클래스 캐시가 낡아 **"Identifier ... not declared" 파스 에러가 무더기로** 뜬다. 실제 결함이 아니다 — `godot --headless --path . --import` 를 **두 번** 돌리면 사라진다.
>
> **환경 메모:** PC 세션은 `C:\Users\User\Downloads\Godot_v4.6.3-stable_win64.exe\`, `플레이.bat`. **이 PC 에는 파이썬이 없다** — `tools/pixel/*.py` 파이프라인은 못 돌리고, 대신 Godot 스크립트(`tools/pixel/assemble_enemy.gd` 등)로 처리한다. WSL/텔레그램 세션은 `~/godot-bin/godot` + `DISPLAY=:0` 인데 X서버가 자주 죽어 있어 캡처가 안 될 때가 많다(먼저 `DISPLAY=:0 xdpyinfo` 로 확인).
> **PixelLab MCP 연결됨** — 프로젝트 루트 `.mcp.json`(HTTP, Bearer 헤더, `.gitignore` 로 커밋 안 됨). 구독 활성(Tier 1, 2026-08-20 이 세션 종료 시점 **441/2000 사용**(이 작업 48 gen)).
> 검증 도구: `tools/Screenshot.tscn`(`--scene=` `--cam=x,y` `--touch`), `tools/BossPatternPreview.tscn`, `tools/SkillFxPreview.tscn`, `tools/TouchHitProbe.tscn`(**창 모드 필수**), `tools/HpBarPreview.tscn`, **신규** `tools/pixel/assemble_enemy.gd`(PixelLab 프레임→스트립 조립 **범용** — 캐릭터마다 스크립트를 복사하던 걸 대체), **신규** `tools/pixel/measure_enemy_sizes.gd`(전 적의 화면상 실제 높이 표), **신규** `tools/pixel/upscale_preview.gd`(`.pl_tmp/prev/*.png` 4배 확대 — 도트 육안 검사용).
>
> **다음 할 일 (우선순위 순):**
> -1. **스테이지 선택 화면 실기기 확인(신규, `main.gd`→`StageSelect.tscn`).** 「새로 시작」눌러서 1/2/3 카드가 폰 화면에서 잘 보이고 터치로 잘 눌리는지, PixelLab 창틀/버튼판이 다른 UI와 톤이 안 어긋나는지. 카드 3개가 세로로 쌓여 화면 밖으로 넘치면 `StageSelect.tscn`의 `Margin` 여백이나 카드 `custom_minimum_size`(현재 높이 96) 조정.
> 0. **3스테이지(저잣거리 도깨비) 실기기 확인 — 최우선(신규).** 등 뒤 판정 기믹이 실제로 "읽히는지"가 이 스테이지의 존재 이유이므로 가장 먼저 확인. `club_slam` 예고 타이밍, HP/배율 밸런스, ◆ 표식 가시성. walk/death 애니는 이번에 받았지만 인게임 확인은 못 함(가능하면 웹빌드로).
> 1. **1·2스테이지 실기기(폰) 확인.** 새 잡몹 3종(두억시니·창귀·물귀신)과 중간보스 2종(못의 이무기·비틀린 장승)이 실제 손맛에서 어떤지. 특히 ① 물귀신의 **붙잡기(1.4초 이동속도 55%)** 가 짜증나지 않고 "끌려간다"로 읽히는지 ② 창귀의 원거리 견제 빈도(쿨 2.2초) ③ 중간보스 HP(이무기 420 / 장승 380)가 최종보스(760) 앞에서 지루하지 않은지. ④ `rock`/`stone` 지면 타일도 같이 확인.
> 2. **주인공 아트 실기기 확인** — 256px 소스·`sprite_scale=0.4503`(화면 77px). 이번 PC 캡처로 크기감은 확인됐으나(잡몹과 자연스럽게 어울림) 애니메이션 손맛은 폰 확인 필요.
> 3. **보스 난이도 실플레이 검증** — 예고 0.6초가 폰에서 반응 가능한지. 빡세면 `FloodWraith.tscn`/`GeuseondaeElder.tscn` 의 `telegraph_seconds`·`pillar_warn` 만 올리면 된다(전부 @export).
> 4. **무릎 꿇은 포즈 아트** — 3막 「마당의 매질」에서 주인공이 서 있다. `create_character_state` + v3 커스텀.
> 5. **모바일 앱 출시 B안** — Android SDK·keystore → APK. 절차는 `docs/RELEASE_MOBILE.md`. 런처 아이콘 미제작.
> 6. ~~**3스테이지 컨셉 기획**~~ ✅ (2026-08-20, 도깨비/저잣거리로 확정 — 위 참고). **4스테이지는 아직 미기획** — 후보로 호랑이/산군, 구미호가 남아 있음. 4스테이지 보스 기믹은 3스테이지(위치/포지셔닝)·2스테이지(상태 게이트)와 또 다른 종류로 갈 것(예: 시간제한·자원관리형)을 고려.
> 7. (여유될 때) 씬 파일명 v1 잔재 개명 — 15곳, `save_manager.gd` 가 경로 문자열을 참조해 세이브 호환 주의.
> 8. (선택) 남은 아트 중복: **수몰 원혼(Wraith)** 만 `enemies/wraith` 시트를 쓴다(이번에 중복 2종 제거). 이야기 모드의 저승사자(Reaper)·저승 군주(ReaperLord)는 시트를 공유한 채 남아 있음 — 이야기 모드를 되살릴 때 정리.
> 9. (선택) 안 쓰게 된 시트 `assets/sprites/enemies/boatman`(저승 뱃사공) 은 지우지 않고 남겨둠 — 나중에 강/나루 스테이지에서 재사용 가능.
> 10. (선택) `stone` 지면 타일이 여전히 살짝 그리드감(작은 사각 판석이 규칙적으로 이어진 느낌)이 남아있다 — 핑크색은 해결됐지만 완전한 자연석은 아님. 폰에서 거슬리면 재생성 검토(rubble 프롬프트로 한 번 더 시도했으나 중앙이 밋밋해져 폐기, 이번 버전을 채택함).
>
> **판단이 필요한 것:**
> - 스코어어택 모드(「새로 시작」) 유지 여부. 엔딩 저울(`EndingResolver.BURN_HEAVY=3`)이 사이드 진혼 3기 추가로 `BURN_FLAGS` 가 7개가 되면서 「망각」이 쉽게 나올 수 있음 — 임계값 재조정 검토.
> ---
>
> **지금 여기 (2026-08-20 텔레그램/WSL — 1스테이지 `rock`/`stone` 지면 타일 자연암반 재생성):** "조선 rpg pull하고 작업 이어서 진행해주세요" 지시로 시작 → pull(`b7c96f9`, 1스테이지 몬스터 개편 반영) → HANDOFF "다음 할 일" 훑어봄. 1·3·4번(잡몹/중간보스 손맛, 주인공 애니, 보스 난이도)은 폰 실플레이가 있어야 판단 가능해 손 못 댐 — 2번(지면 타일 재작업)이 유일하게 이번 세션에서 바로 처리 가능한 항목이라 착수.
> **조사:** `assets/tilesets/side/rock.png`·`stone.png` 를 직접 열어보니 지난 세션 기록대로 회색/**분홍색 벽돌 담**(사각 블록+뚜렷한 줄눈)이었다 — 원래 생성 프롬프트(`rock.json`/`stone.json` 의 `name` 필드)에 이미 "no bricks"가 명시돼 있었는데도 PixelLab 이 무시하고 벽돌을 뽑아낸 케이스(HANDOFF 가 누차 기록한 "PixelLab 은 프롬프트를 문자 그대로 그린다"는 함정과 반대로, 이번엔 부정 지시를 무시한 사례). `stage.gd._build_platforms` 가 `.png` 만 32×32 고정 크롭(`SURFACE_TILE=(96,0)`, `FILL_TILE=(64,32)`)해서 반복 타일링하는 구조라 `.json` 은 참고용 메타데이터일 뿐 게임에 영향 없음을 코드로 확인 — 즉 PNG 교체만으로 안전하게 바뀜.
> **재생성:** `create_sidescroller_tileset` 으로 `rock`(잡곡·산길용 — "jagged grey mountain rock and boulders... no mortar joints, no brick pattern, no rectangular blocks, no wall")과 `stone`(무너진 산신당/제단용 — "weathered natural stone slabs and flagstone... no mortar joints, no brick pattern")을 각각 신규 생성 → `rock` 은 한 번에 성공(불규칙한 균열·이끼 낀 바위, 벽돌 느낌 완전히 사라짐). `stone` 은 첫 결과가 색은 회색으로 고쳐졌지만 여전히 작은 사각 판석이 줄지어 있는 느낌이 남아 "tumbled rubble... no straight rows, no grid pattern" 프롬프트로 재시도했으나 이번엔 `FILL_TILE` 위치가 텍스처 없는 밋밋한 단색 중앙부에 걸려 더 나빠짐(가장자리에만 자갈, 안쪽은 텅 빔) — 폐기하고 첫 결과(그리드감은 살짝 남지만 핑크색 문제는 해결됨)로 확정.
> **검증:** `.png` 만 교체(`.json` 은 기록용으로 같이 갱신) → `--import` 두 번 → **헤드리스 56스위트 전부 그린**(순수 아트 교체라 로직 회귀 없음, 예상대로). `stage.gd` 와 동일한 크롭 좌표로 실제 반복 타일 폭을 재현하는 임시 프리뷰 스크립트(`tools/pixel/preview_ground_tiles.gd`, 검증 후 삭제)로 4배 확대해 육안 확인 — `rock` 은 확실히 자연스러운 바위, `stone` 은 회색 판석(핑크 아님)이지만 규칙적인 느낌이 약간 남음(위 "다음 할 일" 10번에 후속 과제로 기록). PixelLab **447/2000 사용**(이 작업 6 gen: rock 1회 + stone 2회). **⚠ 여전히 시각 미확인:** 이 세션도 X서버가 죽어있어 실제 게임 화면(카메라·조명·다른 소품과의 조화)은 못 봄 — 타일 자체 크롭만 확인, 실플레이 확인은 위 "다음 할 일" 1-④에 추가. 로컬 커밋만, 사용자 규칙([[feedback_git_push]])대로 푸시는 승인 후.
>
> **이전 (2026-08-20 PC — 1스테이지 몬스터 개편 + 중간보스 크기 체계):** 사용자 요청 2건 — "①1스테이지에서 컨셉에 안 맞는 몬스터를 과감히 삭제하고 새 몬스터로 교체 ②1·2스테이지 보스 전 약한 보스는 잡몹과 보스 사이 크기로".
> **①-1 컨셉 밖 3종 제외:** 1스테이지는 `ebb34e0` 에서 「음침한 산골짜기(끝에 보·저수지)」로 통일됐는데, 저승사자(Reaper)·저승 군주(ReaperLord)·저승 뱃사공(DrownedFerryman)이 그대로 남아 있었다 — 저승 관료·나루터 모티프라 골짜기·수몰 서사와 무관. 체인에서 전부 뺐다(Reaper/ReaperLord 씬 자체는 이야기 모드 스테이지가 아직 참조하므로 남김, DrownedFerryman 은 삭제).
> **①-2 진짜 문제는 '변별':** 첫 스크린샷을 찍고 나서 발견 — 수몰 원혼/휩쓸린 넋/물 먹은 넋 **3종이 `enemies/wraith` 시트 하나를 돌려쓰고 있어** 화면이 흰옷 여귀 일색이었다. 컨셉이 아니라 변별이 무너진 것이라 판단해 「휩쓸린 넋」·「물 먹은 넋」도 삭제.
> **①-3 신규 3종(전용 PixelLab 아트, v3 side view, idle/walk/attack/death 5프레임):**
>   · **두억시니**(`Dueoksini`, 70px) — 도롱이를 걸친 웅크린 잡귀. 옛 「휩쓸린 넋」의 빠른 무리 자리.
>   · **창귀**(`Changgwi`, 74px) — 호랑이에게 죽어 그 앞잡이가 된 넋. 원거리 견제(옛 저승사자 자리). 산길 괴담이라 골짜기 컨셉과 정확히 맞는다.
>   · **물귀신**(`Mulgwisin`, 90px) — 소(沼)에 잠긴 넋. 맞으면 **1.4초간 이동 55%**(`scripts/enemies/mulgwisin.gd`). 옛 「저승 뱃사공」+「물 먹은 넋」 자리를 함께 대체.
> **②-1 크기 버그의 진짜 원인:** `.tscn` 들이 `Sprite2D` 에 `scale = Vector2(1.8,1.8)` 식으로 확대를 적어뒀는데, `CharacterVisual._ready()` 가 `scale = Vector2(sprite_scale, sprite_scale)` 로 **덮어써서 전부 1.0 으로 무시되고 있었다**(그래서 중간보스가 잡몹만 했다). 11개 씬의 `scale` 을 `sprite_scale` export 로 이관하고, `test_enemy_footing` 을 ⓐ실효 스케일을 `sprite_scale` 로 읽도록 고치고 ⓑ노드 scale≠1 이면 실패하도록 게이트를 추가했다(같은 실수 재발 차단).
> **②-2 중간보스 2종 신설(둘 다 `boss.gd` 재사용, 새 스크립트 없음):**
>   · **못의 이무기**(`Imugi`, 150px, HP420) — 1스테이지 4굽이(무너진 산신당). 용이 되지 못하고 소에 남은 큰 뱀, 산신당은 본래 그것을 눌러 두려 세운 것. 대사 `temple_imugi.json` 신규(옛 `temple_reaper.json` 삭제), 물등 소품 2개 추가.
>   · **비틀린 장승**(`Jangseung`, 150px, HP380) — 2스테이지 3굽이(고사목 웅덩이, 이미 장승·솟대 소품이 서 있던 자리). 그슨대를 막으려 세운 장승이 되레 물든 것. **칼이 통한다** — 부적이 필요한 그슨대와 대비되어 규칙을 다시 각인시킨다.
> **②-3 패턴 격리:** 보스 패턴 풀을 하드코딩에서 `@export var pattern_pool: PackedStringArray` 로 뺐다(비우면 기존 동작 그대로 — 하위호환). 이무기는 `dash/pillars/wave`(물), 장승은 `dash/volley/summon`(물기둥·밀물 제외 + `water_entrance=false`) — 숲에 물 연출이 섞이지 않게.
> **검증:** 헤드리스 **56스위트 전부 그린**. 신규 3스위트 — `test_midboss`(스테이지별 크기 서열 + 중간보스 패턴 격리), `test_stage_roster`(배치된 적 씬 실존 + 1스테이지 금지 목록), `test_mulgwisin`(감속 후 **복구**까지). **스크린샷으로 눈 확인**: 1스테이지 잡몹 4종이 실루엣으로 구분되고, 중간보스가 잡몹의 약 2배·최종보스의 절반으로 서열이 보인다. **함정 메모:** ⓐPixelLab v3 는 프롬프트를 문자 그대로 그린다 — 「어둑시니」를 "crouching beast made of night"로 적었더니 서양 늑대인간이 나와 폐기하고 장승으로 방향 전환(3 gen 소모). 「창귀」도 첫 판은 유령이 아니라 산적처럼 나와 "translucent/mist/no legs" 명시해 재생성(2 gen 소모). ⓑ`test_mulgwisin` 이 처음엔 통과했다가 나중에 실패 — 테스트가 적 AI 의 **진짜 공격**과 경합해 감속이 재적용된 것이었다(플레이어를 사거리 밖으로 옮겨 해결). 적 AI 가 도는 테스트는 거리부터 확인할 것.
>
> **이전 (2026-08-20 텔레그램/WSL — 그슨대 노괴 전용 보스 아트):** 본편 5굽이+보스 푸시(`526ca42`) 후 사용자가 "보스는 어린 남자아이 모습에서 공격하면 거대한 그림자 요괴로 변하는 컨셉으로 전용 아트 생성" 요청. **구현:** `CharacterVisual`(`scripts/visual/character_visual.gd`)에 `set_sheet(new_sheet, new_scale, new_offset)` 추가 — 시트 자체를 통째로 교체(런타임 재생 중인 애니 이름은 유지해 이어 재생). PixelLab 로 2형태 별도 생성: 위장(소년) `geuseondae_elder_boy`(v3, 48px, idle/walk/attack 3종) + 정체(거대 그림자) `geuseondae_elder_shadow`(v3, 128px, idle/walk/attack/death 4종). **함정:** 첫 시도에서 그림자 프롬프트에 "towering over a person"(사람 위로 우뚝 솟은)을 넣었더니 PixelLab 이 이를 문자 그대로 해석해 **모든 프레임에 작은 사람 형상이 같이 그려짐** — 프리뷰로 발견, 재생성 없이는 못 씀. `geuseondae_elder_shadow_v2` 로 "alone"·"no other figures in frame" 명시해 재생성 → 깨끗한 단독 몬스터로 해결(추가 3+4=7 gen 소모, 첫 시도분은 버림). `tools/pixel/assemble_geuseondae_elder.gd` 신규(두 폼을 한 스크립트에서 처리, `assemble_geuseondae.gd` 패턴과 동일 union-bbox 크롭) — boy: 26×39→26×47 크롭, foot_offset=-23.5 / shadow: 105×123 크롭, foot_offset=-61.5(스케일 1.0, 즉 축소 없이 그대로 — 보스답게 크게). `geuseondae_elder.gd`의 `_reveal()`에서 기존 `sprite.scale`/`modulate` 직접 조작 대신 `sprite.set_sheet(revealed_sheet, ...)` 호출로 교체(형태 자체가 바뀌므로), `revealed_color` 는 흰색으로 되돌림(더는 색조 트릭 불필요 — 진짜 다른 아트라서). **검증:** `--import` 두 번 → 헤드리스 **51스위트 전부 그린**(회귀 없음, 로직 테스트라 아트 교체 영향 없음이 기대대로 확인됨). PixelLab **295/2000 사용**(이 작업 27 gen). **⚠ 시각 미확인:** 여전히 X서버 죽어있어 실제 화면 확인 못 함 — 특히 shadow 폼이 native 105×123 스케일 그대로라 게임 내에서 정말 "거대해 보이는지" 폰 확인 필요(위 "다음 할 일" 1번). 콜리전 박스는 안 건드림(위 11번 참고). 로컬 커밋만, 푸시는 사용자 승인 후.

> **이전 (2026-08-20 텔레그램/WSL — 2스테이지 본편 5굽이+보스 완성, 그슨대 아트 마무리):** 사용자가 수직 슬라이스를 승인·푸시 지시 후 "계속 진행" → 이어서 본편 확장.
> **①아트 마무리:** 이전 세션에 큐잉만 해둔 그슨대 애니 4종(idle/walk/attack/death, id `98c879d2…`)이 이번엔 전부 완료돼 있어 `list_jobs`/`get_character` 로 확인 → 프레임 20장(5×4) 다운로드 → `tools/pixel/assemble_geuseondae.gd`(assemble_floodwraith.gd 패턴 그대로 신규 작성) 로 union-bbox 크롭 스트립+manifest 조립(39×49, foot_offset -24.5 산출) → `assets/sprites/enemies/geuseondae/` 배치, `Geuseondae.tscn` 의 placeholder 서브리소스 제거하고 실제 시트 연결.
> **②본편 확장:** `forest_shadow`(1굽이, 기존) 뒤로 `forest_mist`(2굽이, 그슨대 4기) → `withered_hollow`(3굽이, 4기, 솟대·장승 소품으로 "민간에서 그슨대를 막으려 세운 흔적" 암시) → `wailing_thicket`(4굽이, 5기, 보스 직전 최고밀도) → `elder_hollow`(5굽이·보스) 4개 JSON 신규 작성(전부 `earth` 타일+pine/jangseung/lantern/sotdae 기존 소품 재사용, 새 타일셋 없음) + 대응 `scenes/levels/*.tscn` 4개, `stage.gd` 의 `CHAIN`/`CHAIN_TSCN` 을 10칸으로 확장(1스테이지 물 5굽이 뒤에 2스테이지 그슨대 숲 5굽이가 바로 이어지도록 — 사용자가 확정한 순서).
> **③보스(그슨대 노괴/GeuseondaeElder):** `scripts/enemies/boss.gd` 를 상속하는 신규 `geuseondae_elder.gd`. 잡몹과 같은 위장/성장/부적노출 규칙을 보스 스케일로 이식 — 위장 중 칼 피격은 `_threat` 만 올리고(무적), 부적으로 드러내는 순간 `health.max_hp` 가 `1+threat_hp_mult*위협도` 배로 커진 뒤 그 체력으로 리필돼 실제 전투가 시작(공격력도 같은 비율로 상승) — "함부로 베면 노괴가 더 강해진 채로 진짜 전투가 시작된다"는 리스크를 보스전에도 관철. 패턴은 기존 Boss 클래스의 돌진/부적세례/소환 3종만 재사용(소환은 `summon_scene` 을 `Geuseondae.tscn` 으로 바꿔 "잡몹 그슨대를 불러모음"으로 재해석) — 물기둥/밀물은 물 스테이지 전용 FX(`WaterPillar`/`TideWave`)라 컨셉 불일치로 `_choose_pattern()` 오버라이드해 완전히 배제. `boss.gd` 에 `subtitle`·`water_entrance` export 2개를 추가(하위호환, 기본값은 기존 FloodWraith 텍스트/true 유지)해 이름표 소제목과 물기둥 등장 연출을 보스마다 다르게 끌 수 있게 — 하드코딩돼 있던 걸 최소 변경으로 재사용 가능하게 함. 아트는 전용 제작 대신 잡몹 그슨대 시트를 `sprite_scale=1.8` 로 확대 재사용("늙어서 더 커진 그슨대" 설정과 결이 맞음, 시간 절약).
> **검증:** 신규 `tests/test_geuseondae_elder.gd`(위장 무적+성장 / 부적 노출 시 위협도 비례 체력 배율 / 노출 후 정상 피해 / 물 전용 패턴 미출현 4케이스) + 체인이 10칸으로 늘며 깨진 `test_gameplay_chain.gd` 기대값 갱신(ForestShadow→Clear 였던 하드코딩을 4굽이 경유로) → **헤드리스 51스위트 전부 그린**. **⚠ 시각 미확인:** 이 세션도 X서버가 죽어있어 실제 플레이 화면은 한 번도 못 봄 — 그슨대 노괴 확대 비율·보스 체력 배율 밸런스는 실기기 확인 필요(위 "다음 할 일" 1·2번). 커밋은 로컬만, 사용자 규칙([[feedback_git_push]])대로 푸시는 승인 후.
>
> **이전 (2026-08-20 텔레그램/WSL — 2스테이지 「그슨대 숲」 수직 슬라이스):** 사용자가 「월드 구조」 원칙(스테이지=던전 컨셉 하나) 승인 뒤 "2스테이지 기획·제작" 지시 → 조사 fork로 기존 아키텍처(스테이지는 `stage.gd` 의 `CHAIN` 배열 + `assets/stages/*.json` 데이터 기반, 1스테이지는 이미 물 컨셉 5굽이 통일) 파악, Tiger/Goblin/Fox 코드는 있는데 아트 미완성 상태임을 발견 → 컨셉 후보 3안(호랑이·도깨비·구미호) 제시했으나 사용자가 "조선시대 괴물을 찾아 고증해서 구현" 요청 → 웹서치로 창귀·산군·도깨비·구미호·그슨대·어둑시니 등 설화 고증(출처: 나무위키) → 사용자가 **그슨대** 선택. 코디네이터가 사용자 제공 원문(운몽선 설화)을 재확인해 기믹 보강 요청 — "공격 받을 때마다 커짐"이 핵심이라 단순 무적이 아니라 **성장형 반격 리스크**로 설계 변경. **구현:** `scripts/enemies/geuseondae.gd`(patroller.gd 상속) — 위장 상태에선 `HealthComponent.shield_charges` 를 매 피격마다 999로 재충전해 절대 안 죽지만, `shield_broken` 시그널로 `_threat` 를 올려 스프라이트가 커지고(위협도 2 이상부터는 위장 중에도 반격 시작) — "베려 할수록 위험해진다"를 코드로 구현. `talisman_shot.gd` 에 `_on_talisman_hit` 훅 추가(부적=빛/횃불에 맞으면 데미지 대신 정체 노출, 이후엔 보통 진혼 가능) — 새 스킬 만들지 않고 기존 「진혼의 물등」(TalismanShot) 재활용. 수직 슬라이스 레벨 `assets/stages/forest_shadow.json`(그슨대 3기, earth 타일+pine/jangseung 기존 에셋 재사용, 야간 backdrop — 새 타일셋은 안 만듦) + `scenes/levels/ForestShadow.tscn`, `CHAIN` 배열에 `sacred_altar` 뒤로 추가해 실제 플레이 흐름에 연결(임시 순서 — 아래 "판단이 필요한 것" 참고). **검증:** 신규 `tests/test_geuseondae.gd`(위장 상태 무적+성장 / 부적 노출 / 노출 후 정상 피해 3케이스) 작성·통과, 기존 `test_gameplay_chain.gd` 는 체인에 새 굽이가 끼며 깨져서(SacredAltar→Clear 하드코딩) 기대값 갱신 → **헤드리스 49스위트 전부 그린**. PixelLab 로 그슨대 캐릭터 생성 큐잉(standard 모드, id `98c879d2…`, 아래 "다음 할 일" 1번) — 애니메이션 생성·스트립 조립은 다음 세션. 아트 없이는 아직 `PlaceholderTexture2D`. **⚠ 시각 미확인:** 이 세션도 X서버가 죽어있어 실제 플레이 화면은 한 번도 못 봄. 커밋은 로컬만(사용자 승인 대기), 사용자 규칙([[feedback_git_push]])대로 푸시는 안 함.
>
> **이전 (2026-08-19 텔레그램/WSL — 주인공 아트 후속 수정: walk 버그·전용 구르기·크기 축소):** 재시도 성공 보고 직후 사용자가 실기기(추정)에서 4가지 지적: "① 피부색이 달라지는 프레임 ② 창이 안보이는 프레임 ③ 크기는 이전과 같게(축소 가능?) ④ 구르기 동작이 이전과 다름". **전수 조사:** 직전엔 각 애니 첫 프레임만 비교했던 게 함정 — 이번엔 idle(5)·walk(6)·jump(9)·hurt(7)·death(7)·attack(9) **전 프레임을 하나씩 확인**. 결과: **walk 프레임 2·3에서 창이 완전히 사라짐**(명백한 버그, "②"에 해당) — 나머지 idle/jump/hurt/death/attack 은 전 프레임 스캔해도 무기·피부색 이상 없음("①"은 walk 재생성으로 해결됐길 기대, 특정 못 찾으면 다음 세션에서 사용자에게 정확한 애니/프레임 번호 재요청 필요). **조치:** walk 삭제 후 v3 커스텀으로 7프레임 재생성(전 프레임 창 유지 확인) → "③" 크기는 콘텐츠 높이 비교(구 92px 프레임 실측 77px / 신 256px 프레임 실측 171px)로 `sprite_scale=0.4503` 산출해 `CharacterVisual` 에 신규 export 추가, `foot_offset` 도 스케일 반영 공식(`16/scale - foot_y + canvas_h/2`)으로 -56.47 재계산 → "④" dodge 는 기존 jump 착지 크롭 대신 "웅크려 구르는 회피" 전용 v3 커스텀 5프레임으로 교체. **함정:** 되돌림 커밋이 이전에 만든 `assemble_protagonist_v2.gd`·`detect_foot_y.gd` 도 지워버려서 이번에도 재작성 필요했음(git revert 는 그 세션에 새로 만든 도구 스크립트까지 통째로 지운다는 걸 계속 잊지 말 것). **검증:** 헤드리스 48스위트 그린 → 커밋(`efd19a4`) → 푸시. **⚠ 시각 미확인 계속:** X서버가 이 세션 내내 죽어있어 실제 게임 화면은 여전히 한 번도 못 봄 — sprite_scale 수치가 정말 적절한지, walk 재생성이 skin-tone 이슈까지 실제로 해결했는지는 다음 세션(또는 사용자 실기기 확인)에서 검증 필요.
>
> **이전 (2026-08-19 텔레그램/WSL — 주인공 아트 개선 재시도 → 성공):** 되돌림 직후 사용자가 "네 그렇게 진행하시죠" + "퀄리티 개선을 진행하시죠"로 재시도 승인. 계획대로 **먼저 idle 1개만** v3 커스텀으로 테스트: `delete_animation` 으로 기존 idle 템플릿 그룹 삭제 → `animate_character(mode="v3", action_description="...gripping the same ornate glowing purple spear...")` 로 재생성 → 5프레임 전부 창·로브 유지 확인(그리드 이미지 직접 비교) — **성공**. 이어서 jump·hurt·death 도 동일 패턴(각 action_description에 "같은 자수정 창을 쥔 채" 명시)으로 재생성 → 역시 전부 일관 유지 확인. walk 는 직전 시도에서 우연히 잘 나온 template 결과를 그대로 재사용(재생성 안 함). **재통합:** 되돌림 커밋이 이전 세션에 만든 `tools/pixel/assemble_protagonist_v2.gd`·`detect_foot_y.gd` 도 같이 지워버려서 **재작성 필요**(내용은 동일하게 복원). idle/jump/hurt/death 는 이번엔 전부 v3라 **256px 로 직접 렌더링**(직전 시도처럼 224/256 섞여서 패딩 처리할 필요가 없어짐 — walk 만 224라 여전히 패딩). foot_y 를 idle 새 프레임에서 재측정(y=220, 이전 -78 계산과 살짝 다름) → `foot_offset = -76.0`. 파생 5종(run/attack2/attack3/charge/dodge)은 이전과 동일한 재사용/크롭 방식. **검증:** 6개 애니 첫 프레임 나란히 비교 그리드로 창·로브 일관성 육안 확인 → 헤드리스 48스위트 그린 → 커밋(`2609dff`) → 사용자 사전 승인으로 즉시 푸시. **⚠ 여전히 시각 미확인:** 이 세션 내내 X서버가 죽어있어 실제 게임 화면 스크린샷은 단 한 번도 못 찍음 — 다음 세션 최우선 확인 필요(위 "다음 할 일" 1번).
>
> **이전 (2026-08-19 텔레그램/WSL — 주인공 아트 전면 개선 시도 → 실패 원인 분석 → 되돌림):** 사용자 요청으로 주인공을 92→256px 확대 + 보라 창으로 재제작(idle/walk/jump/hurt/death 템플릿 애니 + attack v3 커스텀, 상세 과정은 이전 커밋 로그 참조 — 이 되돌림으로 해당 HANDOFF 기록 자체도 사라졌으므로 `git log -p` 로 `798ea88`/`2a06329` 확인 가능)해 커밋·푸시까지 마쳤으나, 사용자 피드백 "캐릭터의 일관성이 부족하고 엉망이네요" + "크기도 이전과 같은 크기로 바꿉시다". **원인 분석**(각 애니 첫 프레임을 나란히 비교하는 그리드 이미지로 직접 확인): idle·walk·hurt·death 4개 템플릿 애니에서 **보라 창이 아예 사라짐**(맨손)이고, death 애니는 **장포 로브 자체가 없는 완전히 다른 복장**(그냥 셔츠+바지)으로 나옴 — 반면 v3 커스텀으로 만든 attack 애니만 창·로브가 온전히 유지됨. **결론:** PixelLab 템플릿 애니메이션(`animate_character(template_animation_id=...)`, 1 gen/방향으로 저렴)은 스켈레톤 포즈만 강하게 따르고 참조 캐릭터의 무기·의상 디테일은 **매 애니마다 독립적으로 재해석**해서 약하게만 유지 — 특히 창처럼 크고 특이한 손에 든 아이템은 포즈 템플릿에 잘 안 얹힘. v3 커스텀(action_description 기반)은 참조 이미지 조건화가 더 강해 디테일이 유지된 것으로 보임. **조치:** `git revert --no-edit 2a06329 798ea88` 으로 두 커밋(아트 커밋 + 그 HANDOFF 기록 커밋) 되돌림 → `--import` 두 번 → 헤드리스 48스위트 재확인 그린 → 사용자 사전 승인([[feedback_git_push]])으로 즉시 푸시(`3094f0b`). 주인공은 다시 원래 92px 버전(창 없음, 기존 디자인). PixelLab 쪽 `e29cea1b`(보라 창 상태)·`c6ae478a`(그룹)는 삭제 안 하고 남겨둠 — 재도전 시 참고 가능. **다음 세션 교훈:** 캐릭터 전체 애니 세트를 다시 뽑아야 하면 template 모드의 "저렴하지만 디테일 유지 약함" 트레이드오프를 미리 사용자에게 알리거나, 처음부터 `pro` 모드(20-40 gen/방향, 방향 간 교차참조로 더 일관적일 가능성)나 전부 `v3` 커스텀으로 통일하는 편이 나을 수 있음. 최소한 애니 1~2개만 먼저 뽑아 일관성 확인 후 나머지 진행하는 식으로 조기 검증했어야 함(이번엔 6개를 다 뽑은 뒤에야 사용자가 발견).
>
> **이전 (2026-08-19 텔레그램/WSL — 점프 애니를 시간재생으로 전환):** 텔레그램으로 "작업 잘 되고있나요" 문의 → 스테이징만 돼 있고 미커밋 상태였던 이전 작업(직전 커밋 `c4a2fc1`이 점프를 탭/홀드 관계없이 항상 풀 점프로 고정한 데 이어, 체공 시간이 일정해졌으니 속도 임계값으로 정지 프레임 고르던 방식을 시간재생으로 바꾸는 중이었던 것으로 보임)을 이어서 마무리. **검증:** `--headless --import` 두 번(클래스 캐시 함정 대응) 후 헤드리스 **48스위트 전부 그린**(신규 `jump_animation_plays` 케이스가 체공 중 프레임이 실제로 진행되는지 확인 — PASS). **시각 캡처는 실패:** 신설된 `tools/JumpPreview.tscn`으로 스크린샷을 남기려 했으나 이 세션의 `DISPLAY=:0`이 죽어있고(`xdpyinfo` 접속 실패) `Xvfb`도 미설치(+sudo 비번 필요)라 캡처 불가 — 헤드리스 로직 검증까지만 하고 커밋(`e906a15`). **다음 세션:** 텀블링 모션이 실제로 자연스러운지 스크린샷/실기기로 확인 필요(위 "다음 할 일" 1번). 사용자 푸시 승인 대기 중.
>
> **이전 (2026-08-19 텔레그램/WSL — 카메라 프레이밍 수정: 지면선을 화면 절반→하단으로):** 사용자 피드백 전달 "땅을 더 낮춰서 표시되도록 해주실 수 있나요" + "모바일 풀스크린에서 캐릭터·적이 가려짐". **원인 조사:** `stage.gd`의 동적 카메라(`Camera2D.new()`, offset 없음)와 5개 레거시 씬(Village/Forest/TestLevel/ShrineRuins/BossArena)에 박힌 카메라 둘 다 플레이어 원점(=지면선, world y=684)에 오프셋 없이 붙어 있어 **지면선이 항상 화면 세로 정중앙(50%)** 에 걸림. 동시에 모바일 터치 스킬줄(화면 y≈340부터 하단까지)이 바로 그 지면선 위쪽 구간과 겹쳐, 지면 근처에 선 캐릭터·NPC·적이 버튼 원에 가려짐 — **두 불만이 같은 원인**. `--touch` 옵션으로 `tools/Screenshot.tscn` 캡처해 실제로 재현 확인. **수정:** `stage.gd`에 `CAMERA_OFFSET_Y := 160.0` 상수 추가 후 동적 카메라에 `offset = Vector2(0, -160)` 적용, 레거시 5씬의 인라인 `Camera2D` 노드에도 동일 `offset` 프로퍼티 추가 — 지면선이 화면 하단 ~75% 지점으로 내려가 다른 횡스크롤 게임들의 일반적 프레이밍과 비슷해짐. 스크린샷으로 전/후 비교(마을·숲·보스아레나·이야기모드 3막) — 캐릭터·NPC가 버튼 클러스터 위로 명확히 벗어남. 헤드리스 47스위트 그린 확인 후 커밋(`0670564`). **머지 필요 발견:** push 시 원격에 PC 세션의 최종 보스 대개편 4커밋(`87a479f`)이 먼저 올라와 있어 거부됨 → `git fetch` 로 확인해 겹치는 파일 없음 확인 후 `git merge`(충돌 없음) → 클래스 캐시 함정 메모대로 `--import` 두 번 재적용 → 헤드리스 48스위트(신규 `test_boss_patterns` 포함) 재확인 그린 → `main` 푸시(`a373b64`). **다음:** 사용자 폰 실플레이로 새 카메라 프레이밍 확인, 최종 보스 난이도도 같이 체감 확인.
>
> **이전 (2026-08-19 PC — 최종 보스 대개편: 패턴 5종·전용 아트·등장 연출·잡몹 증원):** 사용자 요청 "마지막 보스 퀄리티를 높이자 — 원거리 등 피할 수 있는 패턴 다양하게, 아주 큰 한국적 귀신". **①PixelLab MCP 연결**(프로젝트 `.mcp.json`, gitignore 처리. 최초엔 구독 만료로 생성 불가 → 사용자가 갱신). **②패턴 5종 + 페이즈2**(커밋 `1bdb9e0`): 돌진 하나뿐이던 보스를 **회피 동작이 각기 다른** 다섯으로 — 돌진(점프/뒤로) / 부적 세례(탄 사이) / 물기둥(옆으로, 신규 `WaterPillar`: 예고 중 판정 OFF) / 밀물(점프만, 신규 `TideWave`: 판정 높이 34px) / 소환(페이즈2). 가중 무작위 + 직전 제외, 머리 위 경고 기호를 패턴별로 다르게(`!` `※` `▲` `≈` `＋`). 신규 `test_boss_patterns` 6케이스가 **5패턴 도달·연속반복 없음·소환 페이즈2 전용·물기둥 예고중 무판정·밀물 점프 가능 높이**를 검사. **③전용 아트**(커밋 `e4a12a8`): 시안 5종(A~E) 중 **E안 '수몰 군체 귀'** 채택 — 소복 여귀 몸통 뒤로 죽은 자들의 팔과 얼굴이 솟고 하반신은 거대한 파도(설정과 정확히 일치). 256px v3 + 애니 4종, `tools/pixel/assemble_floodwraith.gd` 로 **전 애니 union bbox 일괄 크롭**해 스트립 조립(200×236). 잡몹 확대본 → 전용 시트 교체. **④난이도·덩치·이펙트·등장·잡몹**(커밋 `61beaf3`): 보스 1.25배(플레이어의 약 5배)·HP 560→760·예고 0.82→0.62초·회복 1.35→0.85초, 패턴 수치 전반 상향. **PixelLab 물 이펙트 6종**(물기둥·밀물·등장 물기둥)을 코드 연출 위에 얹음. **등장 연출** — 물기둥이 솟구쳐 보스를 드러내고 이름판이 뜸(`_play_entrance`). ⚠ **연출이 비동기라 다음 프레임에 패턴이 먼저 나가던 버그**를 `_entrance_playing` 잠금으로 수정(스샷으로 재현·확인). 잡몹 스케일 일괄 상향 + 전투 체인 **17기→33기**. **검증:** 헤드리스 **48스위트 전부 그린**, 보스 접지·등장·물기둥·밀물·잡몹 렌더 확인. PixelLab **113/2000** 사용. **커밋 3개 푸시 완료.** **⚠ 미검증:** 실제 손맛 — 예고 0.6초가 폰에서 반응 가능한 난이도인지는 실플레이 필요.>
> **이전 (2026-08-18 텔레그램/WSL — Godot 바이너리 발견, 사이드 진혼 3기 헤드리스 그린 검증, 푸시·배포 완료):** "HANDOFF 읽고 이어서" 지시로 시작. 직전 세션이 "Godot 없는 세션"으로 기록해뒀으나 이번엔 `~/godot-bin/godot` 4.6.3 headless 바이너리가 실제로 있어 CI와 동일한 절차(`--headless --import .` ×2 → `tests/*.tscn` 전수)로 **헤드리스 46스위트 전부 그린 확인**(exit code + `[FAIL]` 마커 기준, ObjectDB 잔류 경고는 Godot 종료 시 흔한 무해 로그라 오탐 아님 확인). 사이드 진혼 3기(4막/6막전반/7막) 배선이 회귀 없이 정상 동작. PixelLab 잔여 재확인 결과 여전히 만료 상태라 항목 2는 착수 불가. Android SDK도 이 환경엔 없어 항목 4도 불가. 항목 5(씬 파일명 개명)는 세이브 매니저가 경로 문자열을 참조해 리스크가 있다고 판단, 사용자 확인 없이 단독 진행 안 함. 사용자에게 푸시 승인 요청 → **승인받아 `main` 푸시(`96a7ed5`), GitHub Actions CI 둘 다(`Godot Headless Tests`·`Godot Web Export`) success 확인, GitHub Pages 자동 배포 완료.** **다음: 사용자가 폰으로 사이드 진혼 3곳(4막/6막전반/7막) 실플레이 확인 → 피드백 대기. PixelLab 갱신되면 항목 2 재개.**
>
> **이전 (2026-08-18 텔레그램/WSL, Godot 없는 세션 — 사이드 진혼 1호 배선 + PixelLab 만료 발견):** "HANDOFF 읽고 이어서" 지시로 시작. 로컬이 `4751f99`(스코어어택 피벗)에 멈춰 있어 `git pull`로 `d80adf4`까지 동기(스토리 v2 대개편이 이미 반영돼 있었음 — 로컬 감사 필요했음). "다음 세션 시작점" 항목 3(사이드 진혼 실체화)을 코드/데이터만으로 진행. **①PixelLab 구독 만료 확인**(2026-07-26 만료, 잔여 0/2000, 갱신 전까지 생성 불가) → 항목 2(무릎 꿇은 포즈 아트)는 이 세션에서 착수 불가, 갱신 후로 보류. **②선택 전투 엔진 신설:** 결계(`combat_gate.gd`)가 세던 "enemy" 그룹을 그대로 두고 새 **"enemy_gate"** 그룹을 결계 판정 전용으로 분리 — `patroller.gd`에 `optional`(true면 enemy_gate 미가입, 결계를 막지 않음)+`on_death_flag`(처치 시 플래그) export 신설, `dummy.gd`/`boss.gd`는 무조건 enemy_gate 가입(기존 필수 전투 동작 불변 확인 — 두 곳 다 기본값 false라 회귀 없음). `stage.gd._build_enemies`가 JSON `optional`/`on_death_flag`를 그대로 전달. ③**4막에 1호 배치**(`haewon_3_village.json` — 신칼 두고 간 무당마저 손 못 댄 넋, x=460 Wraith, `haewon_side_lost_wraith`) → `EndingResolver.BURN_FLAGS` 5번째 항목으로 연결(저울 로직은 burned count 기반이라 4→5개도 그대로 동작, `test_endings` 조합 전수 영향 없음 확인). ④**data-integrity 테스트 신설**(`test_story_data._check_side_battle_flags`) — 모든 `on_death_flag`가 `optional=true`이면서 `BURN_FLAGS`에 등록돼 있는지 전수 검사(설계 실수 방지: 필수 전투에 잘못 플래그 달았거나, 죽은 플래그가 남는 경우 즉시 잡음). **⚠ 이 세션은 Godot 없어 헤드리스 미실행·시각 미확인 — 로컬 커밋만 완료, 사용자 승인 전 미푸시([[feedback_git_push]]).**
>
> **이전 (2026-08-17 PC 심야 — 터치 판정 확대 + 스킬 1·3·4 연출 대개편):** 폰 실플레이 피드백 4건 처리. **①터치가 잘 안 눌림** → 그려지는 원보다 **판정원을 1.28배 크게**(텍스처를 판정원 크기 캔버스에 가운데 정렬해 그림·판정을 동심원으로 유지) + 버튼 자체 확대(이동 52→58, 공격 60→66, 점프 48→54, 회피 44→50, 조사 36→42, 스킬 34→40) + **`passby_press=true`**(손가락을 끌어 좌↔우로 넘어가도 눌림) + 커진 판정에 맞춰 우측 클러스터 재배치(겹침 0, 테스트가 검증). **②스킬1 「여울 가르기」**: 즉발 돌진 → **시전 0.32s(`river_gather` 물이 몸으로 감겨듦) → 일직선 돌진(430→560 속도, 0.22→0.42s) → 지나간 자리마다 파도(`wave_wake`, 0.06s 간격) → 멈춘 자리 물기둥(`river_burst`)**, 히트박스 72×34→86×40. **③스킬3 「호신부」**: 흰 사각형 3개 → **한지 바탕+먹 테두리+붉은 주사 획 부적 3장**(각자 나부끼고 금빛 불티 매달림) + 이중 후광(맥동) + 발동 순간 `ward_cast`(한지 조각이 모여들고 금빛 파문). **④궁극기 「귀창 강림」**: 즉발 → **공중 부양 120px → 시전 0.65s(`ultimate_charge`: 창 8자루가 하늘에서 모여듦) → 급강하 0.12s → 착지 순간 광역 피해 + `ground_shock`(좌우로 달리는 충격파·금빛 균열)**. 부양 중 중력·입력 정지(`_ult_hover`), 낙사·사망 시 해제. **검증:** 헤드리스 **44스위트 전부 그린** — 신규 `test_skill_motion`(시전 후 실제 돌진 거리 / 궁극기가 **반드시 원래 높이로 착지**: 부양 상태가 남으면 중력 꺼진 채 얼어붙는 최악 회귀를 막는 케이스), `test_ultimate` 은 피해 시점이 늦어진 새 설계에 맞춰 대기 시간 갱신. **`tools/SkillFxPreview` 신설** — 스킬별 연속 프레임 캡처(`shots/verify/fx_*.png`)로 시전·이동·마무리를 육안 확인(스틸 한 장으론 못 보는 연출용). **커밋 `346a5b7` 푸시.** **⚠ 남은 판단:** 시전 시간(0.32s/0.65s)이 실전에서 답답한지, 터치 판정이 이제 충분한지는 폰 실플레이 확인 필요.
>
> **이전 (2026-08-17 PC 후반 — 모바일 앱 출시 방향 확정 + 터치 UI 전면 개편):** 사용자가 **안드로이드·아이폰 앱 출시**로 전략 전환. **①결론: 웹뷰 래핑 X, Godot 네이티브 익스포트 O.** 근거는 `docs/RELEASE_MOBILE.md` 에 정리 — App Store 가이드라인 4.2(웹사이트 래핑 리젝), **iOS WKWebView 는 `navigator.vibrate` 미지원이라 진동이 아예 안 됨**, 오디오 지연·최초 .pck 다운로드·오프라인 불가, 리더보드/IAP 부착 난이도. 순서는 **Android 먼저(맥 불필요, Play 개인계정은 클로즈드 테스터 20명×12일 필요 → 2주 확보) → iOS(맥+Xcode+$99/년)**, 웹 빌드는 데모·테스트 링크로 유지. **②터치 컨트롤 전면 개편**(`mobile_controls.gd` 를 코드 배치로 재작성, tscn 은 빈 CanvasLayer): 안전영역(노치·제스처바) 반영 + 뷰포트 변화 시 재배치, **왼손=이동/조사 · 오른손=공격·점프·회피 클러스터**로 정리(전엔 대각선으로 흩어져 있었음), **스킬 4종을 아이콘+쿨다운 숫자 버튼으로**(궁극기 skill_4 버튼이 **아예 없었음** → 추가, 옛 스킬명 라벨 '일섬/회천/호신' 제거하고 skills.json 아이콘 사용), **한지·먹 원형 버튼 텍스처를 코드 생성**(눌림=단청 적, 새 아트 0), TouchScreenButton 유지(멀티터치=이동하며 공격) + `shape_centered=false` 로 판정원을 텍스처와 정확히 정렬(기존엔 반 칸 어긋나 있었음), **데스크톱에선 숨었다가 첫 터치에 표시**(PC 브라우저에서 터치 UI 뜨던 문제). HUD 는 터치 기기에서 스킬 줄을 숨겨 화면을 넓힘. **③화면비:** `stretch/aspect` **keep→expand** 로 폰 레터박스 제거(사용자가 예전부터 지적하던 여백) + 넓어진 뷰포트가 스테이지 밖을 비추지 않게 `stage.gd` 가 **지면을 좌우 500px 덧댐**. **④진동 설정:** `ScreenFx.set_haptics()` + `user://settings.cfg` 저장, 설정 메뉴에 토글. **⑤`export_presets.cfg` 에 Android 프리셋**(com.ksjmgrkks.haewon, arm64-v8a, minSdk 24, 몰입모드, 인터넷 권한 없음). **검증:** 헤드리스 **43스위트 전부 그린**(신규 `test_mobile_ui` 5케이스: 버튼 존재·화면 안·비겹침·터치 크기·진동 저장), **1280/1560/1240×720 세 화면비 렌더 확인**(`shots/verify/mob_*.png` — 레터박스 없음, 지면 빈 칸 없음, 버튼이 가장자리에 정확히 붙음). **커밋 `4e7b5a3`·`a79128d`.** **⚠ 미검증:** 실제 APK 빌드·실기기 설치(Android SDK/keystore 이 PC에 없음), 실기기 진동 체감, 런처 아이콘(미제작). **다음:** Android SDK·keystore 세팅 → APK 빌드 → 실기기 확인 / 런처 아이콘 제작 / 스토어 등록 정보.
>
> **이전 (2026-08-17 PC — 적 HP 바 머리 위 재배치 + 타격감 강화):** 사용자 피드백 2건 처리. **①적 HP 바:** 고정 `Y_OFFSET=-28`·폭 36·두께 4px 이라 큰 적은 바가 **몸통 한가운데** 걸리고 얇아 안 보이던 문제 → `EnemyHpBar` 를 **적 스프라이트 실측 기반**으로 재작성. 프레임 크기·`scale`·`foot_offset` 에 더해 **`Image.get_used_rect` 로 실제 그려진 불투명 영역**을 재서(시트마다 투명 여백이 달라 프레임만 쓰면 머리에서 한참 뜸, 텍스처당 1회 캐시) 머리끝 12px 위에 배치, 폭은 몸 폭 비례(34~84), 두께 **4→7px(보스 10px)**, 좌우 반전 시 바 중심도 따라 뒤집음. + **잔상(lag) 바**(방금 깎인 양이 한지빛으로 남았다 0.16초 뒤 따라 줄어듦), 피격 시 바가 짧게 부풀고(punch), HP 30% 이하 주홍 전환, 어두운 우천 톤에서 읽히게 채움색 상향. 보스는 big 모드 + 경고 '!' 를 바 위로 밀어 겹침 제거. **②타격감(피격 인지):** 받는 넉백 **×1.6**, 넉백 감쇠 `KNOCK_FRICTION` 900→**620**(더 멀리 미끄러짐), 경직 0.12~0.4→**0.18~0.5초**, 흰 플래시 0.12→0.2초, squash 강화(1.22/0.8→1.26/0.76) + **맞은 방향으로 휘청(회전 0.22rad)**, 밀려나는 동안 **잔상 트레일**. Dummy/Boss 에도 같은 움찔 적용(보스는 얕게, 받는 넉백 0.3→0.5). **③햅틱 신설:** `ScreenFx.rumble(ms)` — Android/iOS/Web 에서만 `Input.vibrate_handheld`, 적중 시 콤보 단계별(10+8×단계 ms)·피격 시 45ms. 모바일 앱 출시 대비 첫 조각. **검증:** 이 PC에 Godot 이 없어 **사용자 승인 받아 4.6.3 공식 릴리스 재설치**(`C:\Users\User\Downloads\Godot_v4.6.3-stable_win64.exe\`, 실제 파일명은 `_win64.exe.zip`) → **헤드리스 42스위트 전부 그린**(신규 `test_hit_feel` 5케이스: 바 머리 위·두께·잔상·넉백/경직·보스 바 / `test_polish` 의 폐지된 `BAR_WIDTH` 참조 갱신), **`tools/HpBarPreview.tscn` 신설**(적 6종 나란히 세우고 한 대씩 때린 상태 캡처)로 `shots/verify/hpbar_after.png` 렌더 확인 — 바가 각 적 머리 위에 정렬됨. **커밋 `62a5826`·`f387a80` — 미푸시(사용자 확인 대기).** **⚠ 스틸컷으로 못 본 것:** 넉백 거리·휘청임·히트스톱의 실제 손맛, 햅틱 세기 → 폰/PC 실플레이 확인 필요. **다음:** ③ 모바일 출시 전략(웹뷰 X → Godot 네이티브 Android/iOS 익스포트) — 별도 논의 진행 중.
>
> **이전 (2026-08-17 — development→main 병합, 전투체인 스코어어택 피벗 배포):** 직전 PC세션(2026-08-15)이 게임 방향을 크게 전환했으나 HANDOFF 미기록 + `main` 미병합으로 GitHub Pages가 반영 안 된 상태를 발견 → 사용자 승인 받아 `development`(3커밋: 통과판정·스코어어택·라이브루프 스토리 제거)를 `main`에 병합·푸시. ★핵심 전환: **①「해원」 스토리를 라이브 루프에서 제거, '새로 시작'=전투 체인 진입으로 변경**(main_menu가 가리키던 상수가 프롤로그였던 어긋남 수정. stage.gd `GAMEPLAY_ONLY=true`로 스토리/NPC/대사/컷씬/퀘스트/기억소거 없이 결계+전진 출구만 빌드. 스토리 파일들은 삭제 안 하고 dormant 보존 — 되살리려면 `GAMEPLAY_ONLY=false`). **②엽전(골드) 경제 → 점수·플레이타임·최고기록 스코어어택으로 전환**(`ScoreManager` autoload 신설, 혼/보스 처치=점수, coin_pouch 드롭 제거, HUD에 점수+시간 표시, 게임오버/클리어 화면에 이번 런 요약+신기록 표시. gold/상점은 dormant, 크래시 없음 확인). **③메탈슬러그식 통과 판정**(플레이어·적 몸은 서로 안 막고 통과, 피해는 Hitbox/Hurtbox 겹침으로만 — 충돌 레이어 3분할: 플레이어1/적2/월드4). 3커밋 모두 헤드리스 39~41스위트 그린 확인하며 순차 커밋됐고 신규 테스트(test_passthrough·test_score·test_pivot) 추가됨. **병합 시 main에 있던 #3-b 해원 대사 톤 개편(아래 2026-07-15 항목)과 파일 충돌 없이 합쳐짐** — dormant 스토리 자산이라 라이브 루프엔 영향 없음. **이 세션은 Godot 없는 WSL/폰 세션이라 병합·푸시만 하고 로컬 재검증은 못 함 — main 푸시로 CI(web-build/headless-tests)가 새 테스트 포함 원격에서 자동 재검증.** **다음:** CI 결과 확인 + GitHub Pages(https://ksjmgrkks.github.io/joseon-rpg/) 배포 후 사용자가 폰으로 새 전투체인/스코어어택 실플레이 확인 → 밸런스 피드백.
>
> **이전 (2026-07-15 PC — 풀 검증 + #3-b 해원 대사 톤 개편 완료):** ① **풀 검증(대홍수 아크 b5e3682):** PC에서 `--import` 1회(project.godot 불변, 신규 PNG 29종) → **헤드리스 38스위트 122케이스 전부 그린.** (.import 라인엔딩 유령 변경은 checkout 정리 — 커밋 안 함.) ② **PC 실렌더(RTX 3070) 스크린샷:** 신규 원혼 전부 접지+발치 물웅덩이 정상 — 뱃사공(숲속)·물아이+둔중(폐사찰)·무리(고갯길)·수문의 원혼(제단 보스), 스킬 아이콘 HUD(1~4 유저키 라벨)·단일 물줄기 비·새 수묵 산세(mtn_far/hill)·바이옴 지면(earth/rock/stone) 모두 정상(`shots/verify/pc_*.png` 로컬 6장, shots는 gitignore). ③ **#3-b 해원 캠페인 대사 톤 개편 완료(내러티브 패스):** 21파일 중 13파일 수정 — 구조(id/노드/next/dissolve/speaker) 불변, text만. **"원님" 7→3곳 절제**: 유지 = 1굽이 뱃사공 첫 암시·flood_night 물이 삼키는 마지막 부름(이중 호명 1회로)·회상 첫 호명(엔딩 회수 원문 보존) / 제거 = yunseul_approach("예서 줄곧 기다렸소"로 기다림을 문장화)·yunseul_release("당신도 나와 함께") 등 감정 정점 반복 호명. 과설명 완화(prologue "미쳐 울부짖는다"→"물을 헤집으며 일어선다", release 끝줄 "빛이 되어"→"그 안으로" — 페이오프 "그 빛이 곧 윤슬이다"는 보존), 말줄임 문장당 1개 이하로 정리. **검증:** 해원 JSON 21개 파싱 유효, 대화 관련 8스위트 40케이스 그린, 말풍선 2샷(1굽이 뱃사공 "원님…" 유지분·6굽이 윤슬 호칭 제거분) 렌더 확인. **사용자 미감 확인 1건:** yunseul_release b 정인 호칭 "당신" — 더 예스럽게 호칭 생략안도 가능. **남은 것:** 웹빌드 캐시/16:9 레터박스 사용자 확인(대기) → 필요시 stretch 조정.
>
> **이전 (2026-07-11 — 텔레그램 4건: 웹빌드 문의·신규 원혼·대사 개편·'남은 적' 제거):** **①웹빌드 다름:** CI(web-build.yml)가 푸시마다 성공 배포 확인 → 원인은 폰 브라우저 캐시(.pck 강캐싱) 또는 16:9 레터박싱으로 판단, 사용자에게 '옛 화면인지/여백만인지' 확인 요청(대기). **②신규 원혼(진짜 새 아트) 완성·통합:** PixelLab v3 캐릭터 2종 신규 생성 — **저승 뱃사공 넋**(`boatman`, char b1176256, 192px, 삿대 도포)·**물아이 넋**(`waterchild`, char 9c04726a, 120px, 애처로운 아이). idle/walk/death(east) 애니 생성 후 **PIL 없이 Godot 조립 스크립트로 프레임→가로 스트립 합성**(get_used_rect union bbox 크롭 → boatman 61×99·waterchild 36×60, manifest 생성). 씬 `DrownedFerryman`(느리고 단단, hp120)·`DrownedChild`(작고 약함 hp50) 신설, foot_offset 스샷 검증(-32.5·-16). 배치: forest_deep/mountain_pass 뱃사공, ruined_temple 물아이(+조우 대사 water_child '그 물을 부른 손이 내 손이다'). **③대사 오글거림 개편(1차):** 전투 체인 대사의 호랑이/여우 잔재(산군·백호·흰여우·호귀) 제거→수몰·물길·젖은 넋 이미지, 과장 사극투 절제, tut_skill 새 스킬명 반영. **④기억 소거 글자 흐려짐 끔**(dialogue_balloon에서 dissolve 배선 제거, 대사 항상 또렷 — MemoryGlyph 순수함수/테스트는 보존). **⑤'남은 적 N' 카운터 제거**(combat_gate, 결계 안내만 정적). **검증:** 헤드리스 37스위트 전부 그린, llvmpipe 스샷으로 신규 원혼 접지/애니/HUD 확인, 텔레그램 스샷 전송. PixelLab ~30gen. **커밋 f725069·261205b·54930aa 푸시.** **남은 것:** #1 사용자 확인(캐시/비율) → 필요시 stretch 조정. #3-b 해원 캠페인(haewon/*) 대사 톤 개편·확장은 계속(원님 등 절제). **신규 몬스터 재통합 파이프라인 = Godot 조립(/tmp/assemble.gd 패턴): 캐릭터 char/anim UUID로 CDN east 프레임 curl → Image.get_used_rect union bbox 크롭 → 스트립+manifest.** (PIL/pip 이 세션에 없음)
>
> **이전 (2026-07-11 — 텔레그램 2건: 스킬1·2 진혼 리테마 + 스토리 부적합 몬스터 개편):** 앞선 맵/배경 개편 푸시 후 사용자 추가 2요청. **①스킬 1·2 개편(게임 컨셉=물·진혼에 맞게):** 옛 보라 마창 톤(호환/귀창록 잔재) → 물빛·물등. 1번 「마창 일섬」→**「여울 가르기」**(물살 참격 돌진, 넓은 물마루 히트박스로 사거리↑), 2번 「선풍 창무」→**「진혼의 물등」**(앞뒤 회전베기 → **반경 200 광역 파문**: 반경 내 모든 적 피해+바깥 넉백, 적 Hurtbox.hurt 경로 재사용). `SkillFx.river_cleave/requiem_lantern` 신설(WATER/FOAM/LANTERN 팔레트), `skills.json` 이름·설명·수치 교체, **새 PixelLab 아이콘 2종**(물결/물등, map_object)로 skill_ilseom/hoecheon.png 교체(id 유지). **②몬스터·보스 개편(스토리=수몰 진혼):** 전투 체인(foothills~sacred_altar)이 옛 호환 몹(도깨비 Goblin·구미호 Fox/FoxQueen·호랑이 Tiger/GreatTiger)을 쓰던 것 → **물 원혼으로 전면 교체.** 신규 씬 3종(기존 wraith/reaper 스프라이트 재사용): `DrownedSwarm`(휩쓸린 넋·빠른 무리 scale0.85)·`DrownedHeavy`(물 먹은 넋·둔중 완몹 scale1.3)·`FloodWraith`(수문의 원혼·**대호 대체 최종보스** scale1.7, boss.gd, GreatTiger 사망배선 그대로 유지→Ending). 5개 JSON 적 배치 재구성(잡→완→저승사자 Reaper→대장 ReaperLord/보스). 물 서사 대사 신설(`great_flood_intro`·`temple_reaper`), 옛 대호·구미호·흰여우 인트로 대체. **검증:** 헤드리스 **36개 스위트 전부 그린**(gameplay_chain 5/5·haewon_chain 9/9·skills·combat 무회귀), **llvmpipe 스샷으로 원혼·보스 접지/HUD 아이콘 확인**(원혼 발치 물웅덩이 = 수몰감), 텔레그램 3샷 전송. PixelLab ~4gen. **커밋 `2f7292a` 푸시 완료.** **남은 것(사용자 방향 대기):** "많이 확장"의 **진짜 신규 몬스터 그림**(저승 뱃사공·물아이 넋 등 신규 캐릭터 애니 파이프라인)은 미착수 — PIL 부재로 integrate_enemies.py 못 돌림(대안: Godot 조립 스크립트로 프레임 합성 가능, tileset 크롭과 동일 트릭). + 스토리 굽이 대사·회상 확장. **⚠ 이 세션엔 pip/PIL 없음 → 새 캐릭터 시트 통합은 Godot 조립 경로로 재구현 필요.**
>
> **이전 (2026-07-11 — 텔레그램 3건: 비 어색·대화 자세·맵/배경 전면 개편):** 사용자 텔레그램 3요청 처리. **①비 어색 원인=파티클 텍스처가 여러 줄기 박힌 타일(rain.png)이라 빗방울 하나가 슬래시 뭉치로 보였음 → 코드 단일 물줄기(2×26)+낙하각/기울기 9° 일치+등속으로 교체(`weather.gd`), 상단 회색 비늘 먹구름(clouds.png)도 어색해 제거.** **②대화 시 어색한 자세=구르기(회피) 도중 말 걸면 `_freeze_for_dialogue`가 dodge/attack 상태를 안 풀어 player_visual이 구르기 프레임에 얼어붙던 것 → 대화 시작 시 구르기/공격/스킬돌진 즉시 해제+무적 복구(`player.gd`), 회귀테스트 `test_dialogue_freeze` 4/4.** **③맵·배경 전면 개편(2-1=그림+구조 다시, 수묵·한지 유지·심화):** 배경=PixelLab 수묵 산수(먼산맥 `mtn_far`·솔숲 `mtn_hill`)를 좌우반전 교대(mirror-tile)로 이음매 없이 무한반복+코드 안개띠(`parallax_backdrop.gd` 재작성, 옛 washed 스트립은 폴백). 지면=PixelLab 사이드뷰 타일셋 3종(earth/rock/stone, `assets/tilesets/side/`)의 잔디·이끼·서리 윗면+흙/돌 속을 `stage.gd`가 시트에서 잘라 repeat로 깔음(밋밋한 갈색 띠 대체). +JSON `platforms`(one-way 콜리전)로 높이 지형. 5개 전투 체인 스테이지 레이아웃·톤·소품 재구성. **검증:** 헤드리스 test_gameplay_chain 5/5·test_dialogue_freeze 4/4 그린, **llvmpipe 소프트렌더로 5개 맵 스크린샷 육안 확인**(배경 이음매·발판·바이옴 톤 정상), 텔레그램에 3샷 전송. PixelLab ~40gen. **커밋 2개(`ec28eb7`,`160dcdf`) — 미푸시.** **다음(사용자 대기):** ① 지면 흙 타일이 아직 살짝 '블록'스러움 → 더 뭉갠 수묵으로 갈지 ② 폰 플레이용 GitHub Pages 푸시 여부(확인 후). **미감 최종판단·손맛은 사용자 몫.**
>
> **이전 (2026-07-10 — 조사 가능한 지형지물 시스템 #2〔A〕 + 우물 데모):** 사용자 "계속 진행" 지시로 #2 착수. 조사한 결과 **버튼으로 조사하는 상호작용 시스템이 아예 없었음**(props=정적 그림, pickups=밟으면 줍기, auto_dialogue/cutscene=밟으면 발동뿐). 그래서 #2를 〔A〕엔진 뼈대 + 〔B〕스토리 콘텐츠로 나눠, **리스크 없는 〔A〕를 먼저 구현**: `scripts/world/interactable.gd`(class_name Interactable) — 사거리 진입 시 머리 위 '△' 프롬프트, **interact 키로 대사 재생 + 플래그(flag/once_flag) 세팅**, once 면 1회 후 프롬프트 소멸(세이브 연동), tex 주면 지형지물 그림도 함께 렌더. `stage.gd._build_interactables`가 JSON `interactables` 항목으로 배치(스토리·게임플레이 빌드 양쪽). **스키마:** `{tex,x,y,scale,offset,radius,dialogue,flag,once,once_flag,prompt_offset}`. **데모(새 아트 없이 기존 `well` 텍스처 재사용):** 3굽이 강마을 장식 우물을 조사 가능 지형지물로 승격 — 들여다보면 수면에 옛 여인(윤슬)이 잠깐 어렸다 흩어짐(`well_reflection.json`, 윤슬='물에 부서지는 빛'+엔딩 회수 밑밥, 이름 안 댐). **검증:** 헤드리스 **38/38 그린** — `test_interactable` 신설(프롬프트 표시/숨김·발동 플래그·once_flag 세이브 연동), Haewon3Village 스모크 로드 무에러. **⚠ 조사 손맛·프롬프트 가독성은 PC 눈 확인.** **다음(#2〔B〕 — 사용자 선택 대기):** 어느 굽이에 어떤 지형지물을 넣을지. **이미 있는 텍스처**: well·jangseung·sotdae·shrine_ruin·mul_deung·stone(→새 PixelLab 없이도 여럿 배치 가능). 제안 후보(텔레그램): 우물/빨래터·물동이/서낭당 돌무더기·솟대/부서진 수차/비석·장승/낡은 나룻배. 사용자가 고르면 대사 작성 + 필요 시 PixelLab prop 생성(#4)으로 확장. **요청 5개 중 #1·#3·#5 완료, #2는 시스템+데모까지, #4는 상시 규칙(메모리 [[feedback_rpg_pixellab_art]]).**
>
> **이전 (2026-07-10 — 원혼 성불(진혼) 사망 연출 #3, 5개 요청 중 3개째 완료):** 사용자 "계속 진행" 지시로 #3 착수. **전투=진혼 시그니처를 죽음 연출로 실체화** — 원혼이 스러질 때 차갑게 흩어지던 `death_scatter` 대신 따뜻한 빛으로 천도되는 **`SkillFx.soul_ascend(pos, big)`** 신설: PixelLab 성불 스프라이트(빛 속 **합장한 넋** 실루엣)를 아래→위로 부드럽게 상승·확대·페이드 + 위로 가지런히 풀려나는 금빛 알갱이 + 바닥 금빛 파문(위무의 여운). 원혼의 청색과 대비되는 **따뜻한 금빛(1.0,0.92,0.70)=달램**. 텍스처 없으면 코드 파티클 폴백. `patroller/dummy/boss._on_died` 를 soul_ascend 로 전환(보스 big). **PixelLab**(#4 규칙): `create_1_direction_object(sidescroller 64)` 16후보 중 **[12] 빛 속 합장한 넋** 채택(`assets/sprites/fx/soul_ascend.png`), [4] 연화대 넋은 대안 보관(`soul_ascend_lotus.png`). 재현 스크립트 `tools/pixel/integrate_soul.py`. 소모 20gen(잔여 ~1640). **검증:** 헤드리스 **37/37 그린** — `test_soul_ascend` 신설(연출 노드 스폰·big·런타임 안전), `test_combat` 이 적 사망 경로 통과. 겸사겸사 `test_skill_hud` 의 persisted 키설정 오염 flaky 결함도 수정(bindings_changed 배선 재현+reset_to_default). **⚠ 상승 모션·색감·타이밍 체감은 디스플레이 없는 이 세션에서 캡처 불가 → PC 눈 확인.** **다음(남은 요청):** #2 지형지물 확대+상호작용+스토리 연계(최대 덩어리, PixelLab prop 다수 필요). #4(아트=PixelLab)는 상시 규칙(메모리에도 기록).
>
> **이전 (2026-07-10 — 스킬 아이콘 HUD(#1) + 어두운 우천 환경(#5), 5개 요청 중 2개 마무리):** 사용자 조선 RPG 요청 5건(#1 스킬 아이콘화·키설정 반영·궁극기 키 / #2 지형지물+상호작용+스토리 / #3 혼령 성불 애니 / #4 앞으로 아트 전부 PixelLab / #5 밤낮제거·어둡게·비·흐림) 중, **지난 세션이 커밋 없이 WIP로 남긴 #1·#5를 이 세션에서 마무리·검증·커밋**(`c8fb7d4`). **#1**: `player_hud.gd` 스킬 표시를 이름 텍스트→**아이콘 이미지 슬롯**으로 개편(`_build_skill_slots`, skills.json slot 순, 아이콘/쿨다운숫자/키라벨, 상태를 아이콘 밝기로). 키 라벨=**`InputConfig.binding_text`로 유저 키설정 반영**(고정 1234 폐기)+`bindings_changed` 구독 즉시 갱신. **궁극기 skill_4(귀창 강림)를 REBINDABLE+project.godot 입력맵에 신설**(기존 키세팅에 없었음, player.gd는 이미 skill_4→guichang 배선). PixelLab 아이콘 4종(64x64). **#5**: `TimeManager.STATIC_OVERCAST=true`로 밤낮 순환 제거(시간 정지), `WorldTint`를 침잠한 회청색 `OVERCAST`로 전화면 눌러 밝던 지면 가라앉힘, **`Weather` 오토로드 신설**(화면공간 비 파티클+낮게 흐르는 먹구름 띠, PixelLab rain/clouds+코드 폴백), `NightOnly`는 항상 개방. **검증:** 헤드리스 **36/36 그린** — `test_skill_hud` 신설(슬롯수·아이콘로드·유저키반영 단위검증), `test_polish`의 night_only 케이스를 새 설계(항상 활성)로 갱신. **⚠ 이 세션 환경은 디스플레이/GPU/xvfb 없어 스크린샷 캡처 불가** → **아이콘 렌더·비/구름·어둠 체감은 PC/폰 눈 확인 필요**(로직만 검증). **다음(남은 요청):** #3 혼령 성불 애니(진혼 시 혼 달래주는 연출, PixelLab) → #2 지형지물+상호작용+스토리 연계(최대 덩어리) 순으로. #4(아트=PixelLab)는 상시 규칙.
>
> **이전 (2026-07-10 — 대화 몰입: 대화 중 잠금/HUD 숨김 + 말풍선 세련화):** 사용자 요청 처리. ① **주인공 대화 중 완전 잠금**: `player.gd`가 `Dialogue.is_active()`면 `_freeze_for_dialogue()`로 조기 반환 — 이동·점프·공격·차지·회피·스킬 전부 무시(가로속도 감속, 중력만 유지). 이전엔 적만 상태머신으로 멈추고 주인공은 대화 중에도 걷고 때릴 수 있었음. ② **대화 시 인터페이스 전부 숨김**: `player_hud`(HP/스킬/골드 패널 페이드), `mobile_controls`(터치버튼), `enemy_hp_bar`(적 머리 HP) 를 `Dialogue.dialogue_started/ended` 에 물려 감춤 → 대화 종료 시 복구. ③ **말풍선 유지·세련화(중요 방향 전환)**: 처음엔 하단 큰 한지 프레임 대화창(PixelLab `create_ui_asset` 원목+단청 코너 이미지)으로 갔으나 **사용자가 '촌스럽고 코너 장식이 글자를 가린다'고 피드백 → 되돌림.** 기존 머리 위 말풍선 시스템(SPEECH 말풍선+꼬리 / THOUGHT 말풍선+'…'점 / NARRATION 하단 수묵 자막띠)을 유지하되 **세련되게 리스타일**: 맑은 한지 배경(살짝 투명)·옅은 먹(움버) 2px 테두리·모서리 라운드 11(생각16)·은은한 그림자, **화자 이름 아래 금빛 실선(HSeparator, SPEECH 전용)**, 화자명 단청 적(13pt)·본문 16pt+행간 3. 코드 StyleBox 라 이미지 오버레이 없음=글자 가림 원천 제거. 타이핑 리빌·선택지·dissolve·`_unhandled_input`/`_finish_reveal` 시그니처 보존. **폐기:** `assets/ui/hanji_dialogue_panel.png`(미사용 삭제). **검증:** 헤드리스 35/35 그린(신규 `player_freezes_in_dialogue` 포함), WSLg 렌더 3샷(`shots/dlgr_0` 말+선택지, `shots/dlgrl_1` 혼잣말, `shots/dlgrl_2` 나레이션) 시각 확인. PixelLab 40gen 소모(패널 생성분, 이후 미사용). **남은 것(사용자 미감):** 말풍선 팔레트·글자 크기 최종 체감, 초상(portrait) 삽입 여부는 후속 논의.
>
> **이전(2026-07-01, 워크트리 `worktree-monster-dialogue` — 스토리 부적합 몬스터 교체 + 대화 UI 3종 개편):** 사용자 피드백 4건 처리. ① **몬스터**: 물의 비극(홍수·수몰 원혼) 서사에 안 맞는 **구미호(Fox/FoxQueen) 제거** — PixelLab v3 측면 신규 2종 생성·통합: **수몰 원혼**(`Wraith`, 젖은 머리·물결 속 창백한 넋 — 3굽이 Fox·2굽이 Goblin 대체)·**윤슬의 넋**(`YunseulSpirit`, 못 구한 정인의 처연한 물의 여인 — 6굽이 최종보스 FoxQueen 대체). 시트 빌드·접지 패치(`integrate_enemies.py wraith yunseul`)·새 씬 2개·`enemy_pl.json` 2엔트리·해원 2·3·6굽이 JSON 배선. **옛 「귀창록」 캠페인(ruined_temple 등 비-라이브)은 안 건드림.** ② **대화 글자색 일관화**: 근본원인=`MemoryGlyph._threshold`의 Godot `hash()` 군집 버그(유사 입력에 비슷한 값→대사마다 임계가 0.85 부근에 몰려 어떤 대사는 통째로 흐려지고 어떤 건 멀쩡=‘색 들쭉날쭉’)를 **PCG 아발란치로 수정** + 글자 dissolve를 **진혼 직후 기억소거 6개 비트에만**(JSON `"dissolve": true` 노드, `Dialogue.meta()` 훅) 한정 → 평소 대사는 또렷·일관. ③ **나레이션 몰입**: 화자 없는 상황설명을 말풍선이 아니라 **화면 아래 수묵 자막 띠**(어두운 반투명 + 한지빛, 꼬리·화자명 없음)로 — 누가 말하는 듯 안 보이게. ④ **혼잣말**: 주인공 속내(괄호 대사)를 둥근 thought 말풍선 + **머리 위 ‘…’ 점 3개**로 표시. **검증:** 헤드리스 34/34 그린(`bool()` 비생성자 런타임버그 1건 수정 포함), 시각 6샷(`shots/verify/dlg_narration·speech·thought·dissolve`·`mon_wraith·mon_yunseul`) 확인. PixelLab ~19gen 사용(잔여 ~1708). **남은 것(사용자 미감):** 실제 플레이서 윤슬 보스 스케일(1.4×)·자막 위치(화면 76%)·수몰 원혼 크기 체감.
>
> **이전(2026-07-01, PC — 기본 공격 3타 리워크):** **주인공 기본 공격을 '바라보는 쪽으로 역동 전진하는 3타 + 점점 커지는 범위·데미지·이펙트'로 전면 개편.** ① **PixelLab 캐릭터 모션 3종 재생성**(v3, east) — 1타 `attack1_lunge2`(낮게 앞으로 펜싱 런지 찌름)·2타 `attack2_stride2`(전진 수평 횡베기)·3타 `attack3_leap`(도약 회전 내려찍기). 첫 생성이 '머리 위 와인드업'이라 1·2타는 더 명확한 전진 동작으로 리롤. `integrate_combos.py` UUID 갱신 → strip/manifest 재생성(프레임수 동일 9/9/13 → manifest 불변). ② **PixelLab 이펙트 3종 신규**(create_1_direction_object, sidescroller) — `combo_thrust`(날카로운 창 찌름)·`combo_sweep`(보라+금 초승달 베기)·`combo_burst`(보라+금 다중 소용돌이 폭발), 각 4후보 중 베스트 선택. `assets/sprites/fx/`에 배치·import. ③ **player.gd `_do_combo_attack` 점증** — 데미지 ×1.0/1.35/2.0(전 1.0/1.1/1.6)·히트박스 폭 28/44/70(전 24 고정 → **범위 공격화**, 헤드리스 프로브로 28→44→70·dmg 15→20.3→30 확인)·전방 런지 110/150/240(전 70/70/130)·공격 중 마찰 0.2배로 런지 글라이드(전진 손맛). 콤보 종료 시 히트박스 크기·위치 복원. ④ **skill_fx.gd 콤보 VFX 점증** — `combo_*` 텍스처로 교체 + 스케일·기하 확대, 3타는 3겹 링+16 가시+금빛 불티(가장 화려). **검증:** 헤드리스 34/34 그린(신규 회귀 0), 콤보 프로브(히트박스/데미지 점증), `shots/fx_preview.png`(이펙트 1<2<3)·`shots/combo_pose.png`(전진 포즈+이펙트) 시각 확인. **남은 것(실제 플레이 손맛):** 런지 거리·히트스톱·3타 폭발 크기 체감은 `플레이.bat`. 튜닝 위치 — 데미지/범위/런지=`player.gd _do_combo_attack`, VFX 크기=`skill_fx.gd _spear_thrust/_spear_sweep/_spear_spin`. PixelLab ~75gen 사용(잔여 ~1727).
>
> **추가(같은 날 — 플레이 피드백 4건 반영, 코드만):** 사용자 실제 플레이 후: ① **런지 약함** → 런지 200/280/420(전 110/150/240) + 공격 중 마찰 0.2→0.12배로 더 멀리 글라이드. ② **적 피격음** → 적중 순간 `Sfx.HIT`를 **공격자(플레이어) 측 `_on_hitbox_landed`에서 1회** 재생(적 patroller/dummy/boss `_on_hurt`의 중복 HIT 제거 → 넓어진 3타가 여러 적 때려도 한 번만, 허공 스윙엔 안 남; 궁극기는 히트박스를 안 거치므로 적중 시 1회 직접 재생). ③ **공격 중 이동 금지** → `_attacking` 동안 방향키 입력 무시(direction=0, 런지 임펄스는 유지). ④ **콤보 판정 관대화** → `COMBO_WINDOW` 0.45→**1.0초** + **입력 버퍼링**(공격 중 누르면 현재 타 끝나는 즉시 다음 타로 이어감). **검증:** 헤드리스 34/34 그린, 콤보 버퍼 프로브(공격 중 입력→1타→2타→3타 자동 연결 PASS·런지 임펄스 200 확인). 튜닝점은 `player.gd` 상단 const(COMBO_WINDOW)·`_do_combo_attack`(lunge_amt)·`_physics_process`(공격 중 fric 배율).
>
> **추가(2차 미세조정 — 런지↓·피격음↑):** 사용자 "런지 너무 길다 / 피격음 잘 안 들린다 / 1초 콤보는 OK". ① **런지 축소** 200/280/420→**150/200/300** + 공격 중 마찰 0.12→0.18(글라이드 짧게). ② **피격음 강화** — hit.wav 가 짧은 '톡'이라 RMS 낮아 스윙음에 묻혔음 → `sfx_hit()`를 **묵직한 '퍽'**(저음 430→95Hz 바디 + 중음 펀치 + 절제된 트랜지언트)으로 재설계해 RMS −19.6→**−17.4**(저역이라 스윙 whoosh와 대역 안 겹침), `audio_manager.play_sfx(path, volume_db)` 인자 추가해 HIT를 **+3dB**(피크 −0.2dBFS, 클립 없음) 부각. 기존 대비 체감 ~8dB↑. ③ 1초 콤보 윈도우 유지. **검증:** 헤드리스 34/34 그린, hit/attack RMS 비교(실효 −14.4 ≈ attack −14). 실제 청감·런지 거리는 `플레이.bat`. 튜닝점: hit 음색=`tools/audio/gen_audio.py sfx_hit`(수정 후 `python tools/pixel/...` 아닌 `python tools/audio/gen_audio.py`+`--import`), 부스트=`player.gd` HIT 호출 `volume_db`.
>
> **이전(2026-06-30, PC 검증세션):** **폰세션이 Godot 없이 만든 「해원」 회상 '컷 전환' 시스템 3장 + 환경서사 픽업 + 엔딩 변주를 PC에서 전부 import·테스트·시각 검증 완료.** ① 새 컷씬 PNG(윤슬·젊은길손)·스크립트(cutscene/auto_cutscene)·.tscn 3개를 `--import`로 사이드카(.import/.uid) 생성(project.godot md5 불변). ② **헤드리스 전체 34/34 그린** — 신규 `test_cutscene`(8케이스: 래퍼→JSON→에셋 무결, haewon clear/auto 배선, SceneManager 복귀컨텍스트 왕복, auto_cutscene 노드 생성) 추가. ③ **시각 검증(스크린샷 `shots/verify/cut_*.png`)**: 회상 3장 모두 정상 — 윤슬강가(동틀녘 따뜻, 두 인물 마주보며 접지)·수문회상(inkwash 수묵 흑백, 길손+수문)·홍수의밤(수묵, 길손 단독). **인물 발 접지(런타임 autocrop) 완벽**, 카메라 프레이밍·수묵 톤 의도대로. ④ **픽업 가시성**(헤드리스 프로브): haewon_5 꽃신/가락지/비녀 + haewon_2 통문 전부 charm/scroll Sprite2D **visible 확인**(16px×1.5=24px라 어두운 배경에 작게 묻히나 렌더는 정상 — 눈에 더 띄게 하려면 `pickup.gd` `_setup_icon` scale↑ 여지). ⑤ **엔딩 변주**(프로브): 단서 0/1/3/6개 → 등불 3/4/6/9개 + 마지막 줄 4단계 전부 작동. ⑥ `ResourceLoader.exists(.json)=true` 확인 → cutscene/stage 의 대사 게이트가 .json 을 못 찾아 조용히 스킵하는 버그 **없음**. **남은 것:** 실제 플레이 손맛(컷 왕복 체감·전투 밸런스)·전용 prop PixelLab(수문/물등/keepsake 는 현재 wood_platform/charm placeholder). **다음 갈림길:** Ⓑ 전투 밸런스(플레이 피드백 🔴) / Ⓒ 새 굽이·서사 확장(코드·데이터 🟢).
>
> **직전(폰세션 — 컷 전환 시스템 제작, 이번 PC세션이 검증):** **「해원」 스토리 모드는 코드/데이터로 이미 끝까지 배선돼 있음 — item 20 "남은 배선"은 stale(24·25차에 완료). 이번 WSL세션(2026-06-30)은 Godot 없이 해원 체인 전체를 코드 감사 + 단서 픽업 2개 버그 수정.** 감사 결과 정합: 새 게임→Haewon0Prologue 진입(`main_menu.gd`), 7굽이 .tscn 래퍼·체인·gut·clear_dialogue·inkwash('걷는 회상')·`MemoryLedger.erase_for_gut`·`MemoryGlyph.dissolve`(대사 글자)·`HaewonEnding`(단서 수로 등불·마지막 줄 변주)까지 전부 연결, 깨진 참조 0, autoload(MemoryLedger/InkWash) 등록·reset 호출 정상. **발견·수정한 버그:** 통문(2굽이)·비녀(5굽이) 단서 픽업이 ①icon 없어 화면에 안 보이고 ②`item` 없는 플래그 전용이라 주워도 라벨·소리 피드백 0 → 엔딩 단서 변주가 사실상 죽어 있었음. → JSON에 icon(scroll/charm) 부여 + `pickup.gd`에 플래그 전용 픽업도 FloatingNumber+SFX 발동 추가. **⚠ Godot 없어 헤드리스 테스트·시각 확인 못 함 — PC에서 검증 필요(픽업 렌더·피드백·전체 그린).** **다음 갈림길:** Ⓑ 전투 밸런스(플레이 피드백 🔴) / Ⓒ 새 굽이·서사 확장(코드·데이터 🟢).
>
>
> <details><summary>이전 상태 (PC 런북 A→B→C, 2026-06-30 PC세션)</summary>
> **PC 런북 A→B→C 완료 — 적·보스 PixelLab 아트 + VFX/배경/3타가 드디어 인게임 반영 (2026-06-30, PC세션).** 폰세션(13~29차)이 코드/데이터로 쌓아둔 뒤 줄곧 막혀 있던 PC 숙제를 실행: ① 런북A 스크립트 3개(`integrate_enemies`→적4종 측면 시트+telegraph+씬10개 foot_offset 패치, `gen_background`→배경3겹, `integrate_combos`→attack3 13f) ② 런북B `--import`로 FX 10종·물등 타일셋·신규 스크립트 `.import`/`.uid` 생성 ③ 런북C 헤드리스 **33종 그린**+스크린샷4종(적 새아트·접지·새배경) 검증. `integrate_enemies.py` 의 cp949 콘솔 크래시도 수정. **이제 적·보스가 주인공과 같은 PixelLab 톤으로 통일(우선순위 Ⓐ 완료).** **다음:** (web 반영은 이번 푸시 CI 빌드 후) 우선순위 갈림길 — ~~Ⓐ PixelLab 적·보스 아트 import~~✅완료 / **Ⓑ 전투 밸런스·난이도(실제 플레이 피드백)** / **Ⓒ 새 굽이·서사 확장(코드·데이터)**. 플레이 손맛(VFX 강도·13/18차 조작감)은 사용자 판단.
> </details>
> **이전(24차):** **「해원」 24차 — 스토리 모드 배선 완료: 굽이 클리어→기억 소거→대사 글자 dissolve 가 실제로 게임에서 돈다 (2026-06-29, 24차).** 사용자 텔레그램 "푸시하고 해원 스토리로 가시죠". 23차에서 만든 시스템·데이터를 게임에 연결: ① **부팅 전환** — 메인메뉴 '새로 시작'이 옛 전투체인(Foothills) 대신 **Haewon0Prologue(첫 등)** 로 가고, 새 게임 시 `MemoryLedger.reset()`. ② **per-stage 라우팅**(전역 GAMEPLAY_ONLY 안 건드림) — JSON 에 `"gut"` 있는 스테이지(haewon_*)만 스토리 빌드 경로로 가서 기억 배선을 받음. 옛 전투 스테이지엔 gut 없어 그대로 → **test_gameplay_chain 안 깨짐.** ③ **진혼→소거 연결** — `CombatGate` 에 `opened` 신호 추가, `stage.gd._wire_haewon` 가 굽이 클리어(결계 열림) 시 `MemoryLedger.erase_for_gut(gut)` + `clear_dialogue`(진혼 후 독백) 재생. 전투 없는 굽이(빈 고을)는 진입 0.6s 뒤 소거. ④ **글자 dissolve 적용** — `dialogue_balloon` 이 대사 본문을 `MemoryGlyph.dissolve(text, MemoryLedger.progress())` 로 렌더 → 기억이 지워질수록 대사 글자가 함께 바램(우리 게임 한 방의 실제 비주얼). ⑤ **.tscn 래퍼 7개**(Haewon0~6) + SaveManager AREA_LABELS·메인메뉴 AREA_SCENES 에 해원 지역 등록(이어하기 복귀). ⑥ clear_dialogue 분리 위해 '진혼 후/반전' 대사들을 위치 트리거→결계 연동으로 이동(프롤로그 after·1굽이 윤슬회상·3굽이 이름소멸·4굽이 수문반전·6굽이 윤슬 천도). ⑦ 환경서사 픽업(통문·비녀)은 미정의 아이템 인벤 오류 피해 flag-only 로. ⑧ 테스트 `test_haewon_chain`(체인 7 + 결계→소거 + 무전투 굽이 소거 = 9케이스). **검증: 이 환경 Godot 없음 → JSON 전부 유효성 OK·대사 참조 무결·gut 라우팅 정합 확인. 헤드리스 실행(test_haewon_chain/memory_ledger)·실제 글자 dissolve·물등 접지·동틀녘 톤은 PC 확인 대기.** **다음:** 실제 플레이로 흐름·가독성(dissolve 강도) 튜닝 → '걷는 회상' 수묵 흑백 오버랩 연출 → 보스 직후 정적+수묵 한 폭 컷 → 해원 타이틀/로고 아트(현재 메뉴 로고는 옛 귀창록이라 숨김 상태) → 나머지 굽이 lantern→물등 + 물등 촛불 애니(PixelLab).
> **이전(18차):** **전투 손맛 2차 — 히트스톱 등급화 + 적 경직·넉백 실작동 (2026-06-27, 18차).** 추천 후순위. **발견한 버그**: 적 AI 상태(chase 등)가 매 프레임 `velocity.x` 를 덮어써 **피격 넉백이 사실상 무력화**돼 있었음. ① **히트스톱 등급화**: `screen_fx.gd hit_stop(duration, scale)` 에 freeze 깊이 인자 추가 → `player.gd _on_hitbox_landed` 에서 1타(0.035s·얕게)·2타(0.05s)·3타 마무리(0.09s·딱 멈춤)로 차등. ② **적 경직(hitstun)**: `patroller.gd` 에 `hitstun` 필드(넉백 세기 비례 0.12~0.4s) → `state_machine.gd` 가 hitstun>0 동안 **AI 정지 + 넉백을 마찰(KNOCK_FRICTION 900)로 감쇠시키며 실제로 밀어냄** → 넉백이 처음으로 제대로 먹힘. 경직 중 새 공격 판단도 정지. ③ **피격 squash 저크**: 적 스프라이트를 가로로 납작 눌렀다 복귀(`_hit_jolt`, 기준 scale 저장해 드리프트 방지). 위로 붕 뜨던 팝(-160→-90) 완화. **검증: 이 환경 Godot 없음 → 로직/정적 점검만. 실제 손맛(히트스톱 강약·넉백 거리·경직 길이)은 PC 플레이로 튜닝 — 값은 각 파일 상단/인자에 모임.**
> **이전(17차):** **UI/HUD 한지·먹 스킨 (2026-06-27, 17차).** 추천 후순위 작업(코드만, 재생성 불필요). UI가 폰트만 있고 무스킨이던 것 — **전역 테마 `assets/ui/default_theme.tres` 를 한지/먹 스킨으로 채움**(project.godot 가 이미 참조 → 모든 Control 일괄 적용). 방향: **어두운 먹빛 옻칠 패널 + 단청 금(C9A856)/한지 테두리** — 배경이 어두워 기존 밝은 텍스트가 그대로 읽혀 깨지지 않음. StyleBoxFlat 9종: Panel/PanelContainer/PopupPanel(먹 패널+금테), Button(normal/hover/pressed/focus/disabled, 호버 시 금테 강조·한지 글자), ProgressBar(먹 트랙+금빛 fill→HUD 체력바가 금빛, 저체력 시 modulate 로 붉어짐), HSeparator(금 라인). Label/RichTextLabel 글자색=한지빛. 추가: `enemy_hp_bar.gd` 먹 프레임(금테)+단청 적색, `clear_screen.gd` 타이틀 금빛 먹획. **검증: 이 환경 Godot 없음 → .tres 구조·GDScript 정적 점검만. 실제 렌더·가독성·레이아웃은 PC 확인 대기(전역 테마라 깨지면 UI 전반 영향 → 처음 켤 때 패널/버튼/체력바 먼저 확인).**
> **이전(16차):** **적·보스 PixelLab 측면 아트 교체 (2026-06-27, 16차).** "적·보스가 주인공(PixelLab)과 톤이 안 맞는다" 해소 — 추천 작업. **PixelLab로 4종 측면 캐릭터 생성**(주인공과 동일 파이프라인, east 1방향·코드 flip): **도깨비**(goblin, 80px, humanoid)·**저승사자**(reaper, 92px, humanoid)·**구미호**(fox 시트, 84px, humanoid)·**대호/산군**(tiger, 92px, **quadruped lion**—view는 low top-down이나 east 측면이 자연스러움). 각 **idle/walk/attack/death** (attack·일부 death는 v3 커스텀), **telegraph**는 attack 윈드업에서 무크레딧 파생. **UUID·fps·scenes 매핑** → `tools/pixel/enemy_pl.json`. **통합 스크립트** `tools/pixel/integrate_enemies.py`: PC에서 PixelLab 공개 URL로 east 프레임을 curl·캐시 → 가로 스트립+manifest.json 생성(`assets/sprites/enemies/{goblin,reaper,fox,tiger}/`) + **그 시트를 쓰는 씬들의 foot_offset 자동 패치(scale 보정)**. **보스 자동 반영**: GoblinKing→goblin·FoxQueen→fox·ReaperLord→reaper 시트 확대 재사용. **최종보스 GreatTiger**는 `enemies/boss`→`enemies/tiger` 로 시트 repoint(새 호랑이 아트 적용). **PC 필수 1줄**: `python tools/pixel/integrate_enemies.py` → 시트 갱신+씬 패치 → Godot로 접지·미감 확인(어긋나면 `enemy_pl.json` 의 `coll_half` 조정 후 재실행). **이 세션 검증**: 전 캐릭터 east 아트+애니 프레임 URL 표본 확인(404 없음)·스크립트 `py_compile`. **이 환경엔 Godot·PIL 없음 → 실제 스트립 생성·인게임 미감은 PC 확인 대기.** PixelLab ~20gen 사용(잔여 ~1828, 구독 활성).
> **이전(15차):** **전투 마무리 VFX + 배경 비주얼 개선 (2026-06-27, 15차).** 사용자 요청("추천(VFX 마무리)대로 + 배경/맵 비주얼 개선"). **모두 순수 GDScript(코드)라 PNG/PIL 재생성 불필요 — Godot로 열어 플레이하면 바로 보임.** ① **전투 마무리 VFX**: `skill_fx.gd` 에 4종 신규 — `charge_aura_tick`(차지=기 모으기 오라, 주변 기운이 중심으로 빨려드는 마름모 알갱이+수축 링, 완전차지 시 금빛), `hit_flash`(피격=흰빛 과노출 번쩍→핏빛→원색, 논블로킹 트윈), `death_scatter`(사망=핏빛 대신 창백한 '혼 흩어짐' — 넋 알갱이 위로 흩어짐+솟는 넋 줄기+바닥 먹 번짐 링, 은은한 괴담 톤), `boss_entrance`(보스 첫 교전 시 1회 — 바닥 마기 솟구침+먼지 링+머리 위 마기 무리+화면 흔들림). 배선: `player.gd` 차지 틱마다 오라, `patroller/dummy.gd` 피격 플래시·사망 흩어짐, `boss.gd` 첫 교전 등장 연출·피격 흰빛·사망 흩어짐. `tools/FxPreview.gd` 에 신규 4종 미리보기 추가. ② **배경 개선**: `parallax_backdrop.gd` 전면 업그레이드 — 밋밋한 단색 하늘→**세로 그라데이션**(상단 하늘색 60% 유지→지평선 한지빛), **떠다니는 구름**(코드 드로잉 타원 구름 5송이, 별도 레이어라 산은 안 움직이고 구름만 느리게 흐름), **대기 원근**(먼 산일수록 하늘색으로 흐려 깊이감), **야간 분위기**(하늘 휘도<0.55 인 폐사지·제단·숲 등에 별/반딧불이 깜빡임+떠다님 — 푸른 하늘=별, 녹빛=반딧불). 모두 stage JSON 의 기존 `backdrop.sky/tint` 그대로 활용, 새 export(`aerial/cloud_drift/night_luminance`)로 조절. ③ **배경 아트 자체 풍부화**(추가 작업): `tools/pixel/gen_background.py` 개선 — 최원경 능선 1겹(25% 디더)·구름띠 3겹·먼 하늘 새 떼·근경 납작돌·풀잎 밀도↑·소나무 1그루↑. 기존 팔레트만·주기 W 타일링 유지, `py_compile` 통과. **이 PNG 는 PIL 필요 → PC에서 `python tools/pixel/gen_background.py` 재생성해야 반영**(폰 세션 PIL 없음). **검증: 이 환경엔 Godot·PIL 없음 → 코드 작성·문법/정적 점검만. 실제 렌더/타이밍/미감은 PC 플레이+재생성 후 확인 대기.**
> **이전(14차):** **PixelLab VFX 퀄리티 업 — 3타 모션 재생성 + 페인티드 이펙트 + 몬스터 출혈 (2026-06-27, 14차).** PixelLab **구독 활성(2000 gen 확인, 트라이얼 아님)** → 비싼 오브젝트 VFX도 가능. ① **3타 모션**: 캐릭터 `attack3_spin_v2`(13f, 도약→머리 위→내려찍기 역동 강화)를 v3 재생성. `tools/pixel/integrate_combos.py` 의 attack3 항목을 새 UUID/13f 로 교체 → **PC에서 `python tools/pixel/integrate_combos.py` 실행해야 strip/manifest 반영**(현재 repo는 기존 11f strip 유지, 정합 상태). ② **페인티드 이펙트**: PixelLab 1방향 sidescroller 오브젝트로 VFX 7장 생성→`assets/sprites/fx/`(slash_crescent/swirl/wide, thrust_lance, spark_star/burst, blood_splat/spray). `skill_fx.gd` 에 `_fx_tex`(지연로드·캐시)·`_painted`(스케일업·회전·드리프트·페이드) 헬퍼 추가하고 **콤보1(창격)/2(횡소)/3(소용돌이)·일섬·회천격·적중 임팩트·궁극기(귀창 강림: 바닥 부적 진법 rune_circle + 데몬 폭발 ult_burst)**에 페인티드 '주역' 레이어를 합성(기존 코드 드로잉 디테일은 보조로 유지). **텍스처 미존재 시 자동 폴백**(autoload 안 깨짐). ③ **몬스터 출혈**: `SkillFx.bleed()` 신규(핏방울 스프레이+작은 얼룩, 은은한 괴담 톤 맞춰 절제) → `player.gd` 적중(`_on_hitbox_landed`)·궁극기 루프에 배선. **검증: WSL엔 Godot 없음 → VFX 후보는 PixelLab MCP 프리뷰로 눈 확인(채택만 다운로드), 인게임 합성/타이밍/3타 손맛은 PC 확인 대기. PixelLab ~175 gen 사용(잔여 ~1825).**
> **이전(13차):** **조작 손맛 1차 — 점프/이동 게임필 + 공격 런지 (2026-06-27, 13차).** "푸시 후 조작 손맛 작업" 방향. `scripts/player/player.gd` 에 액션 플랫포머 표준 손맛 7종 추가: ① **코요테 타임**(0.10s — 발판 떠난 직후에도 점프 구제) ② **점프 버퍼**(0.12s — 착지 직전 입력 기억 후 즉시 발동) ③ **가변 점프**(상승 중 점프 떼면 상승속도 0.45배 컷 → 단타/풀점프 구분) ④ **묵직한 낙하**(하강 시 중력 1.45배, 상승 중 점프 뗀 구간 1.9배 → 붕 뜨는 느낌 제거) ⑤ **가속/마찰 이동**(즉시 스냅 폐기 → 지상 2400·공중 1500 가속, 반대방향 전환 시 +마찰로 칼전환, 정점 부근 공중제어 보너스) ⑥ **공격 런지**(콤보 70/3타 130/강타 150 전방 1회성 임펄스 — move_toward 가 자연 감쇠, 누적 버그 회피) ⑦ **착지 흔들림**(낙하속도 360+ 일 때만 가벼운 shake). 되돌리기: 추가한 const/블록만 제거. **검증: 로컬 Godot 헤드리스(`test_player_movement` 신규 2케이스 variable_jump/coyote_jump 포함) + 실제 플레이 손맛은 사용자 확인 대기(이 환경엔 Godot 없음).**
> **이전(12차):** **게임성 우선 — 스토리/대화/NPC 제거, 전투-클리어 전용 모드 (2026-06-18, 12차).** "스토리는 나중, 지금은 몬스터 잡고 클리어만" 방향. ① **`stage.gd`에 `GAMEPLAY_ONLY` 모드 추가**: NPC·자동대사·퀘스트 트리거·스토리 픽업·auto_quest 를 빌드하지 않고, **전투 스테이지 직선 체인**(foothills→forest_deep→ruined_temple(구미호 여왕)→mountain_pass→sacred_altar(대호 보스))을 따라 **전진 차단 결계(적 전멸 시 자동 개방)+전진 출구**만 합성. 마지막 스테이지는 **Clear 화면**으로 연결. (마을/고을 허브 스킵.) **되돌리기: `GAMEPLAY_ONLY=false` 한 줄이면 기존 스토리 데이터 흐름 전부 복구.** ② **MainMenu 간소화**: 제목·로고·컨셉 부제 숨김, '새로 시작'→프롤로그 대신 곧장 첫 전투 스테이지, '이어하기'·설정 유지. ③ **`Clear.tscn`(클리어 화면)** 신규 — 스토리 엔딩 대체, 다시 도전/처음으로. ④ 유지 시스템(사용자 선택): 인벤토리·장비, 세이브/로드, HUD 스킬바·쿨다운 + 전투/스킬/드롭. 제거(코드는 보존, 미사용): 대화·NPC·퀘스트·상점·프롤로그·엔딩·타이틀 컨셉. **검증: 헤드리스 31/31 그린(신규 test_gameplay_chain 포함) + 메뉴·스테이지·클리어 스크린샷.** (전투 손맛·밸런스 polish 는 이제부터 실제 플레이로.)
> **이전(11차):** **주인공 공격 모션 역동화 + 이펙트 강화 (2026-06-18, 11차).** "기본 공격 모션·이펙트가 아쉽다" 피드백 반영. ① **콤보 1·2·3타를 각기 다른 역동적 PixelLab v3 애니로 재생성**: attack(폭발 전진 찌름 9f)·attack2(횡쓸기 9f)·attack3(도약 회전 내려찍기 11f)·charge(기 모으기 7f 루프) — 기존엔 셋이 같은 찌름 재사용이던 것을 분리. ② **이펙트 강화**: `skill_fx.gd` 전면 업그레이드(찌름=창대+보랏빛 마기+속도선+창끝 섬광, 횡쓸기=3겹 초승달+꽃잎 스파크, 회전=2겹 링+12방 마기 가시+지면 먼지, 일섬/회천격/임팩트/궁극기 다층화) + **캐릭터 잔상(afterimage) 트레일** 신규(콤보·차지·일섬·회천격·궁극기·회피에서 현재 프레임 복제 색조 잔상 → 새 아트가 모션에 살아남). ③ **크레딧 현실 메모**: PixelLab '오브젝트(VFX 스프라이트시트)'는 1개당 20~40 generations이라 트라이얼(잔여 26)로 비현실적 → 이펙트는 코드+잔상으로, 모션은 PixelLab(애니 1~2gen)으로. **트라이얼 14/40 소모(26 잔여).** 검증: **헤드리스 30/30 그린 + FX 미리보기·콤보 포즈 스크린샷**. (손맛·타이밍 최종 미감은 실제 플레이 판단.)
> **이전(10차):** **PixelLab MCP 주인공 아트 교체 완료 (2026-06-18, 10차).** 코드생성 미감 한계로 외부 AI 전환 — PixelLab MCP 로 **측면(side-view) 조선 마창 무사**("Joseon Spearman", ID `f5fd2830…`, 4dir/side/92px) 생성. **먼저 캐릭터 1개로 옆모습(east 우향 프로필+창)·크레딧 확인** 후 진행. **생성 6종(east 1방향, 좌향은 코드 flip_h):** idle(breathing-idle)·walk(walking-6-frames)·jump(jumping-1)·hurt(taking-punch)·death(falling-back-death) 템플릿 + attack(v3 창 찌름). **파생(무크레딧) 5종:** run(=walk 가속)·attack2/attack3(=attack 재사용, 콤보 차별은 SkillFx 유지)·charge(=attack 윈드업 홀드)·dodge(=jump 웅크림/착지 발췌). 통합 스크립트 `tools/pixel/integrate_pixellab.py`(zip→92×92 스트립+manifest, foot_offset −28 산출=콜리전 바닥 +16 정렬), `player_visual.gd` foot_offset −21→−28. **트라이얼 크레딧 7/40 소모(33 잔여).** 검증: **헤드리스 30/30 그린 + 인게임 마을 스크린샷(발 접지·우향 확인)**. (최종 미감·로브 색감(거의 흑색, 수묵톤보다 어두움)은 실제 플레이 판단.)
> **이전(9차):** **주인공 외모·창 휘두름 개선 (2026-06-17, 9차).** "못생겼다 / 창이 작아진다" 피드백 반영. ① **창 작아짐 해결**: 프레임 폭 32→48px 확장(창 잘림 방지) + **각도 기반 일정 길이 창**(spear_angle/SPEAR_LEN=24) — 찌르기는 창을 평행이동(물미 후퇴), 횡쓸기·회전은 각도만 회전+sweep 잔상. 모든 공격 프레임에서 창 길이 불변. ② **얼굴 리디자인**: 잘려나간 코·눈썹 덩어리·뺨 청색 얼룩 정리 → 또렷한 almond 눈+얇은 눈썹+매끈한 콧대+좌상 광원 음영, 갓끈은 얼굴 밖 드리움. 갓(黑笠) 모정/챙 음영 정돈, 목 보강. 몸은 CX=24 중앙 정렬(전 부품 CX 상대좌표 → 자동 재배치). **헤드리스 30종 그린, 전 애니 스트립+인게임 스크린샷 확인.** (최종 미감은 실제 플레이 판단.)
> **이전(8차):** **주인공 모션 전면 고도화 (2026-06-17, 8차).** "모션이 조잡하다"는 피드백 반영 — 전 10종 애니메이션을 프레임 증량 + 애니메이션 원칙(예비동작·따라가기·무게이동·2차 모션)으로 재작업. 보행 6→8(정식 사이클: 접지/통과 무게이동, 발끝 들림, 자락·팔·창 카운터 스윙, 진행방향 lean, 갓 1박자 지연), 대기 4→6(들숨/날숨+창 홍술 살랑임), 도약 2→3(상승/정점/하강+velocity 매핑), 찌르기 3→5·횡쓸기 3→5·회전내려찍기 4→6(예비→타격→여운→회수, 런지·풍압·궤적 잔상, 3타 홍 궤적), 차지 2→3·구르기 3→4·피격 2→3. **헤드리스 30종 그린, 스트립·인게임 스크린샷 확인.** (손맛·타이밍 최종 미감은 실제 플레이 판단.)
> **이전(7차):** **「귀창록」(鬼槍錄) 리브랜딩 — 창 마검사 컨셉 (2026-06-17, 7차).** 제목을 호환기담→**귀창록**으로 바꾸고 주인공을 **마창(魔槍)을 든 자**로 재정립. ① **창(spear) 컨셉**: 주인공 스프라이트 무기를 환도→창으로 전면 재생성(자루+강철 창날+홍 술), 기본 콤보 1·2·3타를 **직선 찌름 / 넓은 횡쓸기 / 회전 내려찍기**로 모션·이펙트 차별화(SkillFx.combo), **3타 1.6배 추가 피해+홍 궤적**. ② **궁극기 4번 '귀창 강림'**(쿨 40s, 3.5배 광역, 화면 섬광+충격파 링+12방 영혼창 — SkillFx.ultimate). ③ **NPC 상호작용 제거**: 스토리 진행 따라 대화 자동 재생(auto_dialogue 트리거), **대화 중 적 전원 정지**(state_machine/patroller/boss/spirit_orb 동결). ④ **시간 경과 자연 회복**(HealthComponent.regen, 피격 6s 후 초당 2.5). ⑤ **보스 다양화**: 호랑이에 묶이지 않게 **구미호 여왕(폐사지)·저승 군주(관아)** 중간보스 2종 추가 + 등장 자동 대사. ⑥ **세계관 통합**: 호환(虎患)→**호귀(虎鬼, 한 서린 범의 넋)**, 최종보스 대호→**호귀로 화한 산군(山君)**으로 재해석(에셋 유지). 프롤로그/에필로그/크레딧 마창·귀(鬼) 테마 재작성. **헤드리스 테스트 30종 그린, 전 스테이지 스크린샷 확인.** (전투 손맛·밸런스·미감은 실제 플레이 판단 영역.)
> **이전(6차):** **자율 고도화 — 전투 깊이·공정성·UX·경제·정합 (2026-06-12, 6차).** 잡몹이 실제로 공격(근접/저승사자 원거리 투사체)하고, 피격 무적·드롭·레벨업 회복·저체력 경고로 전투가 양방향이며 공정. 대화 선택지·메뉴 키보드 조작, 전투 게이트 '남은 적' 안내, 보스 돌진 '!' 경고, 산기슭 행상(엽전 사용처), 클리어 구간 재진입 무결성. **헤드리스 테스트 26종 그린, 월드 연결성 OK.** (전투 손맛·밸런스·음악 청감은 실제 플레이 판단 영역.)
> **이전(5차):** 1스테이지 튜토리얼화 + 컨트롤·대화·음악·배경 개선. ① 기본 키 재배치: **공격 Ctrl, 점프 Alt, 상호작용/넘기기 Space**(+보조 X/W/E). ② 대화 교착 버그 해결(숨겨진 선택지로 advance 막히던 문제). ③ **1막을 퀘스트 받기 없는 튜토리얼 가닥으로 재구성**: 프롤로그→마을 어귀(이동·공격 튜토)→산기슭(점프·콤보)→깊은 숲(스킬)→절벽 보스. 각 구간은 **적을 모두 베야 결계(전투 게이트)가 풀려** 다음으로, 보스 처치 시 **곧바로 2막(고을)로 전환**. 퀘스트는 스테이지 진입 시 자동 시작(받으러 안 다님), 쓸데없는 사이드 퀘스트는 1막에서 비노출. ④ **BGM 전곡 재작곡**(드론→가야금 가락+굿거리/세마치/자진모리 장단). ⑤ 배경 한국화(보름달+동양화 구름띠). 전체 흐름 12 스테이지(튜토 3 + 산신당/절벽 + 고을 3 + 신산 3) 3막. 헤드리스 테스트 17종 그린, 월드 연결성 검증 OK.
> **이전(4차):** 대형 캠페인 확장 — 10 스테이지 3막. 데이터 기반 스테이지 빌더(`scripts/world/stage.gd` + `assets/stages/*.json`) 도입으로 스테이지 양산. 전체 동선: 타이틀→프롤로그→**[1막 산골]** 마을→들판→숲→산신당 터→절벽(호환 두령)→**[2막 고을]** 저잣거리→관아 동헌→폐사지→**[3막 신산]** 산길→제단(**최종보스 大虎/백호**)→엔딩. 스토리도 한 마을 비극 → 신산 산신의 진노가 고을·신산으로 번지는 大호환 서사로 확장(주인공=무당 단의 아들, 칼이 아닌 진언으로 산을 잠재움). 메인 퀘스트 2개(main_tiger_lord 5단계 + main_great_tiger 6단계). **월드 그래프 17개 출구 전부 연결 검증(tools/check_connectivity.py), 헤드리스 테스트 16종 그린, 완주 테스트 대호 캠페인 포함 4/4.**
> **이전(3차):** 버그·스토리·이펙트·콘텐츠 (2026-06-12). ① 점프 추락 버그 해결(지면 충돌 폭 정합 + 낙사 안전망). ② 스토리 부자(父子) 인연으로 전면 재설계(주인공=무당 단의 아들, 두령=한이 된 산군). ③ 스킬 이펙트 추가(일섬 참격·회천격 회전·호신부 부적 오라·적중 스파크, SkillFx). ④ 신규 스테이지 '산신당 터'(숲→산신당 터→보스, 구미호 회상·미니보스 팩·보물 궤짝). 전체 동선 타이틀→프롤로그→마을→들판→숲→산신당 터→보스→귀환→엔딩. **헤드리스 테스트 16종 그린.**
> **이전(2차):** 처음부터 끝까지 완주 가능한 게임 (2026-06-12, 2차). 타이틀 → **오프닝 프롤로그(호환 괴담 도입 5문단)** → 마을(어르신 퀘스트 수락) → 들판(약초·서찰 픽업) → 숲(적 5종) → 보스 아레나(두령 일갈→전투) → 마을 귀환(어금니 보고→어르신 고백) → 엔딩(사이드 완료수별 3분기) 까지 한 줄기로 연결. **엔드투엔드 통합 테스트(test_playthrough)로 메인 퀘스트 5단계 완주·부적→호신부 해금·엔딩 분기를 검증.** 이어하기는 저장 지역으로 복귀, 보스 귀환 동선 정합, 픽업은 아이콘+부유 연출. 헤드리스 테스트 15종 그린. 전투 밸런스(평타 ~7초로 보스 처치, 회피·호신부 여유) 수치 확인.
> **이전(1차) 반영분:** **완결 게임 + 1차 사용자 피드백 반영 완료 (2026-06-12).** Godot 4.6.3 / 「호환기담」 / 자체 제작 픽셀아트·사운드 전량. 타이틀→마을→들판→숲→보스→엔딩 한 바퀴 완결에 더해: **주인공 리디자인**(청 전복+홍 전대+금장 환도 — '밋밋함' 피드백 반영), **스킬 3종**(일섬 Lv3 / 회천격 Lv5 / 호신부=부적 퀘스트 해금, HUD 쿨다운 표시), **표준 키세팅**(X공격/Z·Space점프/C회피/1·2·3스킬) + **키 리바인딩 UI**(설정 메뉴, user://input.cfg 저장), **비장한 BGM 전면 재합성**(계면조 하강 선율+북·대금+5도 드론, 타이틀 서곡 신규), **스토리 3막 괴담**(산신당 방화의 업 — 어르신 고백·보스 일갈·상인 복선·**엔딩 3분기**). **헤드리스 테스트 15종 그린** (신규 test_skills 5건 포함). 모든 에셋은 `tools/pixel/`·`tools/audio/` 재현 스크립트 산출물.
> **남은 것(미감·실기기):** ① 손맛/애니 타이밍/전투 밸런스 — PC 실제 플레이 확인 ② **BGM 비장함이 귀에 맞는지** — 합성 구조상 비장하게 설계했으나 청감 판단은 사용자 몫 ③ 폰 미리보기(웹 빌드 호스팅 — itch.io 권장) ④ 픽업 아이템 스프라이트화·말풍선 한지 스킨(폴리시 여지).

## 바로 다음 할 일 (Next Action)
> **21. [최우선 — 사용자 결정 대기] `development`→`main` 병합·푸시.** 3커밋(전투체인 전환+스코어어택+통과판정)이 헤드리스 검증까지 끝난 채 `development`에서 잠자고 있고, `main`(=CI/GitHub Pages 배포 대상)엔 반영이 안 됨. 병합·푸시하면: 헤드리스 테스트 CI가 새 테스트(test_pivot·test_score·test_passthrough) 포함 자동 재검증 + web export가 GitHub Pages에 배포되어 폰으로 새 전투체인/스코어어택이 바로 보임. **푸시는 사용자 확인 필요**([[feedback_git_push]]) — 승인 시 바로 진행 가능(로컬엔 이미 커밋 완료 상태).
> 22. 병합 후: 사용자가 웹빌드로 실제 스코어어택 손맛 확인(적 통과판정 위화감 없는지, 점수/시간 표시 가독성, 게임오버·클리어 요약 화면) → 피드백 받아 밸런스(적 배치·HP·점수 배율) 조정. PC 세션(Godot 있음)에서 헤드리스 재검증 병행.
> 23. dormant 처리된 스토리 시스템(dialogue/quests/memory/cutscene/haewon_ending 등)을 완전히 정리(삭제)할지, 나중에 별도 모드로 되살릴지는 미결 — 당장 급하지 않음, 사용자 방향 확인 시 착수.
>
> 1~7. ~~Phase 0~3 + 콘텐츠/2차/3차 + 테스트 그린 + 4.6.3 마이그레이션 + 에셋 전량 + 게임 통합~~ ✅
> 8. ~~**1차 피드백 반영: 주인공 리디자인 / 스킬+키세팅+리바인딩 / 비장 BGM / 스토리(산신당 축)+엔딩 분기**~~ ✅ (2026-06-12)
> 9. **사용자: 실제 플레이 확인** — `플레이.bat` 더블클릭
>    - 새 키: X공격·Z점프·C회피·1/2/3스킬 (설정→키 설정에서 변경 가능)
>    - 확인 포인트: BGM 톤이 비장한지 / 스킬 손맛(레벨 3·5 해금, 호신부는 아낙 부적 퀘스트) / 보스전 입장 일갈 / 엔딩 분기(사이드 완료 수)
> 10. ~~**6차 자율 고도화: 전투 양방향화·UX·경제·정합 (26종 그린)**~~ ✅ (2026-06-12)
> 11. ~~**7차 「귀창록」 리브랜딩: 창 마검사(1·2·3타 차별+3타 추가피해)·궁극기 4번·자동 대화/적 동결·자연 회복·보스 다양화(구미호 여왕/저승 군주)·호귀/산군 세계관 (30종 그린)**~~ ✅ (2026-06-17)
> 12. **사용자: 실제 플레이 확인** — `플레이.bat` (공격 Ctrl·점프 Alt·상호작용/넘기기 Space·회피 Shift·스킬 1/2/3·**궁극기 4**). 확인 포인트: 창 콤보 1·2·3타 손맛/이펙트, 궁극기 화려함, 자동 대화 흐름, 보스 다양성.
> 13. (다음 후보) 신규 스테이지 추가 / 창 전용 장비 아이템 / 웹 빌드 itch.io 업로드 자동화(폰 플레이) / 말풍선·패널 한지 스킨 / 전투 밸런스 패스
> 14. ~~**주인공 아트 외부 교체 (PixelLab MCP)** — 측면 마창 무사 생성 6종 + 파생 5종, 통합·발정렬, 30/30 그린·인게임 확인 (트라이얼 7/40)~~ ✅ (2026-06-18)
> 15. **사용자: 실제 플레이로 새 주인공 손맛/미감 확인** — `플레이.bat`. 확인 포인트: 보행/찌르기/점프 타이밍, 로브 색(거의 흑색이 톤에 맞는지), 콤보 2·3타가 같은 찌름+SkillFx 로 충분히 구분되는지. 불만 시 후속 후보: ⓐ 콤보 2타(횡쓸기)·3타(회전)를 PixelLab v3 로 별도 생성(잔여 33gen), ⓑ 로브 색 `create_character_state` 로 밝은 수묵톤 변형, ⓒ 4·궁극기 전용 포즈.
> 16. ~~**11차 공격 모션 역동화 + 이펙트/잔상 강화 (PixelLab v3 콤보 4종 + skill_fx 업그레이드)**~~ ✅ (2026-06-18)
> 17. ~~**12차 게임성 우선: 스토리/대화/NPC 제거, 전투-클리어 전용(GAMEPLAY_ONLY) + Clear 화면 (31/31 그린)**~~ ✅ (2026-06-18)
> 18. **[현재 단계] 전투 게임성 polish** — 실제 플레이로 손맛/밸런스 다듬기. 후보: 적 AI·체력·드롭 밸런스, 콤보 타이밍/히트스톱, 스테이지 적 배치·난이도 곡선, 결계/클리어 피드백. 스토리는 그 다음(되살리려면 `stage.gd`의 `GAMEPLAY_ONLY=false`).
>    - ~~13차: 조작 손맛 1차(코요테/점프버퍼/가변점프/묵직낙하/가속이동/공격런지/착지흔들림)~~ ✅ (2026-06-27, **사용자 PC 검증 대기**)
>    - **사용자: `플레이.bat` 로 손맛 확인** — 점프가 단타/풀점프로 갈리는지, 이동에 발구름·관성이 생겼는지(너무 미끄러우면 FRICTION↑/AIR_FRICTION↑), 공격 시 앞으로 치고 나가는 느낌이 과하지/약한지(런지 값 조정), 낙하가 묵직한지. 수치는 `player.gd` 상단 "조작 손맛 튜닝" const 한곳에 모음.
>    - ~~14차: PixelLab VFX 퀄업(3타 모션 v2 + 페인티드 슬래시/창격/스파크 이펙트 + 출혈)~~ ✅ (2026-06-27, **PC 반영·검증 대기**)
>    - **사용자 PC 필수 단계**: ① Godot로 프로젝트 한 번 열어 `assets/sprites/fx/*.png` import(.import 생성) ② `python tools/pixel/integrate_combos.py` 실행 → 3타 strip(attack3.png 13f)+manifest 갱신 ③ `플레이.bat` 로 이펙트/출혈/3타 확인. 마음에 안 들면: 슬래시/출혈 크기·수명은 `skill_fx.gd` `_painted(...)` 호출 인자, 3타 모션은 PixelLab `animate_character` v3 재롤(잔여 gen 충분).
> 20. **✅ [완료] 「해원」 스토리 모드 배선 — (a)~(d) 24·25차에 이미 완료(2026-06-30 WSL세션 감사로 확인).** 아래 "남은 배선"은 **stale**: (a) story 스테이지는 `gut` 보유 시 `GAMEPLAY_ONLY`와 무관하게 스토리 경로로 감(stage.gd) + 7굽이 .tscn 래퍼·체인 전부 존재, 새 게임이 Haewon0Prologue로 진입 ✅ / (b) gate.opened→`erase_for_gut`+clear_dialogue ✅ / (c) 대사 글자 `MemoryGlyph.dissolve` ✅(dialogue_balloon.gd) / (d) '걷는 회상' inkwash ✅(1·4·6굽이 clear_inkwash). **남은 것은 (e)뿐:** 물등/수문/비녀 등 **신규 prop PixelLab 생성**(PC·아트 작업) — 현재는 단서 픽업이 기존 scroll/charm 아이콘 재사용(2026-06-30 WSL세션, 보이게·피드백 수정). 아래는 원본 기록(참고용):
>     <details><summary>원본 item 20 (구축 중 시점)</summary>**[당시] 「해원」 제작 — 설계 잠금 완료, 시스템·데이터 구축 중.** 코어/주인공(길손)/비트시트는 `docs/STORY_BIBLE.md`에 잠금. ~~① 무대 그릇 확정(물등+동틀녘+빈집)~~ ✅(22차) → ~~② 6굽이 스테이지 JSON 스켈레톤~~ ✅(23차, `assets/stages/haewon_*.json`) → ~~③ '기억 지워짐' 시스템 프로토타입~~ ✅(23차, `MemoryLedger`+`MemoryGlyph`+테스트) → ~~⑤ 1막 대사 데이터~~ ✅(23차, `assets/dialogue/haewon/*.json` 14편). **남은 배선(다음 세션):** (a) `stage.gd` `GAMEPLAY_ONLY=false` 로 스토리 모드 복원 + 새 haewon 스테이지용 .tscn 래퍼 7개 생성(루트명=저장키, stage_id 지정) + CHAIN/CHAIN_TSCN 을 haewon 체인으로 (b) 굽이 클리어(전투게이트 열림)→`MemoryLedger.erase_for_gut(stage 의 gut)` 연결 + 진혼 후 dissolve 대사 트리거 (c) 대사/HUD 글자에 `MemoryGlyph.dissolve(...)` 적용(우리 게임 한 방의 실제 비주얼) (d) ④ '걷는 회상' 1장면(수묵 흑백 오버랩, 조작 유지 — recall_yunseul.json 연결) (e) 물등/수문/비녀 등 신규 prop **PixelLab** 생성. (b)부터가 "실제로 게임에서 글자가 지워지는" 첫 체감 — 우선순위 높음.</details>
> 19. **[보존] 주인공 아트 외부 교체 기록** — 코드 생성 미감 한계로 외부 AI 아트 선택.
>     현재: Leonardo 생성 idle 1장을 `assets/sprites/protagonist_custom/`에 통합(배경제거·다운스케일·발정렬),
>     단일 이미지 퍼펫 변형으로 11개 동작 임시 생성(`player_visual`이 custom 시트 우선 사용, 코드생성 폴백).
>     사용자가 품질 불만 → **PixelLab MCP**(https://api.pixellab.ai/mcp, `claude mcp list`에 connected) 결제·연결 완료.
>     **다음 세션 할 일:** PixelLab MCP 도구로 조선 마창 무사 **측면(side-view)** 캐릭터 + walk/attack/hurt/death/jump
>     애니 생성(먼저 1개로 옆모습 지원·크레딧 확인) → `protagonist_custom/`에 넣고 슬라이싱·매니페스트·배선·발정렬
>     → 2타·3타·차지·구르기 파생 → 헤드리스 30종 + 인게임 스크린샷 검증 → 커밋.
>     (MCP 도구는 세션 시작 때만 로드되므로 앱 재시작·새 대화 필요.)

## ▶ 지금 PC에서 시작점 (2026-06-30 — 컷 전환 시스템 + 픽업 + 엔딩 PC 검증 완료)
> **★ 이번 PC세션: 폰세션이 쌓아둔 「해원」 회상 컷 전환·픽업·엔딩을 전부 검증·커밋함.** 더 이상 막힌 PC 숙제 없음.
> - ~~컷씬 PNG/.tscn/.gd import(.import·.uid 생성)~~ ✅ (사이드카 커밋, project.godot md5 불변).
> - ~~헤드리스 전체 그린~~ ✅ **34/34**(신규 `test_cutscene` 8케이스 포함; test_pickup_fx/test_drops/test_haewon_chain 정상).
> - ~~회상 3장 시각 확인~~ ✅ `shots/verify/cut_recall_yunseul·cut_watergate·cut_flood_night.png` — 인물 접지·프레이밍·inkwash 정상.
> - ~~단서 픽업 보이는지~~ ✅ 헤드리스 프로브로 charm/scroll Sprite2D visible 확인(렌더 정상, 다만 24px라 작음 — scale↑ 여지).
> - ~~엔딩 등불·마지막 줄 변주~~ ✅ 프로브로 단서 0/1/3/6→등불 3/4/6/9 + 4단계 줄 확인.
> - **유일하게 코드론 못 보는 것 = 실제 플레이 손맛**: 1굽이/4굽이 클리어 시 회상 맵으로 컷 전환됐다가 전투로 복귀하는 '왕복 체감'·복귀 위치(from_recall)·완급은 `플레이.bat`로 한 번 플레이해 눈으로. 어긋나면 인물 위치=cutscene JSON `figures`, 카메라=`camera.x/y`, 복귀 위치=stage JSON `from_recall` entry x.
>
> **★ 직전 PC 숙제(13~16차 PNG import) 끝남 (2026-06-30).** 적·보스 PixelLab 아트·VFX·배경·3타가 인게임 반영되어 푸시됨 — 이번 CI 빌드가 끝나면 web(Pages)에도 반영. `git pull` 후:
> 1. **(선택) 눈으로 확인** — 빌드 후 https://ksjmgrkks.github.io/joseon-rpg/ 또는 로컬 `플레이.bat`: 적·보스 새 측면 아트(도깨비·저승사자·구미호·백호), 전투 VFX, 대화 말풍선, '해원' 타이틀, 6굽이 BGM. 로컬 검증 스크린샷은 `shots/verify/`.
> 2. ~~**PC 숙제 런북 A→B→C**~~ ✅ 완료 (적·보스 아트 import·접지, 헤드리스 33/33, 배경/UI 스크린샷 확인).
> 3. **그다음 우선순위(사용자와 갈림길):** ~~Ⓐ PixelLab 적·보스 아트 교체~~ ✅완료 / **Ⓑ 전투 밸런스·난이도**(아래 #4, 실제 플레이 "쉽다/어렵다" 피드백 필요 — 🔴) / **Ⓒ 새 굽이·서사 확장**(코드·데이터, 폰·PC 양쪽 가능 — 🟢). Ⓑ는 사용자 플레이 피드백 대기, Ⓒ는 지시만 있으면 바로 착수 가능.
> 4. **말풍선 미세조정 후보**(폰 OK였으나 PC서 더 볼 것): `dialogue_balloon.gd` `HEAD_MARGIN`(현재 10, +면 풍선 내려옴), 타이핑 속도 `REVEAL_CPS`(34), 한지 StyleBox 색.

## 다음 작업 후보 (2026-06-27 기준 — 폰세션 후 상태 갱신)
> 폰(WSL) 세션 가능 여부 표기: 🟢 폰서 바로 / 🟡 폰서 생성·코드+PC서 반영 / 🔴 PC 플레이 피드백 필요.
> 1. ~~🟢 **VFX 마무리**~~ ✅ (27차 — 보스 분노 격발 `boss_enrage`+완전차지 `charge_ready`+금빛 일섬. 그 외 차지오라·피격플래시·사망혼·보스등장은 13~16차에 이미 있었음.)
> 2. ~~🟡 **적·보스 아트 외부 교체**~~ ✅ (2026-06-30 — 도깨비·저승사자·구미호·호랑이 PixelLab 측면 시트 생성·import·접지 패치, 보스 4종 scale 재사용. 헤드리스 33/33 + 스크린샷 확인.)
> 3. ~~🟡 **전투 손맛 2차**~~ ✅ (히트스톱·넉백·피격경직/squash — 18차 코드 완료, PC 체감 확인은 런북 C.)
> 4. 🔴 **전투 밸런스/난이도 곡선** — 적 HP·드롭률·스테이지 적 배치. 실제 플레이 피드백("쉽다/어렵다") 받은 뒤 조정.
> 5. 🟡 **웹 빌드 → itch.io 업로드** — *(참고: GitHub Pages 로 이미 폰 플레이 라이브. itch 는 선택.)*
> 6. ~~🟢 **UI/HUD 한지·먹 스킨 폴리시**~~ ✅ (전역 테마 `assets/ui/default_theme.tres` 적용 확인 — 별도 작업 불필요. + 28차 대화 말풍선 한지 StyleBox.)
> 7. 🟢 **새 스테이지/적 콘텐츠(=Ⓒ)** — `stage.gd` 데이터(JSON) 기반 양산. 단 라이브 경로는 해원 6굽이이므로 새 굽이/서사로 붙여야 플레이어가 봄.
> 8. (신규) 🟢 **해원 서사·연출 폴리시** — 대사 추가, 환경서사 픽업, 걷는 회상 확장 등. 코드/데이터라 폰·PC 양쪽 가능.

## 🖥️ PC에서 해야 할 일 — "HANDOFF 읽고 바로 실행" 런북
> 폰/WSL 세션엔 **Godot·PIL 이 없어** 코드·데이터만 커밋해 둠. 아래 **A→B→C 를 순서대로** 하면 폰에서 한 작업(13~16차)이 전부 인게임에 반영된다. 먼저 `git pull`. 끝낸 항목은 `[x]`.
>
> ### A. 명령 실행 (프로젝트 루트에서, 각 1회)
> ```bash
> git pull
> python tools/pixel/integrate_enemies.py   # 16차 적·보스 아트: 스트립+manifest 생성 + 씬 foot_offset 자동 패치(scale 보정)
> python tools/pixel/gen_background.py       # 15차 배경 아트: bg_far/mid/near PNG 갱신(최원경 능선·구름 3겹·새 떼·돌·풀)
> python tools/pixel/integrate_combos.py     # 14차 3타 모션: attack3.png 13프레임+manifest 갱신
> ```
> ### B. Godot 에디터로 1회 열기 (import 생성)
> - [x] **(2026-06-30 완료)** `godot --headless --import` 1회로 새 PNG 자동 import: `assets/sprites/fx/*.png`(VFX 10종) + `assets/sprites/enemies/{goblin,reaper,fox,tiger}/*.png`(새 적) + 물등 타일셋 → `.import`/`.uid` 사이드카 커밋됨. project.godot md5 불변 확인.
> ### C. 검증 (`플레이.bat` + 헤드리스)
> - [x] **(2026-06-30) 헤드리스 그린**: 전체 스위트 **33종 33/33 그린(100+ 케이스)** — 적 시트·씬 foot_offset 패치·combos manifest 변경이 아무것도 안 깸.
> - [x] **(2026-06-30) 16차 적·보스**: 도깨비·저승사자·구미호여왕·백호(최종보스) 새 측면 아트 + **접지** 스크린샷 확인(`shots/verify/`). 패치된 foot_offset: goblin −15·reaper −22·fox −19·tiger −5(보스는 scale 보정). 어긋나면 `enemy_pl.json` `coll_half` ±조정 후 `integrate_enemies.py` 재실행.
> - [ ] **14차 VFX(플레이 손맛)**: 콤보1/2/3·일섬·회천격·적중·출혈·궁극기 + 차지오라·피격흰빛·혼흩어짐·보스등장. **PNG는 import 완료(이제 렌더됨)** — 합성 강도/타이밍 미감만 실제 전투로 판단, 과하면 `skill_fx.gd` 인자 조정.
> - [x] **(2026-06-30) 15차 배경**: 재생성된 bg_far/mid/near — 그라데이션 하늘·원경 산 대기원근·별·새 떼·소나무 스크린샷 확인(`shots/verify/`). 톤·속도 미세조정은 `parallax_backdrop.gd` export(`aerial`/`cloud_drift`/`night_luminance`)·stage JSON `backdrop.sky/tint`.
> - [ ] **13차 손맛**: 점프 단타/풀점프·관성·공격 런지·묵직 낙하 (`player.gd` 상단 "조작 손맛 튜닝" const). *(코드는 반영됨 — 실제 플레이 손맛만 판단)*
> - [x] **(2026-06-30) 17차 UI 한지/먹 스킨**: 스크린샷서 HUD 금빛 HP바·먹빛 패널·스킬바 정상 확인. 글자 가독성·모바일 패널 여백은 폰 확인 영역. 어긋나면 `assets/ui/default_theme.tres` StyleBox 값 조정.
> - [ ] **18차 전투 손맛 2차**: 적 때릴 때 ① 콤보 3타(마무리)가 1·2타보다 '딱' 묵직하게 멈추는지(히트스톱 등급) ② 적이 맞고 **실제로 뒤로 밀리며 잠깐 경직**되는지(예전엔 넉백이 안 먹었음) ③ 맞는 순간 적이 납작 움찔(squash)하는지. 튜닝: 히트스톱 `player.gd _on_hitbox_landed` 의 match 값, 넉백 감쇠 `state_machine.gd KNOCK_FRICTION`, 경직 길이 `patroller.gd _on_hurt hitstun`, 팝 높이 velocity.y. (코드만)
> ### D. 메모
> - 구버전 적 gen 스크립트(`gen_goblin_fox.py`/`gen_reaper_tiger.py`/`gen_boss.py`)는 이제 폴백 — 재실행 불필요.
> - 배경 재생성 후 `shots/bg_composite.png`·`shots/bg_seam_check.png` 로 이음매 눈확인.

## 대기 / 막힌 것 (Blocked / Waiting)
> - **미감 최종 판단은 사용자 몫** — 스크린샷으로 정적 화면은 검증했으나 애니메이션 타이밍·손맛·전투 밸런스는 실제 플레이 필요.
> - web export 빌드 + 폰 미리보기 환경(호스팅) 결정.
> - **에셋 재생성 방법:** 모양이 마음에 안 들면 `python tools/pixel/gen_<카테고리>.py` 수정·재실행 → `--import` → 게임 반영. 팔레트는 `tools/pixel/palette.py`(STYLE_BIBLE 25색)에서 잠금.

---

# 🗂️ PART 3. 빠른 참조 & 진행 로그

## 프로젝트 정보
- **레포 주소:** https://github.com/ksjmgrkks/joseon-rpg (**public** — 2026-06-29 전환, Pages 무료 호스팅 위해)
- **폰 플레이(웹 미리보기):** **https://ksjmgrkks.github.io/joseon-rpg/** — main 푸시마다 자동 배포(GitHub Pages). '새로 시작'→「해원」 프롤로그.
- **메인 브랜치:** main
- **상세 규칙:** `CLAUDE.md` 참조 / **상세 로드맵:** `조선시대_도트_RPG_제작_로드맵.md` 참조 / **서사 바이블:** `docs/STORY_BIBLE.md` (「해원」 스토리 — 코어·주인공·비트시트) / **서사 히스토리 아카이브:** `docs/STORY_DEV_LOG.md` (HANDOFF에서 뺀 스토리 관련 과거 결정·작업 기록 — 대부분 v1 시절 자료, 참고용)

## 완료된 작업 로그 (최신이 위로)
> 작업 마칠 때마다 한 줄씩 위에 추가 (날짜 + 한 일).

- **2026-08-20 (텔레그램/WSL — 피드백 3번: 웹 세로↔가로 회전 안내문구 안 사라지는 버그 방어적 수정)** — 사용자 지시 "네 푸시하고 3번 진행해주세요"로 마지막 피드백 착수. `scripts/ui/screen_fit.gd`가 `size_changed` 신호 1회만 믿던 걸, ①디바운스 재검사(0.2s, 안 쌓임) ②저빈도 폴링(0.5s, 신호 유실 무관 수렴) ③안내 탭 시 즉시 재검사(수동 안전망) 3중으로 보강. `_apply()`에 `size_override`/`handheld_override` 테스트용 선택 인자 추가. `tests/test_screen_fit.gd` 5케이스 신설(탭하면 박제 상태 풀리는 회귀 테스트 포함). 헤드리스 61스위트 전부 그린. **실기기(iOS Safari 등) 회전 타이밍은 이 환경에서 재현 불가 — 검증 못 함, 정직하게 사용자 확인 요청.** 로컬 커밋만, 푸시는 사용자 승인 대기.
- **2026-08-20 (텔레그램/WSL — 피드백 2번: 모바일 터치 버튼 위치 드래그 편집 기능)** — 사용자 지시 "2번 작업 가시죠"(모바일 키 설정을 드래그로 위치 조정). 설정 화면에 "터치 버튼 위치 → 드래그로 조정" 진입점 추가, `TouchLayoutEditor.tscn`이 실제 `MobileControls`를 잠깐 띄워 현재 배치·텍스처를 그대로 복제한 드래그 가능 핸들(`TouchLayoutHandle`, `TextureRect`+`_gui_input`)로 보여줌. 위치는 신규 오토로드 `TouchLayoutConfig`가 `user://touch_layout.cfg`에 정규화 비율로 저장(`InputConfig`와 같은 패턴), `MobileControls._layout()`의 안전영역 좌표계(`_layout_origin`/`_layout_size`)를 그대로 공유해 해상도·회전이 바뀌어도 같은 비율로 재배치. `MobileControls._place()`가 커스텀 있으면 그걸, 없으면 기존 기본식을 쓰도록 수정(폴백 유지). 저장/초기화/뒤로(미저장 변경 폐기) 버튼, 세이브 슬롯과 무관해 새로 시작해도 유지. `tests/test_touch_layout.gd` 4케이스 신설. **헤드리스 60스위트 전부 그린**(회귀 없음). X서버 죽어있어 실기기 확인은 못 함 — 다음 세션 확인 필요. 로컬 커밋만, **푸시는 사용자 승인 대기**. 3번(웹 회전 안내문구 버그)은 다음.
- **2026-08-20 (텔레그램/WSL — 대화창 초상화 2차 피드백: 초록 잔여물 제거 + 하단 바 밀착·1/3 확대)** — 1차 매팅으로 못 잡은 초록 얼룩 2종(연결요소는 몸통에 붙어 있어 고립 판정 실패 + 색은 밝고 채도 높아 어둡기 기준 침식도 실패)을 `probe_green.gd`/`probe_trim.gd` 실측으로 색 시그니처 규명(`g>=0.20`&`g-min(r,b)>0.10`&`b<g*0.85` = 노이즈, 로브 진짜 청록 자수는 `g<0.20`) 후 `_strip_bleed_green()`(색상 직접 판정) + `_keep_largest_component()`(알파 연결요소 최대값만 남김) 두 단계를 `tools/pixel/extract_protagonist_portraits.gd`/`extract_protagonist_closeup.gd`에 추가(casting/profile_staff 두 칸은 초록이 디자인이라 제외), 재추출 후 7종 육안 확인 완료. `dialogue_balloon.gd`에 `PORTRAIT_HEIGHT_RATIO=1/3`+`_place_portrait(vp, bar_top)` 도입해 초상화를 매 대화마다 "하단 바 바로 위, 뷰포트 1/3 높이"로 재계산 배치(예전엔 `.tscn` 고정좌표라 바와 268px 떠 있었음). 헤드리스 59스위트 전부 그린. X서버 죽어있어 육안 캡처는 못 함 — 좌표 수식·에셋 파일 직접 열람으로만 검증, 실기기 확인은 다음 세션. 로컬 커밋만, 푸시는 사용자 승인 대기.

- **2026-08-20 (텔레그램/WSL — 대화창 개편: 하단 바 + 주인공 상단 초상화, 사용자 외부 아트 적용)** — 사용자 피드백 3건 중 1번. 머리 위로 뜨던 말풍선을 화면 하단 고정 바(스테이지 선택 화면과 같은 PixelLab UI 키트에서 패널 하나를 크롭해 재사용, `assets/ui/dialogue_panel.png`)로 옮기고, 주인공("길손") 대사일 때 상단에 초상화 표시. 초상화는 **사용자가 외부 AI로 만들어 온 원본을 그대로 적용**(PixelLab 아님) — 배경만 우리가 매팅. 첫 시도(밝기<24 배경 판정)는 캐릭터 옷까지 지워버려 실패, `probe_luminance`로 실측(진짜 배경=정확히 0,0,0)한 뒤 임계값 2.0+얕은 경계 침식 2회로 재작업해 성공(`tools/pixel/extract_protagonist_portraits.gd`, `extract_protagonist_closeup.gd`). 표정 9종+풀샷 1종을 `assets/ui/portraits/protagonist/`에 저장, `STYLE_BIBLE.md` §9에 "픽셀아트 규칙 의도적 예외" 명시(되돌림 방지). JSON `"expression"` 메타로 표정 지정 가능. `tests/test_dialogue_portrait.gd` 4케이스 신설. **헤드리스 59스위트(211케이스) 전부 그린.** 이번엔 드물게 X서버가 살아있어 `tools/DlgShot.gd`로 실제 스크린샷 캡처·육안 확인까지 완료(텔레그램 전송). 로컬 커밋만, **푸시는 사용자 승인 대기**. 2/3번 피드백(모바일 키 드래그 배치, 웹 회전 안내문구 안 사라짐)은 다음 세션.
- **2026-08-20 (텔레그램/WSL — 폰 스샷 버그 2건 수정: UI 마젠타 번짐 + 3스테이지 보스 발 정렬)** — 사용자가 폰 스샷 2장으로 신고: 스테이지 선택 창 테두리 핑크색, 3스테이지 보스가 평소 붕 뜨고 공격 시 바닥에 파고듦. **①** `stage_select_window.png`에서 완전불투명 마젠타(245,6,247) 537픽셀을 정확한 색상 매칭으로 투명 처리(안티에일리어싱 없이 고립된 픽셀이라 안전). **②** 근본 원인 규명: `assemble_dokkaebi_chief.gd`가 idle/walk/death(원본 160×160)와 telegraph/attack(원본 220×220, 무기 반경용으로 더 크게 생성됨)의 `get_used_rect()`를 그대로 merge해 서로 다른 캔버스 좌표계를 섞어 쓰고 있었음(캐릭터는 실제로는 두 그룹 다 자기 캔버스 중심에 동일하게 배치돼 있었다는 걸 실측으로 확인). 조립 스크립트를 "프레임마다 자기 캔버스 중심=출력 캔버스 중심" 정렬 방식으로 재작성해 `.pl_tmp/`의 원본 프레임으로 5개 시트 재조립(220×220 통일), `DokkaebiChief.tscn`의 `foot_offset` -11.6→-25.6로 재계산(공식 역산, `test_enemy_footing` 권장치와 일치). `test_enemy_footing.gd`에 "idle 이외 애니메이션 프레임0 발끝이 idle과 일치하는지" 교차검증 신설(기존엔 idle만 봐서 이 버그를 못 잡았음) — 25종 적 전부 통과, 재발 방지. PixelLab 신규 생성 0건(기존 프레임 재사용). 헤드리스 58스위트 207케이스 전부 그린. X서버 죽어있어 육안 확인은 못 함(테스트+데이터 재계산으로만 검증, 실기기 확인은 사용자 몫). 로컬 커밋만, **푸시는 사용자 승인 대기**.
- **2026-08-20 (텔레그램/WSL — 스테이지 선택 화면 + PixelLab 고퀄 UI 아트)** — 3스테이지 완성 직후 사용자 지시 "시작하기 누르면 1,2,3 스테이지 선택할 수 있는 UI 만들어서 푸시, PixelLab으로 이쁘게"(→"이쁘게=고퀄리티" 로 명확화). `scenes/ui/StageSelect.tscn`+`scripts/ui/stage_select.gd` 신설 — `SlotPicker`와 같은 오버레이 패턴(메인 메뉴가 자식으로 얹고 신호로 통신)으로 1/2/3스테이지 카드 3장을 보여주고 고르면 그 스테이지 첫 굽이부터 새 런 시작. 기존 프로젝트 전체가 `default_theme.tres`의 flat `StyleBoxFlat` 버튼만 써왔던 것과 달리, 이 화면만 **PixelLab `create_ui_asset`로 실제 그려진 UI 아트**(한지·먹 톤 창틀 688×384 + 버튼판 560×192, 각 40gen)를 얹어 품질을 눈에 띄게 올림 — 색상 차별화는 PNG를 다시 굽지 않고 런타임 `modulate` 틴트(물=청/숲=녹/저잣거리=주황)로만 처리해 팔레트 원본은 그대로 유지. `Stage.CHAIN[0]` 신규 런 판정 로직은 건드리지 않음 — 선택 화면이 `ScoreManager.start_run()`을 먼저 호출하고 이후 `Stage._ready()`의 `resume()`은 `_running=true` 재확인만 하므로 스코어 리셋이 안전하게 유지됨(둘 다 호출돼도 무해함을 `ScoreManager.resume()` 구현 확인 후 판단, `stage.gd` 미수정으로 리스크 최소화). `tests/test_stage_select.gd` 4케이스 신설(카드 3장 생성·STAGES→씬 경로 매핑·클릭 시 `stage_chosen` emit·아트 파일 존재) — 작성 중 GDScript 람다가 지역변수를 **값으로 캡처**해 클릭 콜백에서 외부 변수가 안 바뀌는 걸 직접 겪음(배열로 감싸 참조처럼 우회, 이 프로젝트 최초 기록이라 다음에 람다로 결과 캡처할 때 참고). PixelLab 사용량 466→**546/2000**. **헤드리스 58스위트(207케이스) 전부 그린.** 로컬 커밋만, **푸시는 사용자 승인 대기**(직전 3스테이지 커밋은 이미 승인받아 푸시됨 — 이 커밋은 그 위에 새로 얹힌 것). 실기기 확인은 다음 세션 몫(카드 터치 크기·레이아웃).
- **2026-08-20 (텔레그램/WSL — 3스테이지 「저잣거리 도깨비」 신규 제작)** — 사용자 지시 "3스테이지 만들어주시고, 기믹이 있었으면". 1·2스테이지 패턴(JSON 데이터 기반 굽이 + `Stage.CHAIN`)을 그대로 따라 5굽이(`market_ruins`·`broken_stalls`·`well_court`·`lantern_alley`·`goblin_court`) 신설, `stage.gd` CHAIN 10→15로 확장. **잡몹(Dokkaebi)·중간보스(DokkaebiBrute)는 기존 미사용 `enemies/goblin` 시트를 스케일만 바꿔 재사용**(도깨비/도깨비 대장 관련 씬이 예전 스토리 모드용으로 `Goblin.tscn`/`GoblinKing.tscn`로 이미 존재했으나 현재 크기 규격에 안 맞고 해원 스토리 JSON에 물려 있어 새 이름(Dokkaebi/DokkaebiBrute/DokkaebiChief)으로 신설, 기존 파일은 안 건드림). **보스(도깨비 대장)는 PixelLab v3 신규 생성**(idle/walk/telegraph/attack/death, east 1방향, 19 generations, 447→466/2000) 후 `tools/pixel/assemble_dokkaebi_chief.gd`(Godot 스크립트, PIL 불필요)로 조립. **기믹:** 그슨대(1회성 위장→노출 게이트)와 다른 종류로, `dokkaebi_chief.gd`가 **매 피격마다** 공격자가 보스 정면/등뒤 어느 쪽인지 판정 — 정면은 칩 데미지(×0.2), 등 뒤(항상 보이는 ◆ 표식)는 온전한 피해+보너스(×1.4). 텔레그래프/공격 중엔 보스가 재조준하지 않아 "회피 후 등 뒤로 도는" 실력 플레이가 성립하게 설계. `HealthComponent.hurtbox_path`를 의도적으로 비워 이중 피해를 막음(자동 연결 시 원본 데미지가 별도로 한 번 더 들어감) — `test_dokkaebi_chief.gd` 4케이스로 고정. 발 정렬은 `tests/test_enemy_footing.gd`의 공식(`half/scale - (foot - frame_h/2)`)을 역산해 계산 후 실제 테스트로 재검증(1차 DokkaebiBrute 14px 뜸 발견→수정). 크기 서열(`test_midboss.gd`)에 3스테이지 항목 추가. `test_gameplay_chain.gd`/`test_stage_roster.gd` 신규 스테이지 반영. **헤드리스 57스위트(203케이스) 전부 그린**(회귀 없음). WSL/텔레그램 세션이라 실기기 확인은 못 함 — 다음 세션 최우선 항목. 로컬 커밋만, **푸시는 사용자 승인 대기**.
- **2026-08-18 (텔레그램/WSL — 몬스터 피격 넉백 2단 완화)** — 사용자 피드백 "몬스터가 맞고 밀려나는 수치가 조금 줄어들었으면" → 1차 반영(1.6→1.2) 후 "조금 더 줄여도 될 것 같습니다" → 2차 반영(1.2→1.0, 증폭 없음·원래 넉백값 그대로). `patroller.gd`·`dummy.gd`의 `KNOCK_RECEIVE`. `test_hit_feel._check_knockback` 기대치를 매번 새 배수에 맞게 갱신. 헤드리스 47스위트 그린, 두 커밋 모두 CI·배포 완료.
- **2026-08-18 (텔레그램/WSL — 물등 스킬 추가 튠: 더 높이 + 대각선 아래로)** — 위 항목 반영 후 사용자 피드백 "더 높게 떠서 바라보는 방향의 대각선 아래로 쐈으면". `HOECHEON_RISE` 56→150px(궁극기 120px보다 높게)·`RISE_TIME` 0.22→0.32s, `charge_time` 0.42→0.5s. 부채꼴 기준 방향을 수평(0°)에서 새 필드 `down_deg`(기본 40°) 만큼 아래로 꺾은 대각선으로 변경 — `base_dir = Vector2(dir*cos(down_deg), sin(down_deg))`로 구성해 좌우 반전에도 항상 아래로 향하게(단순히 각도에 dir 부호를 곱하는 방식은 왼쪽을 볼 때 위로 꺾여버리는 버그가 나서 벡터 성분을 직접 구성하는 방식으로 고쳐 잡음). `tools/SkillFxPreview` 캡처 시점 갱신 후 llvmpipe로 재확인 — 플레이어가 훨씬 높이 뜨고, 부적이 대각선으로 쏟아지며 가까운 적부터 순서대로 스치듯 맞는 게 잘 보임. 헤드리스 47스위트 그린(회귀 없음). CI green·배포 완료.
- **2026-08-18 (텔레그램/WSL — 플레이 피드백 3건: 비/어둠 제거·몬스터 아군오사 수정·물등 스킬샷 개편)** — 사용자가 폰 실플레이하며 3건 지적. **①비+전역 다운틴트 제거:** `Weather` 오토로드의 비 파티클을 빈 CanvasLayer로(스크립트는 남김, `SkillFxPreview` 참조 안 깨지게), `WorldTint.OVERCAST`를 회청색(0.40,0.45,0.55)→중립(1,1,1)로 — 전 화면 곱하기 다운틴트가 근본 원인이었음. **②몬스터끼리 공격 버그:** `Hurtbox._on_area_entered`가 공격자·피격자 진영을 안 가려서, Hitbox를 쓰는 보스가 근처 다른 적의 Hurtbox까지 때릴 수 있었음(patroller는 direct call이라 원래 안전) → 둘 다 "enemy" 그룹이면 무시하는 가드 추가, `test_combat.no_friendly_fire_between_enemies` 신설. **③「진혼의 물등」스킬샷 개편:** 즉발 광역(반경 200 전체 피해) → 궁극기처럼 살짝 부양(0.42s 시전) 후 부채꼴(70°/7발)로 부적 투척, 각 부적은 실제 직선 투사체(`TalismanShot` 신설, SpiritOrb와 대칭 구조)라 사거리·각도 밖 적은 안 맞음. `_ult_hover`→범용 `_hover_lock`으로 리네임(두 스킬이 공유). `skills.json` hoecheon 필드 전면 교체(radius/knock_mult → charge_time/count/spread_deg/speed/life). **검증:** 헤드리스 47스위트(신규 test_hoecheon 3케이스 포함) 전부 그린, `DISPLAY=:0` llvmpipe 스크린샷으로 메뉴/스테이지 밝기와 부채꼴 투척 연출 실제 확인 후 텔레그램 전송·사용자 확인. 3커밋 모두 CI(헤드리스+웹빌드) green, GitHub Pages 배포 완료. **⚠ 남은 판단:** 물등의 70° 스프레드 폭·데미지가 실전에서 괜찮은지는 사용자 실플레이 피드백 필요.
- **2026-08-18 (텔레그램/WSL — 시작 화면 배경 톤 업)** — 사용자 피드백 "시작 화면 배경이 너무 어둡다". `scenes/ui/MainMenu.tscn`의 `Bg` ColorRect가 `Color(0.16,0.14,0.11)`(거의 검정)이던 것을 `Color(0.58,0.53,0.44)`(밝은 한지빛, `parallax_backdrop.gd`의 낮 하늘색 0.93,0.89,0.78보다는 어둡게 눌러 무드 유지)로 교체. **WSLg `/tmp/.X11-unix/X0` 소켓 발견 → `DISPLAY=:0`로 `tools/Screenshot.tscn` 렌더 성공(llvmpipe 소프트렌더)**, 텔레그램으로 전송해 사용자 확인("좋네요") 받고 푸시. CI(헤드리스+웹빌드+Pages 배포) 전부 success. **참고: 이 환경에서 앞으로 시각 검증 가능**(직전 세션들이 "디스플레이 없음"으로 기록한 건 `DISPLAY` 미설정 때문이었음, `DISPLAY=:0` 붙이면 됨).
- **2026-08-18 (텔레그램/WSL — 사이드 진혼 3기 헤드리스 그린 검증)** — "조선 rpg 대화 멈춘곳부터 진행" 지시로 시작. `~/godot-bin/godot` 4.6.3 headless 바이너리 발견(직전 세션 기록엔 "Godot 없음"이었으나 이 세션엔 있음). CI와 동일 절차로 `--import` 2회 후 `tests/*.tscn` 46종 전수 실행 → **exit code + `[FAIL]` 마커 기준 46스위트 전부 그린**(사이드 진혼 선택 전투 배선 관련 `test_story_data`·`test_endings`·`test_haewon_chain`·`test_gameplay_chain`·`test_combat_gate` 포함, 회귀 없음). PixelLab 잔여 재확인 → 여전히 만료(항목 2 착수 불가). Android SDK 부재 확인(항목 4 불가). 씬 파일명 개명(항목 5) 참조 범위 조사 — `save_manager.gd`가 세이브 경로 문자열을 포함해 리스크 있다고 판단, 단독 진행 보류. HANDOFF 갱신. **⚠ 디스플레이 없어 시각 검증은 여전히 불가.** **다음: 사용자 푸시 승인 대기.**
- **2026-08-17 (텔레그램/WSL, Godot 없는 세션 — 인수인계 감사·HANDOFF 갱신만)** — "최근 세션 이어서 진행" 지시로 시작. `git log` 확인 결과 직전 PC세션(2026-08-15, `development` 브랜치)이 게임 방향을 스토리→전투체인 스코어어택으로 크게 전환(3커밋: 통과판정·스코어어택·라이브루프 스토리 제거, 헤드리스 39~41스위트 그린 확인하며 커밋됨)했는데 **HANDOFF에 기록이 전혀 안 남아 있었고, 이 3커밋이 `main`엔 병합/푸시가 안 돼 있어 GitHub Pages 라이브 빌드가 여전히 옛 「해원」 버전**임을 발견. 이 세션은 Godot 없는 환경이라 코드 수정·재검증 불가 → HANDOFF "현재 상태"·"다음 할 일" 갱신으로 기록만 정리. **다음(최우선, 사용자 결정 대기): `development`→`main` 병합·푸시**(항목 21 참조) — 승인 시 즉시 CI 재검증+GitHub Pages 배포 가능.
- **2026-07-11 (텔레그램 — 신규 원혼 아트 + 대사 개편 + 소거 끔 + '남은 적' 제거 + 웹빌드 진단)** — 사용자 4건. **신규 원혼(진짜 새 아트):** PixelLab v3로 저승 뱃사공 넋·물아이 넋 캐릭터 신규 생성 → idle/walk/death east 애니 → **PIL 없이 Godot 조립**(/tmp/assemble.gd: CDN 프레임 curl → get_used_rect union bbox 크롭 → 스트립+manifest)로 assets/sprites/enemies/boatman·waterchild 시트화. DrownedFerryman/DrownedChild 씬 신설, forest_deep·mountain_pass·ruined_temple 배치, 물아이 조우 대사 추가. foot_offset 스샷 검증. **대사:** 전투 체인 대사를 호환 잔재 제거+물 진혼 톤으로 재작성, 오글거림 절제, tut_skill 새 스킬명. **소거 끔:** dialogue_balloon에서 기억 소거(글자 흐려짐) 배선 제거→대사 항상 또렷(순수함수/테스트 보존). **'남은 적' 제거:** combat_gate 카운터 삭제. **웹빌드 문의:** CI 배포 성공 확인, 캐시/비율 문제로 판단해 사용자 확인 요청. 헤드리스 37스위트 그린. 커밋 f725069·261205b·54930aa 푸시. 남은 것: 해원 캠페인 대사 톤 개편·확장 지속.

- **2026-07-11 (텔레그램 — 스킬1·2 진혼 리테마 + 스토리 부적합 몬스터 개편)** — 사용자: "①스토리 안 맞는 몬스터/보스 개편+스토리·몬스터·보스 많이 확장 ②1·2 스킬 특색 없음, 컨셉 맞게 새로". **스킬:** 물·진혼 컨셉으로 1번=「여울 가르기」(물살 참격 돌진+넓은 물마루 히트박스), 2번=「진혼의 물등」(반경 200 광역 파문, 적 Hurtbox.hurt 로 피해·넉백). `skill_fx.gd` river_cleave/requiem_lantern 신설(물빛/물등 팔레트), `player.gd` _skill_ilseom/_skill_hoecheon 개편, `skills.json` 이름·설명·수치, 새 PixelLab 아이콘 2종(물결/물등)로 교체. **몬스터:** 전투 체인의 도깨비/구미호/호랑이 → 물 원혼. 신규 씬 DrownedSwarm(휩쓸린 넋)·DrownedHeavy(물 먹은 넋)·FloodWraith(수문의 원혼 보스, 대호 대체·사망배선 유지). 5개 스테이지 JSON 적 재배치, 물 서사 대사 2종 신설(great_flood_intro·temple_reaper)로 옛 대호/구미호 인트로 대체. **검증:** 헤드리스 36스위트 그린, llvmpipe 스샷(원혼 발치 물웅덩이·보스 접지·새 HUD 아이콘) 확인. 커밋 2f7292a 푸시. **미완(방향 대기):** 완전 신규 몬스터 그림(PIL 부재로 캐릭터 시트 통합 미착수 — Godot 조립으로 재구현 가능) + 스토리 굽이 확장.

- **2026-07-11 (텔레그램 — 비 어색·대화 자세·맵/배경 전면 개편 3건)** — 사용자: "①비가 너무 어색 ②대화 시 구르던 자세로 멈춤 ③맵·배경 아예 새로(2-1: 그림+구조, 수묵 유지·심화)". **①** rain.png가 여러 줄기 타일이라 파티클당 슬래시 뭉치로 보이던 것 → 코드 단일 물줄기(2×26, 머리 옅고 꼬리 진함)+낙하방향/기울기 9° 일치+등속 낙하로 교체, 어색한 회색 비늘 먹구름(clouds.png) 제거(`weather.gd`). **②** `_freeze_for_dialogue`가 dodge/attack/skill_dash 상태를 안 풀어 player_visual이 구르기 프레임에 얼어붙던 버그 → 대화 시작 시 즉시 해제+hurtbox 무적 복구(`player.gd`), `test_dialogue_freeze`에 dodge_resets 케이스 추가(4/4). **③** 배경=PixelLab 수묵 산수 2장(mtn_far 먼산맥·mtn_hill 솔숲)을 좌우반전 교대 mirror-tile로 이음매 없이 무한반복+코드 안개띠, `parallax_backdrop.gd` 전면 재작성(옛 스트립 폴백 유지). 지면=PixelLab 사이드뷰 wang 타일셋 3종(earth/rock/stone)의 윗면(잔디/이끼/서리)+속을 시트에서 잘라 repeat로 깔도록 `stage.gd` 확장 + JSON `platforms`(one-way) 신설. 5개 전투 체인 스테이지 JSON 재구성. **검증:** headless test_gameplay_chain 5/5·test_dialogue_freeze 4/4, llvmpipe 소프트렌더 5맵 스샷 육안 확인(이음매/발판/톤 정상), 텔레그램 3샷 전송. PixelLab ~40gen(잔여 ~1600). 커밋 ec28eb7·160dcdf, 미푸시. 남은 것(사용자): 흙 타일 더 뭉갤지 + Pages 푸시 여부.

- **2026-07-01 (워크트리 `worktree-monster-dialogue` — 스토리 부적합 몬스터 교체 + 대화 UI 3종)** — 사용자: "몬스터가 스토리에 안 맞음(구미호?), 대화 텍스트 색 들쭉날쭉, 상황설명 대화가 몰입 깸, 혼잣말은 말풍선 아래 … 표시로." 새 브랜치/워크트리에서 처리. ① **구미호 제거**: 홍수·수몰 서사에 안 맞는 Fox(3굽이)·FoxQueen(6굽이 최종보스)·Goblin(2굽이)을 PixelLab v3 신규 **수몰 원혼(Wraith)**·**윤슬의 넋(YunseulSpirit)**으로 교체(생성→idle/walk/attack/death east 애니→`integrate_enemies.py`로 시트+접지 패치→새 .tscn 2개→`enemy_pl.json`→스테이지 JSON 배선). 옛 귀창록 캠페인은 보존. ② **대화 색 일관화**: `MemoryGlyph._threshold` 군집버그(Godot hash() 유사입력 군집→대사마다 0.85 부근 몰림) PCG로 수정 + dissolve를 진혼-후 6개 비트에만 한정(`"dissolve": true`+`Dialogue.meta()`). ③ **나레이션**=수묵 자막 띠. ④ **혼잣말**=thought 말풍선+‘…’ 점. `dialogue_balloon.gd` 3모드(SPEECH/THOUGHT/NARRATION) 재구성, `bool()` 런타임버그 수정. 헤드리스 34/34 그린 + 시각 6샷 검증. PixelLab ~19gen.

- **2026-07-01 (PC — 기본 공격 3타 리워크: 역동 전진 + PixelLab 모션/이펙트 + 범위·데미지 점증)** — 사용자 "기본 공격을 ①바라보는 쪽으로 역동 전진하는 3타 ②자연스러운 이동 모션(PixelLab) ③1→2→3 점점 커지는 화려한 범위 이펙트+데미지(PixelLab)". **PixelLab(잔여 1802→~1727):** 캐릭터 콤보 모션 3종 v3 재생성(`attack1_lunge2` 2a881a59·`attack2_stride2` 7d977231·`attack3_leap` ee0f899b — 1·2타는 첫 생성이 머리위 와인드업이라 '전진 펜싱 런지/수평 횡베기'로 리롤) + 이펙트 오브젝트 3종 신규(sidescroller, 4후보 선택): `combo_thrust`(862eb99f)·`combo_sweep`(766878ad)·`combo_burst`(a65238d2) → `assets/sprites/fx/combo_*.png`. **코드:** `player.gd _do_combo_attack` — 단계별 데미지 ×1.0/1.35/2.0·히트박스 폭 28/44/70(전 24 고정, 앞으로 뻗어 범위 공격화)·런지 110/150/240·공격 중 마찰 0.2배(전진 글라이드)·종료 시 히트박스 복원; `attack_shape` onready + `_hitbox_base_size` 추가. `skill_fx.gd _spear_thrust/_spear_sweep/_spear_spin` — `combo_*` 텍스처로 교체 + 스케일/기하 확대(3타=3겹 링+16 가시+금 불티). `integrate_combos.py` UUID 갱신, `ComboPosePreview.gd` 프레임 7/7/10. combo_thrust 우향 텍스처에 맞춰 flip `not facing_right` 보정. **검증:** 헤드리스 34/34 그린(회귀 0), 콤보 프로브(히트박스 28→44→70·데미지 15→20.3→30·전방 위치 20→28→41), `shots/fx_preview.png`(이펙트 1<2<3 점증)·`shots/combo_pose.png`(전진 런지 포즈+이펙트) 확인. 실제 플레이 손맛(런지 거리·3타 폭발 크기)은 `플레이.bat`로 사용자 판단 — 튜닝점은 player.gd/skill_fx.gd에 모음.
- **2026-07-01 (사운드 전반 개편 — 오디오 버스·리버브 + 해원 6굽이 전용곡 4곡 + 효과음 톤 정리)** — 사용자(PC, worktree) "사운드 전반 개편 + 4번 귀창 강림 밝은 소리 제거 + 생성 방식?". `worktree-sound-overhaul` 에서 4갈래: ① **귀창 강림(궁극기) 밝은 징글 제거** — `_skill_ultimate` 가 퀘스트용 `jingle_quest`(5음계 상승 종소리)를 함께 울려 어두운 마창 궁극기 톤을 깨던 것. ② **공간감/잔향** — `audio_manager` 를 Master←Music/SFX 버스 구조로 재작성, 각 버스에 `AudioEffectReverb`(Music wet .26 깊게 / SFX wet .10 옅게)를 코드로 구성(default_bus_layout 없이). 볼륨이 버스 게인으로 통일(BGM/SFX 분리 제어, 세이브 연동 유지) → 모노 소스도 스테레오 공간감. ③ **BGM 다양화** — 해원 6굽이가 `night` 1곡 돌려막기였던 것을 `sound` 에이전트가 계면조 전용곡 4곡 작곡(`haewon_river` 강가진혼·`grief` 죄·`hollow` 공허·`requiem` 최종승화, 38~44s). `bgm_director` 매핑 분화(프롤·1·2→river, 3→grief, 5→hollow, 6·엔딩→requiem, 4굽이 수문=boss 유지). ④ **효과음 톤** — 궁극기 전용 `sfx_ultimate`(저역 임팩트+마기 우르릉+귀신 창) 신설해 평타와 차별, 호신부 밝은 징글→절제된 가호음 `sfx_ward`, 과대 점프음(RMS −9.4 outlier)을 `write_wav` `peak_override` 로 −13.7 정상화. **검증: 신규 `test_audio`(버스/리버브/볼륨/bgm경로 4케이스) + `test_skills` 4/4·`test_ultimate` 1/1·`test_save` 3/3 헤드리스 그린. 잔향 양·곡 정서·효과음 무게는 청각 미감 = 사용자 확인 항목(미머지 시점 미청취).** 커밋 6개(8739f10/5d92057/26e225a/f9906db/fbf593e/8f61d56), main 머지.

- **2026-06-30 (PC 검증세션 — 회상 컷 전환 시스템 import·테스트·시각 검증 + test_cutscene 신설)** — 사용자 "git pull 받고 HANDOFF 읽고 계속". `git pull`로 폰세션이 만든 회상 '컷 전환' 시스템(cutscene.gd/auto_cutscene.gd + SceneManager.play/return_from_cutscene + 회상 3장 JSON·.tscn + 윤슬·젊은길손 PixelLab PNG) 수신 — 전부 "Godot 없어 미검증" 상태였음. 이번 PC세션이 끝까지 검증: ① **import** — 새 컷씬 PNG 2개·스크립트 2개·테스트의 `.import`/`.uid` 사이드카 생성(`--import`, project.godot md5 불변 확인) 후 커밋. ② **헤드리스 34/34 그린** — 기존 33종 + **신규 `test_cutscene`(8케이스)**: Cut*.tscn 래퍼→cutscene_id→JSON→참조 에셋(대사·인물 스프라이트·prop/바닥 텍스처) 무결, haewon_1/4 `clear_cutscene`·haewon_5 `auto_cutscenes`→존재하는 Cut*.tscn + `from_recall` 복귀점, `SceneManager.play_cutscene→return_from_cutscene` 복귀컨텍스트(전투씬+entry) 왕복·소비, `_build_auto_cutscenes` 노드 파라미터. ③ **시각 검증(Screenshot.tscn)** — 회상 3장 `shots/verify/cut_*.png`: 윤슬강가(동틀녘 따뜻 톤, 젊은길손↔윤슬 마주봄, **발 접지 완벽**)·수문회상(inkwash 수묵 흑백, 길손+수문 한 폭)·홍수의밤(수묵, 길손 단독—윤슬 안 보임 의도). 런타임 `get_used_rect()` autocrop 접지 정상, 카메라 프레이밍·inkwash 톤 의도대로. ④ **픽업 가시성**(헤드리스 프로브) — haewon_5 꽃신/가락지/비녀·haewon_2 통문 전부 charm/scroll **Sprite2D visible=1** 확인(직전 세션 가시화 수정이 실제로 렌더됨; 24px라 작아서 스샷엔 묻히나 코드는 정상). ⑤ **엔딩 변주**(프로브) — 단서 0/1/3/6개→등불 3/4/6/9개 + 마지막 줄 4단계 변주 전부 작동. ⑥ **잠재버그 점검** — cutscene/stage 의 대사·컷씬 게이트 `ResourceLoader.exists(.json)`가 .json 에 **true** 반환 확인(Godot 4.x JSON 리소스) → 대사 조용히-스킵 버그 없음. **남은 것(코드론 못 봄):** 실제 플레이 왕복 손맛·복귀 위치 체감(`플레이.bat`), 전용 prop PixelLab(수문/물등/keepsake placeholder). 푸시 완료.
- **2026-06-30 (WSL세션 — 해원 체인 코드 감사 + 단서 픽업 2개 버그 수정)** — 텔레그램 "조선 rpg 다시 pull 받고 이어서 진행". `git pull`로 직전 PC 런북 커밋 수신. WSL이라 **Godot·PIL 없음** → 시각 작업 불가, 대신 **「해원」 스토리 모드 전체를 코드/데이터로 끝까지 감사**: 새 게임→`Haewon0Prologue`(main_menu.gd `START_LEVEL_PATH`) → 7굽이 .tscn 래퍼(루트명=저장키, stage_id 지정)·체인(각 stage exits 다음 굽이)·gut(0~6)·clear_dialogue·clear_inkwash·`MemoryLedger.erase_for_gut`(gate.opened bind)·`MemoryGlyph.dissolve`(dialogue_balloon.gd 글자)·`HaewonEnding`(단서 수→등불 3~5·마지막 줄 변주)까지 **전부 연결, 깨진 dialogue/scene 참조 0, autoload(MemoryLedger/InkWash) 등록·새게임 reset 정상** 확인. **→ HANDOFF item 20 "남은 배선(a~d)"은 stale(24·25차에 이미 완료), 남은 건 (e) prop PixelLab 아트뿐.** **발견·수정한 실버그:** 게임 내 유일한 데이터 픽업인 통문(2굽이)·비녀(5굽이) 단서가 ① `icon` 미지정 → `_build_pickups`가 ColorRect 플레이스홀더도 안 만들고 `_setup_icon`도 스킵 → **화면에 안 보임** ② `item` 없는 플래그 전용이라 `pickup.gd`의 라벨·SFX가 item 분기 안에만 있어 **주워도 아무 피드백 없음** → 엔딩의 "발견한 단서 수" 변주가 사실상 죽어 있었음. **수정:** 두 JSON에 `icon`(통문=scroll·비녀=charm, 기존 스프라이트 재사용, import 불필요) + `pickup.gd`에 `gave_item==false && pickup_label!=""` 시 FloatingNumber+PICKUP SFX 발동. **⚠ Godot 없어 헤드리스/시각 미검증** — PC에서 ① 전체 스위트 그린(test_pickup_fx/test_drops) ② 단서 픽업이 보이고 줍힐 때 라벨 뜨는지 ③ 엔딩 등불 수·마지막 줄 변주 눈 확인 필요.
- **2026-06-30 (PC 런북 A→B→C — 적·보스 PixelLab 아트 + VFX/배경/3타 인게임 반영)** — 사용자 "git pull 받고 HANDOFF 읽고 계속". 지금 **PC 세션**이라, 27~29차까지 폰세션이 코드/데이터로 쌓아둔 뒤 **줄곧 막혀 있던 PC 숙제(13~16차 PNG import)** 를 실행. ① **런북 A**(스크립트 3개): `integrate_enemies.py`→도깨비(80px)·저승사자(92px)·구미호(84px)·호랑이(92px) east 프레임을 PixelLab CDN서 curl 로 받아 가로 스트립+manifest+telegraph 파생 생성, **씬 10개(Goblin/Patroller/Dummy/GoblinKing/Reaper/ReaperLord/Fox/FoxQueen/Tiger/GreatTiger) foot_offset 자동 패치**(scale 보정 접지); `gen_background.py`→bg_far/mid/near 재생성; `integrate_combos.py`→attack3 13프레임. **실행 중 발견·수정**: `integrate_enemies.py` 가 `≈` 출력에서 Windows cp949 콘솔 크래시 → stdout UTF-8 재설정 추가(향후 재실행 안전). ② **런북 B**: `godot --headless --import` 1회 → FX 10종·물등 타일셋·신규 스크립트(memory_glyph/ledger/ink_wash/haewon_ending/테스트) `.import`·`.uid` 사이드카 생성(project.godot md5 불변 확인). ③ **런북 C**: **헤드리스 33종 전부 그린(100+ 케이스)** + 스크린샷 4종으로 적 4종+보스2(구미호여왕·백호) **새 PixelLab 아트·접지·새 배경**(그라데이션 하늘·별·원경 산·새 떼) 확인. **이로써 적·보스가 주인공과 같은 PixelLab 톤으로 통일됨(우선순위 Ⓐ 완료, 가장 큰 시각 임팩트).** 새 PNG라 web(Pages) 반영엔 이번 푸시의 CI 빌드 필요. **남은 PC 판단(플레이 손맛)**: 14차 VFX 합성 강도·13/18차 조작 손맛·전투 밸런스는 실제 플레이 몫.
- **2026-06-29 (29차 — 해원 타이틀 표시 + 6굽이 BGM 무음 해소)** — 사용자 "다음 예정 작업 쭉 진행". 먼저 Explore 에이전트로 현황 점검: **UI 한지·먹 스킨(전역 테마 `default_theme.tres`)·모바일 터치조작(`MobileControls.tscn`)·전투 VFX 모두 이미 완료** 확인(후보 1·6 done). 실제 남은 폰-검증가능 갭 2개 수정(둘 다 오디오 `.import` 존재 → web 반영): ① **메인 메뉴 타이틀 숨김 잔재**(12차 게임플레이모드에서 title/subtitle/logo 숨김) — 이제 해원 게임인데 제목이 안 보였음. `main_menu.gd` title/subtitle 표시(옛 그래픽 로고 TitleLogo='귀창록'은 숨김 유지), locale `menu.title` '귀창록'→**'해원'**, 부제 '解冤 · 강을 거슬러, 맺힌 한을 풀다'(en도), Title 라벨 font_size 84+금빛. ② **해원 6굽이 BGM 부재** — `bgm_director.gd`가 `current_scene.name`으로 곡을 찾는데 `Haewon0~6`·`HaewonEnding`이 매핑에 없어 **해원 플레이 내내 무음**이었음 → 밤(진혼톤)/보스굽이(수문4·윤슬6=전투곡)/엔딩(잔잔) 매핑 추가. **검증: 엔딩 단서(통문 haewon_2·비녀 haewon_5 pickup flag)는 이미 배치·`haewon_ending.gd` 배선 확인(갭 아님). CI 헤드리스 그린·웹빌드·Pages 배포.** **→ 후보 목록 중 폰-검증가능 항목은 사실상 소진. 남은 건 PC게이트(2. PixelLab 적·보스 아트 import)·플레이피드백(4. 밸런스)·방향결정(신규 굽이/서사 확장).**
- **2026-06-29 (28차 — 대화 말풍선: 화자 머리 위 + 좌→우 타이핑 연출)** — 텔레그램 지시: "대화가 캐릭터 위 말풍선으로 뜨고, AI 답변처럼 좌→우 애니메이션으로 나타나게. 필요하면 픽셀랩으로 말풍선 생성." 기존 대화창은 화면 하단 고정 패널이었음. ① **화자 추적 말풍선** — `DialogueBalloon.tscn`을 하단 패널→떠다니는 말풍선(Bubble PanelContainer + 코드 _draw 꼬리 Tail)으로 교체. `dialogue_balloon.gd` `_process`에서 매 프레임 화자 월드노드를 `get_canvas_transform()`로 화면투영해 머리 위에 배치(클램프). ② **화자 이름→노드 매칭**: "길손/길손(낮게)"→플레이어(group player), NPC는 대화 트리거가 넘긴 partner 노드, 적/보스는 `display_name` 매칭. 매칭 안 되는 나레이션(`speaker:""`)·넋은 화면 상단 중앙 박스로 폴백. 이를 위해 `dialogue_manager.start(path, partner=null)` + `partner_node()` 추가, `npc.gd`가 `start(path, self)`로 자기 노드 전달 + `add_to_group("npc")`. ③ **좌→우 타이핑** — 본문을 `RichTextLabel.visible_ratio` 0→1 트윈(34자/초, 기억 dissolve bbcode와 호환). 진행 중 [Space]/상호작용이면 먼저 즉시 완성(스킵), 완료 상태면 다음으로. 선택지는 즉시 노출(입력/테스트 즉시성). 말풍선 등장 시 페이드인. ④ **한지·먹 스킨**: 코드 StyleBoxFlat(크림 bg·먹 테두리·둥근 모서리·그림자) + 화자명 단청 적 + 본문 먹빛. **PixelLab은 보류** — 새 PNG는 PC에서 Godot import해야 web 반영돼 '폰서 바로 확인' 루프가 끊김. 코드드로잉이라 푸시 시 web 즉시 반영. 9-slice 픽셀 말풍선은 사용자가 원하면 후속(생성은 폰서, 적용·import는 PC). **검증: WSL Godot 없음 → 들여쓰기·괄호 정합·`test_dialogue_keys`(선택지 즉시성) 로직 정적 확인. 실제 렌더/추적/타이핑은 CI 헤드리스 + 폰 브라우저(Pages).** **CI 헤드리스/웹빌드/Pages 모두 그린 푸시 완료.** ⑤ **폰 피드백 즉시 반영(503fca7)**: 말풍선이 너무 높다 → 머리 위 오프셋 −54/−48/−52→**−28/−26/−28**, `GAP_ABOVE` 12→**4** (캐릭터 원점이 발/중심인지 미정이라 추가 미세조정 여지). 폰에서 대화 넘길 방법 없음(Space 키 부재) → **`TapCatcher` 전체화면 투명 Button**(focus_mode 0, modulate a=0) 추가, `pressed`→`_advance_or_skip`(타이핑 중 스킵/완료 시 advance), 선택지 있을 땐 탭 무시(버튼으로 고름). 힌트 '탭 ▶'/'골라서 탭'. (CI 재검증 그린.) ⑥ **꼬리 위치 정밀 수정(3ca3bbf)**: "높이 여전히 높음 + 꼬리가 머리 위에 있어야" → 주인공 스프라이트가 **92px 프레임·centered·offset(0,−28)**이라 머리 꼭대기가 원점 −74인데 −28(가슴) 앵커라 꼬리가 몸통을 찌르고 풍선만 떠 보였음. 추측 오프셋 폐기 → `_head_world()`가 **Visual 스프라이트 프레임 높이로 머리 꼭대기 월드좌표 계산**(AnimatedSprite2D/Sprite2D, offset·centered·`get_global_transform()` 반영, 스프라이트 없으면 `_fallback_offset`). 꼬리 끝을 머리 꼭대기에 부착, 풍선은 그 위. **`HEAD_MARGIN`(현재 10)** 으로 높이 미세조정(+면 풍선 더 내려옴). 캐릭터별 자동 정렬이라 적/NPC도 각자 머리 위에 붙음. (CI 그린·배포.)
- **2026-06-29 (27차 — 전투 VFX 마무리: 보스 분노 격발 + 완전차지 한 방 루프)** — 텔레그램서 "VFX 마무리(3번)" 지시. 먼저 코드 점검: 차지 오라(`charge_aura_tick`)·적 피격 플래시(`hit_flash`)·사망 혼 흩어짐(`death_scatter`)·보스 등장(`boss_entrance`)은 **이미 구현·배선 완료**(13~16차 분, 코드드로잉이라 web 이미 반영)였음. 실제 갭 2곳만 보강 — 전부 코드/web 즉시 반영, PC import 불필요: ① **보스 페이즈2 전환 연출** — 기존엔 색조+shake뿐이라 '분노' 순간이 밋밋 → `skill_fx.gd boss_enrage(pos)` 신규(핏빛 폭발 링 2겹·사방 핏빛 마기 알갱이 12개·머리위 분노 기둥 5가닥, 등장=솟음 vs 분노=바깥터짐 대비), `boss.gd._enter_phase2`에 배선. ② **완전차지 도달 한 방 피드백** — 임계 통과 순간 신호 없이 오라만 반복되던 것 → `skill_fx.gd charge_ready(pos)` 신규(금빛 BACK-ease 번쩍 링+사방 8불티, 누름당 1회), `player.gd`에 `_charge_full_fired` 플래그로 1회성 배선(릴리스 시 리셋). ③ 루프 완성차 **차지 강타 발동 시 금빛 일섬**(`SkillFx.slash(..., GOLD)`) 추가 — '기 모음(금빛 ready)→강타(금빛 일섬)' 시각 연결. **검증: WSL Godot 없음 → 들여쓰기(4-space, 탭 혼용 0)·함수 시그니처(`slash`/`_mote`/`_pulse_ring`/`_line`) 정합 정적 확인. 실제 렌더 미감은 폰 브라우저(Pages) 또는 PC.** 새 PNG 없음 → 푸시 시 web 자동 반영.
- **2026-06-29 (CI/배포 — 웹빌드 수정 + GitHub Pages 폰 플레이 가동)** — 사용자 "폰 플레이 되게 해달라(1번)". ① **web export 수정**(`.github/workflows/web-build.yml`): export 템플릿 디렉터리명이 `4.6.3-stable`(대시)라 Godot가 `4.6.3.stable`(점)에서 `web_nothreads_release.zip` 못 찾아 **21차부터 줄곧 실패**하던 것 → `${GODOT_VERSION/-/.}` 점 표기로 수정 + 템플릿 캐시 포함. ② **GitHub Pages 자동배포**: configure-pages+upload-pages-artifact+deploy-pages 잡 추가. ③ 사용자가 레포 **public 전환** + Settings→Pages→Source 'GitHub Actions' 활성화(워크플로 토큰은 Pages 생성 권한 없어 수동 1회 필요했음). ④ **결과: https://ksjmgrkks.github.io/joseon-rpg/ 라이브(HTTP 200, Godot wasm/pck 정상)** — 폰에서 「해원」 플레이 가능, main 푸시마다 자동 갱신. 워크플로 파일 푸시는 기본 토큰에 workflow 스코프 없어 사용자 fine-grained PAT 로 1회 처리(이후 폐기 요청). **잔여 cosmetic: 브라우저 탭/부트 제목이 아직 '귀창록'**(project.godot application/config/name) — 해원 타이틀 작업 때 '해원'으로 교체 예정.
- **2026-06-29 (24차 — 해원 스토리 모드 배선: 실제로 게임에서 글자가 지워짐)** — 사용자 "푸시하고 해원 스토리로". ① 메인메뉴 START→`Haewon0Prologue`, 새 게임 시 `MemoryLedger.reset()`(`main_menu.gd`). ② `stage.gd._ready` per-stage 라우팅: JSON `"gut"` 있으면 스토리 빌드(전역 GAMEPLAY_ONLY 유지 → 옛 test_gameplay_chain 안전). ③ `combat_gate.gd` 에 `signal opened` 추가(개방 시 emit). ④ `stage.gd._wire_haewon`/`_on_gut_cleared`: 결계 opened→`MemoryLedger.erase_for_gut(gut)`+`clear_dialogue` 재생, 무전투 굽이는 진입 0.6s 후 소거. ⑤ `dialogue_balloon.gd` 본문을 `MemoryGlyph.dissolve(text, progress, hash(speaker+text))` 로 렌더(진행도 비례 글자 바램). ⑥ `scenes/levels/Haewon{0..6}.tscn` 7개 래퍼(stage_id 지정). ⑦ SaveManager AREA_LABELS + main_menu AREA_SCENES 해원 7지역. ⑧ haewon JSON: clear_dialogue 분리(after/reveal 대사를 위치트리거→결계연동), 픽업 flag-only 화. ⑨ `tests/test_haewon_chain.{gd,tscn}` 9케이스. **검증: WSL Godot 없음 → JSON 유효·대사참조 무결·gut 라우팅 정합 정적 확인. 헤드리스/인게임 dissolve·접지는 PC.** 푸시 완료(22~23차 4커밋).
- **2026-06-29 (23차 이어서 — 물등 PixelLab 아트)** — 사용자 "PixelLab 적극 활용" 지시. **물등(유등)** 픽셀아트 PixelLab 생성(sidescroller object 96px, 4후보→2채택, 20gen, 잔여 ~1806). 채택: `assets/tilesets/mul_deung.png`(뗏목 받침형, 한지 '평안' 패널 — 강에 띄우는 위령 등, d98e6f43) + `assets/tilesets/mul_deung_stand.png`(받침대형, e996de1e). tilesets `manifest.json` props 에 등록(96×96). 가장 상징적인 두 지점 — **프롤로그 '첫 등'(haewon_0)·최종 '마지막 등'(haewon_6)** 의 floating lantern prop 을 `lantern`→`mul_deung` 으로 교체(offset[-48,-96]·y686·scale 0.8~0.85 = 캔버스 바닥을 지면선에 정렬, 96px 패딩 고려). **검증: 이 환경 Godot 없음 → PNG 다운로드·MCP 프리뷰로 미감 확인(평안 패널·한지톤 OK), 인게임 접지·스케일은 PC 확인/미세조정 대기. .import 는 PC에서 Godot 첫 실행 시 생성.** 후보 [2](단청 채색)·[3](깨진 더미)는 톤 불일치/실패로 폐기. 나머지 굽이의 lantern→mul_deung 교체와 애니(촛불 흔들림 animate_object)는 선택적 후속.
- **2026-06-27 (14차 — PixelLab VFX 퀄업: 3타 모션 + 페인티드 이펙트 + 출혈)** — 사용자 "3타 모션·이펙트 + 스킬 이펙트 + 몬스터 출혈을 PixelLab MCP로 퀄업". PixelLab 구독 활성 확인(2000 gen). ① 캐릭터 `animate_character` v3 로 `attack3_spin_v2`(13f, east) 생성 → `tools/pixel/integrate_combos.py` attack3 = 새 UUID `1552a702…`/13f 로 교체(PC에서 스크립트 실행 시 strip·manifest 반영). ② `create_1_direction_object`(sidescroller, 160px, 4후보) 3건으로 VFX 생성·후보 검토(MCP 프리뷰)·채택 다운로드: slash(crescent/swirl/wide 3장)·thrust_lance·spark(star/burst 2장)·blood(splat/spray 2장) = `assets/sprites/fx/` 7장. ③ `skill_fx.gd`: `_tex_cache`/`_fx_tex`(지연로드, 미존재 시 null 폴백)+`_painted`(Sprite2D 스케일업·회전·드리프트·페이드 트윈) 헬퍼, 콤보 `_spear_thrust`/`_spear_sweep`/`_spear_spin`·스킬 `slash`/`spin`·`impact` 에 페인티드 주역 레이어 합성, `bleed()`(핏방울+얼룩, 절제 톤) 신규. ④ `player.gd` `_on_hitbox_landed`·`_skill_ultimate` 에 `SkillFx.bleed` 배선. ⑤ **궁극기 연출 페인티드화**: VFX 2종 추가(`ult_burst` 데몬 폭발, `rune_circle` 바닥 부적 진법 탑다운) → `ultimate()` 에 바닥 진법(세로 납작·회전·페이드)+중심 폭발 버스트+보조 스파크 합성(기존 섬광/충격파 링/영혼창 코드는 유지). `.gitignore` 에 `.pl_tmp/` 추가. **PixelLab ~125 gen 사용. WSL엔 Godot 없어 인게임 합성은 PC 검증 대기**(후보 미감은 MCP 프리뷰로 확인함).
- **2026-06-27 (13차 — 조작 손맛 1차: 점프/이동 게임필 + 공격 런지)** — 사용자 "푸시 후 조작 손맛 작업". `scripts/player/player.gd` `_physics_process` 재구성: (a) 중력을 상승/하강·점프유지로 분기(FALL_GRAVITY_MULT 1.45 / LOW_JUMP_GRAVITY_MULT 1.9), (b) `_coyote_timer`(COYOTE_TIME 0.10)·`_jump_buffer_timer`(0.12)·`_jumping` 추가 — 점프를 "버퍼 입력 + (지상 or 코요테)"로 발동하고 상승 중 키 떼면 `velocity.y *= JUMP_CUT_MULT(0.45)`(가변 점프), (c) 좌우 이동을 즉시대입 → `move_toward` 가속/마찰(ACCEL 2400/AIR 1500, FRICTION 2800/AIR 700, 반대전환 시 +마찰, 정점 부근 APEX 가속 보너스), (d) `_lunge_vel` 1회성 전방 임펄스를 콤보(70/3타 130)·강타(150)에 부여 — move_and_slide 직전 1회만 더해 누적 버그 회피, (e) 빠른 착지(낙하속도 360+)에 가벼운 shake. 튜닝 const 13개를 파일 상단 한곳에 모음(되돌리기 쉽게). `tests/test_player_movement` 에 **variable_jump**(탭<풀점프 최고높이)·**coyote_jump**(발판 떠난 직후 점프 발동) 2케이스 추가(바닥 StaticBody 생성 헬퍼 포함). **이 환경(WSL)엔 Godot 미설치 → 헤드리스/플레이 검증은 사용자 PC 대기.**
- **2026-06-18 (12차 — 게임성 우선: 스토리/대화/NPC 제거)** — 사용자 방향("스토리 나중, 지금은 몬스터 잡고 클리어만"). ① `scripts/world/stage.gd` 에 `GAMEPLAY_ONLY`(기본 true)·`CHAIN`/`CHAIN_TSCN`/`CLEAR_SCENE` 추가 + `_build_gameplay`/`_clear_flag`/`_next_target`/`_spawn_exit`: 전투 스테이지 직선 체인(foothills→forest_deep→ruined_temple→mountain_pass→sacred_altar)만 빌드(NPC·대사·퀘스트·스토리픽업·auto_quest 스킵), 적 있는 신규 진입엔 전진 결계, 끝은 Clear. 되돌리기는 `GAMEPLAY_ONLY=false`. ② `scenes/ui/Clear.tscn`+`scripts/ui/clear_screen.gd` 신규(엔딩 대체). ③ `main_menu.gd`: START→Foothills, 새 게임 프롤로그 생략, 제목/로고/부제 숨김. ④ `scene_manager.gd` NON_GAMEPLAY_SCENES += Clear. ⑤ `tests/test_gameplay_chain` 신규(체인 5스테이지: 적 스폰·NPC0·자동대사0·전진출구·게이트 검증). 헤드리스 **31/31 그린** + 메뉴/스테이지/클리어 스크린샷.
- **2026-06-18 (11차 — 공격 모션 역동화 + 이펙트 강화)** — 피드백 반영. ① PixelLab v3 콤보 4종 재생성(attack/attack2/attack3/charge — 1·2·3타 모션 차별, 셋이 같은 찌름이던 문제 해결), `tools/pixel/integrate_combos.py`로 공개 프레임 URL→92×92 스트립·manifest 갱신. ② `scripts/combat/skill_fx.gd` 전면 업그레이드: `_line`/`_belly_curve`/`_ground_dust` 헬퍼, 콤보 다층 이펙트, slash/spin/impact/ultimate 화려화, **afterimage/afterimage_burst**(현재 AnimatedSprite2D 프레임 복제 색조 잔상) 추가. ③ `player.gd`: 콤보·차지·일섬·회천격·궁극기·회피에 `SkillFx.afterimage_burst` 트레일 배선. 크레딧 14/40(콤보 v3 8/10프레임은 2gen씩). 검증: 헤드리스 30/30 그린 + FxPreview/ComboPosePreview 스크린샷. (오브젝트형 VFX 스프라이트는 20~40gen/개라 트라이얼서 보류.)
- **2026-06-18 (10차 — PixelLab 주인공 아트 교체)** — PixelLab MCP(트라이얼 40gen)로 외부 AI 주인공 전면 교체. ① `create_character`(side/4dir/92px/heroic/high-detail) 1gen → **east 우향 프로필+창 측면 확인**. ② `animate_character` east 1방향씩(=1gen) 6종: idle/walk/jump/hurt/death 템플릿 + attack v3 창 찌름 → **7/40 소모**. ③ zip 추출(`metadata.json` 폴더명 generic → 프레임 수+몽타주로 idle(4)/walk(6)/jump(9) 식별). ④ `tools/pixel/integrate_pixellab.py` 신규: east 프레임→92×92 대칭 캔버스 가로 스트립+manifest, run/attack2/attack3/charge/dodge 무크레딧 파생, foot_offset 자동 산출(발행 90→−28). ⑤ `player_visual.gd` foot_offset −28. 검증 **헤드리스 30/30 그린 + 마을 인게임 스크린샷**. (콤보 sprite 는 attack 재사용 — 시각 차별은 기존 SkillFx 오버레이가 담당.)
- **2026-06-17 (9차 — 주인공 외모·창 휘두름)** — /goal 자율. ① 프레임 폭 **32→48px**(W/CX=24) — 창이 프레임에 안 잘리게. ② **각도 기반 일정 길이 창** `spear_angle()`/`_tip()`/SPEAR_LEN=24 — 휘둘러도 창 길이 불변(찌르기=평행이동, 쓸기/회전=각도 회전+sweep 잔상). attack/attack2/attack3/charge 재작성. ③ **얼굴 리디자인** head()/hat()/neck() — almond 눈·얇은 눈썹·매끈 콧대·좌상 광원, 갓끈 얼굴 밖, 갓 음영 정돈. 검증: 30/30 그린 + 스트립/인게임 스크린샷.
- **2026-06-17 (8차 — 주인공 모션 고도화)** — /goal 자율. "모션 조잡" 피드백 → `tools/pixel/gen_protagonist.py` 전 10종 애니 재작업: 프레임 증량(walk 8·idle 6·jump 3·attack 5·attack2 5·attack3 6·charge 3·dodge 4·hurt 3) + 애니 원칙 적용(예비동작/follow-through/무게 bob/2차 모션). 헬퍼 추가: 발끝 들림(feet lift), 창 sway/tassel 2차 모션, body lean, _atk_base 공통체. player_visual jump 프레임 velocity 매핑(상승/정점/하강). ANIMS fps 재조정. 검증: 30/30 그린 + 스트립/인게임 스크린샷.
- **2026-06-17 (7차 — 「귀창록」 리브랜딩·창 마검사)** — /goal 자율(토큰 한도까지). ① 주인공 무기 환도→**창(spear)** 전면 재생성(`tools/pixel/gen_protagonist.py` spear/spear_stand 헬퍼, 10종 애니 전부): idle/walk 창 세워 든 실루엣, 콤보 **1 직선찌름·2 횡쓸기·3 회전내려찍기**, charge 창끝 점화, death 창 떨굼. ② **콤보 이펙트 차별화** `SkillFx.combo(step)` — 찌름 랜스/횡 크레센트/회전 링+8방 스파이크, **3타 1.6배+홍 궤적**(player.gd). ③ **궁극기 '귀창 강림'**(skill_4, skills.json guichang, 쿨40·3.5배 광역·SkillFx.ultimate 섬광/충격파/12방 영혼창). ④ **NPC 상호작용 제거**(npc.gd body_entered 자동) + **대화 중 적 동결**(state_machine/patroller/boss/spirit_orb) + 보스 등장 자동 대사(foxqueen_intro/reaperlord_intro). ⑤ **자연 회복**(health_component.gd regen_rate/delay, Player.tscn 2.5/6s). ⑥ **보스 다양화**: FoxQueen(구미호 여왕·폐사지)·ReaperLord(저승 군주·관아) 중간보스 2종. ⑦ **세계관 재해석** 호환→호귀(虎鬼)·산군(山君), 제목/프롤로그/에필로그/크레딧/메인퀘스트 텍스트 귀창록 테마. ⑧ **세번째 다양 보스 도깨비 대장(GoblinKing)** 저잣거리 배치+자동 대사(보스 3종: 구미호 여왕/저승 군주/도깨비 대장 + 최종 산군). ⑨ **무기 창 계열 재명명**(철검→무쇠창/옥장도→옥자루 창/강철 검→강철 장창) + **마창·각성(spear_demon)** 최종보스 보상. ⑩ **타이틀 로고 재생성**(title_logo(_menu).png 호환기담→귀창록 — 메뉴 실제 표시 로고) + 보스 display_name 정리. 신규 테스트 3종(dialogue_freeze/ultimate/regen), 전체 **30 스위트 그린**.
- **2026-06-12 (6차 — 자율 고도화 전투/UX/정합)** — /goal 자율(토큰 한도까지). 감사(Explore) 후 임팩트순 12건: ① **잡몹 근접 공격**(patroller 예비동작→타격→넉백, 적별 차별, 거리판정 아군오사 없음) — 일방적 전투 해소. ② **대화 선택지 키보드 선택**(1~9/↑↓/Space, 번호 라벨·자동 포커스). ③ **적 처치 드롭**(회복약/엽전, Pickup.spawn 팩토리). ④ **피격 무적시간(i-frame)+피드백**(HealthComponent.invuln_on_hit 0.55s·붉은 깜빡임) — 무리 연타 공정성. ⑤ **메뉴 키보드 포커스**(Main/Pause/GameOver) + 사망→게임오버 검증. ⑥ **전투 게이트 '남은 적 N' 표시**. ⑦ **저승사자 원거리 영혼 구슬**(SpiritOrb, 회피 가능). ⑧ **보스 돌진 '!' 경고**(텔레그래프 가독성). ⑨ **산기슭 떠돌이 행상**(엽전 사용처 — 경제 루프 완성). ⑩ **레벨업 회복(40%)+저체력 HP바 경고색**. ⑪ **클리어 구간 재진입 무결성**(적·게이트 재생성 방지). 신규 테스트 9종, 전체 26스위트 그린 + 월드 연결성 OK.

- **2026-06-12 (3차 — 버그/스토리/이펙트/콘텐츠)** — /goal 4건. ① **추락 버그 수정**: 시각 흙바닥(2800px)보다 좁던 충돌 지면(1600px)을 2800 으로 정합 + player.gd 낙사 안전망(안전지점 상시 기록·y>1100 복귀)·종단속도 제한. test_fall_safety 2건. ② **스토리 전면 재설계 — 부자 인연**: 주인공=무당 단의 아들, 두령=사당 방화로 한이 된 산군. 프롤로그 6문단/어르신 고백/대장장이(아버지 환도)/아낙(사당 유물 부적)/상인(목격자)/보스 일갈/엔딩 전부 재집필(노드 ID 보존 → 테스트 유지). ③ **스킬 이펙트**: SkillFx autoload(코드 생성) — 일섬 참격 궤적·회천격 회전베기·호신부 부적 오라·적중 스파크. ④ **신규 스테이지 산신당 터**(숲→산신당 터→보스): 불탄 사당 프롭·구미호 영물 회상(아버지 이야기)·미니보스 팩(호랑이+도깨비2)·보물 궤짝. chest/shrine_ruin 프롭 생성, 저장/메뉴/BGM 통합. 테스트 16종 그린.

- **2026-06-12 (2차 — 처음부터 끝까지 플레이)** — /goal: 완주 가능한 하나의 게임으로 정비. ① **흐름 진단**(Explore 에이전트+전 씬 캡처): 씬 체인·퀘스트 5단계 전이가 엔딩까지 연결됨 확인, 결함은 보스 귀환 entry 불일치·이어하기 마을 고정·픽업 placeholder 정도. ② **오프닝 프롤로그**(Prologue.tscn — 호환 괴담 5문단 타자기, 스킵 가능 → 마을): 새 게임이 '스토리부터' 시작. ③ **이어하기 복귀 지역**(저장 메타 area→씬 역매핑), **보스 귀환 from_boss 정합**. ④ **픽업 스프라이트화**: 부적/약초/엽전/서찰 16px 아이콘(gen_pickups.py) + pickup.gd icon 자동부착·부유, 색박스 7곳 교체. ⑤ **엔드투엔드 통합 테스트**(test_playthrough 3건): 메인 5단계 완주·부적→호신부·엔딩 3분기. ⑥ 전투 밸런스 수치 확인(보스 380HP를 평타 ~21타/약7초, 회피·호신부 여유 — 완주 가능). 테스트 15종 그린.

- **2026-06-12 (스킬·키·음악·스토리)** — 사용자 피드백 4건 일괄 반영. ① **키 표준화**: 공격 J→X, 점프+Z, 회피+C, 스킬 1/2/3 신설 + **키 리바인딩 UI**(설정 메뉴, InputConfig autoload 가 user://input.cfg 저장·충돌 자동 제거·기본값 복원). ② **스킬 3종**(SkillManager autoload + assets/data/skills.json): 발도 일섬(1, 돌진 강타 1.8x, Lv3), 회천격(2, 앞뒤 타격+넉백, Lv5), 호신부(3, 피해 1회 무효, 부적 퀘스트 해금 — charm_blessing). HealthComponent.shield_charges, HUD 스킬 줄(잠김/쿨다운), 모바일 터치에 회피+스킬 버튼. ③ **BGM 비장하게 재합성**: 계면조 하강 선율 + 북(법고)/대금 보이스 + 5도 드론 — village 32s/forest 심장박동/boss 전고 19.2s/title 서곡 24s 신규(MainMenu·Ending 매핑). ④ **스토리 — 산신당 괴담 축**: 어르신 고백 2노드(스무 해 전 산신당 방화 = 두령의 정체), 보스전 입장 시 두령 일갈(AutoDialogue 트리거+boss_intro_seen), 떠돌이 상인 정체 복선 2노드, 부적 반환→가호 해금 연결, **엔딩 3분기**(사이드 0/일부/전부 완료별 문단). SceneManager.transitions_enabled 테스트 시임 추가. 테스트 15종(신규 test_skills 5건 포함).

- **2026-06-11 (자율 대작업 — 실제 돌아가는 비주얼 게임)** — 사용자 지시 "4.6.3 마이그레이션 + 이미지 직접 생성·적용 + 처음부터 끝까지 돌아가는 게임 + 권한 안 묻고 쭉". ① **마이그레이션**: project.godot features 4.6 / CI GODOT_VERSION 4.6.3 / `.gitignore` 사이드카 커밋 전환 / CLAUDE·README·STYLE_BIBLE 버전·컨셉 잠금 / `.claude` 권한 allowlist + `/goal` 커맨드. ② **픽셀아트 파이프라인**: `tools/pixel/`(palette.py=STYLE_BIBLE 25색 강제·검증, core.py=Canvas/strip/contact_sheet, 저장 시 팔레트 검증+8x preview) + `tools/Screenshot.tscn`(윈도우드 임의 씬 PNG 캡처, --scene/--out/--cam/--night)로 시각 자체검증 루프 확립. ③ **에셋 전량 생성**(워크플로 asset-forge + 직접): 주인공 11애님·적 5종(도깨비·구미호·저승사자·호환)·보스 호환두령(telegraph/heal 포함 6애님)·NPC 4종·타일&소품(기와집/초가집/우물/장승/솟대/소나무/등롱)·수묵 산수 배경 3겹·한지 UI 스킨·SFX 10+BGM 4(`tools/audio` 순수 파이썬 신디사이저). 세션 한도로 누락된 도깨비/구미호/보스는 직접 작성(구미호 꼬리 빈약 → 재작업). ④ **통합**: SpriteDb(manifest→SpriteFrames)·CharacterVisual·PlayerVisual·EnemyVisual·BgmDirector autoload, 적/NPC 씬 placeholder→AnimatedSprite2D 배선, 레벨 4종에 ParallaxBackdrop+타일 지면+한옥·소품 드레싱, 타이틀(로고+산수)·엔딩(두루마리 타자기) 완성, 대화 change_scene 액션으로 보스 처치→엔딩 연결. ⑤ Hitbox 게이트(이전 세션 layer 전환)·has_flag 타입분기 유지. 헤드리스 13종 52케이스 그린 유지. 스크린샷으로 타이틀·마을·들판·숲·보스·엔딩 전부 검증. **미감·손맛은 사용자 실플레이 확인 대기.**
- **2026-06-11 (테스트 전면 수리 + 4.6 호환)** — 헤드리스 스위트가 실제로는 절반쯤 좌초돼 있던 것을 발견·복구, **13종 52케이스 전부 통과** (로컬 Godot 4.6.3). ① 좌초 원인: (a) 대화 드레인 `while Dialogue.is_active(): advance()` 가 choices 노드에서 무한 루프(advance 는 choices 노드에서 no-op) — 테스트 4파일 11곳을 `_drain_dialogue()` 헬퍼(첫 선택지 고르기 + 32스텝 상한)로 교체. (b) Godot 4.4+ 파서 강화: 코루틴을 await 없이 호출(test_equipment/pickup_fx/scene)과 `var x := max(...)`/`var ok := dict.v and ...` Variant 추론(time_manager→`maxf`, test_combat→타입 명시)이 파스 에러 → 스크립트 로드 실패 → quit() 없는 빈 씬으로 타임아웃. (c) `has_flag` 의 `v == false or v == 0` Variant 교차 비교가 4.4+ 런타임 에러 — 타입별 분기로 재작성. ② **Hitbox 게이트 교체(전투 핵심 픽스)**: monitoring/monitorable 토글 게이트는 4.4+ 브로드페이즈에서 겹친 채 재활성 시 페어를 안 만들어 *정지 상태 공격이 헛스윙* — `collision_layer` 0↔1 게이트로 변경(hitbox.gd + boss.gd 수동 토글 2곳). 회피 무적 사이클(영구 무적 없음)과 밤 NPC 걸어들어오기 감지는 프로브로 확인. ③ 낡은 단정 2건: test_flags 필터링(샘플 JSON 3번째 선택지가 Phase 3에서 quest 게이트로 변경됨 — village_woman 의 if/unless_flag 로 재작성), test_questline 부적 반납(A2 약초 선택지 추가로 choose(1) 위치 어긋남 — 텍스트 기반 선택). ④ **test_polish 신규 10건** (H 자동저장 슬롯 0/메뉴 제외/토글, I 토스트 시작·완료/단계 침묵, J HP 바 표시·비율·자동 숨김, K NightOnly 초기화·페이즈 토글). ⑤ project.godot `flush_stdout_on_print=true`, .gitignore 에 `*.uid`/`*.import`(버전 확정 전 미커밋 방침).
- **2026-06-10 (3차 폴리시 — 전 세션 커밋 `40c7e41`, HANDOFF 반영 누락분)** — ① **H** 씬 전환 자동 저장: SceneManager._do_change 가 떠나기 전 슬롯 0(autosave) 저장, MainMenu/SettingsMenu 는 NON_GAMEPLAY_SCENES 로 제외. SlotPicker load 모드에 슬롯 0('자동 저장') 노출, MainMenu '이어하기' 활성 조건을 슬롯 0~3 으로 확대. ② **I** QuestToast autoload: quest_changed 를 듣고 시작/완료만 상단 토스트(2.4초 페이드), 단계 전이는 침묵. ③ **J** EnemyHpBar: patroller/dummy/boss 가 attach_to 로 동적 부착, 피격 시 2초 표시 후 숨김(보스는 y 오프셋 상향). ④ **K** NightOnly 노드 + 떠돌이 상인 NPC(Village, 밤 전용 등장, 야시장 open_shop 대화).
- **2026-06-09 (2차 확장)** — A~F 후속 6 트랙. ① **E2** 저장 슬롯 멀티: SaveManager.save 가 area/level/gold 메타 동봉, SlotPicker UI(load/save 양모드, 3 슬롯, 카드별 요약), MainMenu '이어하기'·PauseMenu '저장' 이 picker 호출. ② **D2** 상점: ShopManager autoload + ShopPanel UI(좌 구매·우 판매, 판매가는 정가 50%, 퀘스트 아이템 판매 차단), 대화 액션 `open_shop`, 대장간 어르신이 tiger_lord_resolved 이후 상점 해금. ③ **B2** 전투 폴리시: 회피 구르기(Shift, 무적 + dash + 0.6s 쿨다운), 보스 페이즈 2(HP ≤ 50% 에서 텔레그래프 0.70x · 회복 0.55x · 데미지 1.25x + 핏빛 톤). ④ **A2** 사이드 퀘스트 2종 추가(약초 5포기 수집·들판 서찰), Dialogue 조건 `if_has_item`/`if_inventory_at_least`, 액션 `take_item` 신규. ⑤ **F2** Locale 스캐폴드(ko/en JSON, MainMenu 시범 적용, locale_changed 시그널). ⑥ **C2/G2** 검증: 현 placeholder 박스가 사실상 ColorRect 기반 grid 역할을 수행 — 실제 TileMap 마이그레이션은 타일 텍스처 도착 후 사용자 PC에서. 헤드리스 테스트 +1종(test_shop_slots, 3건).
- **2026-06-09** — 콘텐츠 확장 6 트랙 (사용자 지시: A~F 일괄). ① **C** 맵/씬 전환: SceneManager.change_scene_to(path,entry) + LevelExit/LevelEntry, Village/TestLevel/Forest/BossArena 4씬, 명명 스폰 마커. ② **B** 전투 콘텐츠: ScreenFx autoload(shake/hit_stop) + Player 콤보(1-2-3타)·차지(누르고 떼면 강타) + 적 변종 4종(도깨비/구미호/저승사자/호환) + 보스 호환 두령(텔레그래프→돌진→회복 패턴). ③ **A** 퀘스트 라인: 메인 main_tiger_lord(5단계, 사이드 부적·대장간 2종 포함), Pickup/QuestTrigger 제너릭 Area2D, 어르신 대화에 if_quest_stage 분기로 어금니 보고 노드 추가. ④ **D** 장비/경제/낮밤: Equipment autoload(weapon/armor 슬롯, 인벤토리 패널 [장착] 버튼) + PlayerStats.gold + HUD 엽전 표시 + TimeManager(낮/밤 270초 사이클) + WorldTint autoload(CanvasModulate 자동 갱신). ⑤ **E** CI: GitHub Actions 두 워크플로(Web export + 헤드리스 테스트 전체), export_presets.cfg Web 프리셋 포함. ⑥ **F** 테스트 +4종(test_scene 3건, test_questline 3건, test_equipment 5건, test_pickup_fx 4건) — 총 11종 40+ 케이스.
- **2026-05-29 (후속)** — Phase 3 아트 무관 시스템 4단계: ① SceneManager(페이드 인/아웃) + MainMenu + SettingsMenu(볼륨 슬라이더 3종) — `run/main_scene` 변경 (`5d2f75f` + fix `8d2d642`). ② QuestManager + QuestLog UI(Q 키) + 대화 quest action(start/set_stage/complete/give_item) + choice 조건(if_quest_active/completed/stage) — 샘플 villager 에 적용, rewards.items 자동 지급 (`05815c1` + fix `2bc5d61`). ③ PlayerStats autoload(레벨/XP 곡선) + FloatingNumber(부유 데미지/XP 숫자) + PlayerHud 에 Lv/XP 표시 + Dummy/Patroller xp_reward 통합 (`f73f976`). ④ GameOverScreen — 사망 시 이어하기(슬롯1 로드+리로드) / 메인 메뉴 선택 (`fb208ef`). 헤드리스 테스트 +1종(quests, 3건).
- **2026-05-29** — Phase 2 아트 무관 시스템 6단계: ① SaveManager autoload — JSON 슬롯·save_requested/loaded 시그널 패턴 (`e864b46`). ② Inventory + items.json(5종) + InventoryPanel UI(I 키 토글) — SaveManager 자동 연동 (`aeaf970`, fix `4107c7e`). ③ Flags autoload + Dialogue actions/if_flag 확장 — 'set_flag' 액션 + 'if_flag/unless_flag' 조건 choices, 샘플 villager에 적용 (`720dbf4`, fix `8c44b07`). ④ 적 AI 상태머신 — AIState·StateMachine·Idle/Patrol/Chase + Patroller 적 (`234541b`). ⑤ 일시정지 메뉴(Esc) + 인벤토리 소모품 '사용' 버튼 — process_mode=ALWAYS, 슬롯 1 저장 버튼 (`5d2d6dc`, fix `41ae097`). ⑥ AudioManager autoload + SFX 6종 상수 + 공격/피격/사망/포션 hook (`546dc8a`, fix `ec324ac`). 헤드리스 테스트 추가 3종(save·inventory·flags = 11건).
- **2026-05-28** — Phase 1 골격 5단계: ① 입력 매핑(5 액션) + Player(중력/이동/점프) + TestLevel + 헤드리스 테스트(`0cb8858`). ② NPC + 분기형 대화 시스템(autoload Dialogue/DialogueBalloon · 샘플 JSON · 테스트 4건) (`f3b7426`). ③ 전투 컴포넌트(Hitbox/Hurtbox/HealthComponent) + 공격 hitbox + Dummy 적 + 전투 테스트 3건 (`55c1cfd`). ④ 모바일 터치 컨트롤(TouchScreenButton 5종 · 데스크탑 자동 숨김 · 원형 SVG placeholder) (`ee1129c`). ⑤ Player HUD HP 바 (`83ff048`). 모두 푸시 완료.
- **2026-05-28** — Phase 0 자동 셋업 4단계: ① CLAUDE.md placeholder 잠금 ② Galmuri 픽셀 폰트(11/9) + OFL 라이선스 자동 다운로드·배치 ③ `docs/STYLE_BIBLE.md` v0 (팔레트 25색·48px 비율·아웃라인·음영·Aseprite 파이프라인) ④ `docs/PROMPTS_PROTAGONIST.md` (코어 + A/B/C 변형 + 도구별 호환 + 실패 패턴). `docs/HUMAN_TASKS.md` 신설·정제.
- **2026-05-28** — 깃헙 private 레포 생성 + Godot 4 GDScript 골격 푸시 (project.godot · 1280×720 landscape · GL Compatibility · Nearest 필터 · 폴더 구조 · README · CLAUDE.md · 로드맵 문서).

## 시스템별 진행 현황
- [x] 깃헙 레포 + Godot 프로젝트 셋업 (2026-05-28)
- [ ] 주인공 스프라이트 1종 (Phase 0 스타일 확정) — 사용자 작업 대기 (`HUMAN_TASKS.md` 🅑·🅒)
- [x] 플레이어 이동/점프 — 골격 완성, AnimationPlayer는 스프라이트 후 (2026-05-28)
- [x] 전투(기본) — Hitbox/Hurtbox/HealthComponent/Dummy/Patroller + 상태머신(Idle/Patrol/Chase) (2026-05-28~29). 콤보·스킬·다양한 적 비주얼은 추후.
- [x] 인벤토리/아이템 — InventoryManager·items.json(5종)·InventoryPanel(I 키)·소모품 사용 + Save 연동 (2026-05-29)
- [x] 세이브/로드 — SaveManager 슬롯 기반 JSON, Inventory/Flags/Audio 자동 연동, 일시정지 메뉴에서 저장 (2026-05-29)
- [x] 대화/퀘스트 — 분기 대화 + Flags(set_flag 액션·if_flag 조건) 통합 (2026-05-28~29). 본격 퀘스트 라인은 추후.
- [x] UI/HUD (모바일 터치) — HP 바 + 5개 터치 컨트롤 + 인벤토리 패널 + 일시정지 메뉴 (2026-05-28~29). 한지·먹 톤 스킨은 폰트 임포트 후.
- [x] 오디오 — AudioManager + SFX 호출 hook (파일 미존재 시 no-op) (2026-05-29). **버스 구조 Master←Music/SFX + 리버브(공간감)·볼륨=버스 게인 (2026-07-01).** SFX 11종(+ultimate·ward) + BGM 9곡(마을/숲/보스/밤/타이틀 + 해원 river/grief/hollow/requiem) `tools/audio` 파이썬 합성, `bgm_director` 씬별 자동재생.
- [x] 게임 흐름 — MainMenu → 새로 시작/이어하기 → TestLevel → 사망 시 GameOverScreen → 이어하기/메인 메뉴. 설정 메뉴 볼륨 슬라이더(Master/SFX/BGM). 일시정지 메뉴에서 슬롯 1 저장 (2026-05-29).
- [x] 퀘스트 — QuestManager + QuestLog(Q 키) + 대화 quest action·condition + rewards 자동 지급 (2026-05-29). 메인 5단계 main_tiger_lord + 사이드 2종 (2026-06-09).
- [x] 스탯/성장 — PlayerStats 레벨/XP 곡선 + FloatingNumber 부유 숫자 + HUD 레벨/XP/엽전 표시 (2026-05-29~06-09).
- [x] 맵/씬 전환 — SceneManager.change_scene_to(path, entry), LevelExit/LevelEntry, Village/TestLevel/Forest/BossArena 4씬 (2026-06-09).
- [x] 전투 콘텐츠 — 콤보(1-2-3)/차지/ScreenFx shake+hit_stop, 적 변종 4종 + 보스 패턴 (2026-06-09).
- [x] 장비/경제 — Equipment autoload(weapon/armor) · PlayerStats.gold · 인벤토리 [장착] 버튼 · Pickup 의 gold 환산 (2026-06-09).
- [x] 낮/밤 — TimeManager 사이클 + WorldTint(CanvasModulate) 자동 색감 변화 (2026-06-09).
- [x] CI — GitHub Actions Web export 워크플로 + 헤드리스 테스트 워크플로 + export_presets.cfg Web 프리셋 (2026-06-09).
- [x] 폴리시 — 씬 전환 자동저장(슬롯 0) + 퀘스트 토스트 + 적 HP 바 + 밤 전용 NPC/야시장 (2026-06-10).
- [x] 헤드리스 테스트 — 13종 52케이스 그린. 좌초 6종 수리 + Godot 4.4+/4.6 호환 정비 + Hitbox layer 게이트 (2026-06-11).
- [x] 스킬 — 일섬/회천격/호신부 (쿨다운+레벨·퀘스트 해금) + HUD/터치 + 키 리바인딩 UI (2026-06-12).
- [x] 스토리 — 산신당 괴담(어르신 고백·보스 일갈·상인 복선) + 엔딩 3분기 (2026-06-12).
- [ ] web export 빌드 + 폰 미리보기 — Actions 실행 결과는 자동, 폰 확인은 사용자 PC/모바일
- [ ] GitHub Pages 배포 — 워크플로에 주석으로 준비, Settings → Pages 에서 켜면 활성화

## 다음 세션에게 남기는 메모
- 폰(텔레그램+Claude Code)과 PC를 오가며 작업하므로 이 파일을 항상 최신으로 유지하는 게 생명줄.
- 시각 확인 필요한 작업은 "완료"로 단정하지 말 것 — web export 또는 PC로 눈 확인 필요하다고 메모.

## 세션 종료 체크리스트 (매번)
- [ ] "현재 상태" 한 줄 갱신
- [ ] "바로 다음 할 일" 갱신
- [ ] "완료된 작업 로그"에 이번 작업 추가
- [ ] git 커밋 & 푸시
